# H5 端截图验收报告（2026-08-08）

> **验收基准**：`反馈.md` + `新建文本文档.txt` + `验收报告-2026-08-07.md`
> **截图目录**：`截图存档/2026-08-08-4/h5/`（8 张）
> **验收方式**：逐张截图注脚 + 功能实现对照 + 问题清单 + 修改方案

---

## 一、截图逐一注脚与功能对照

### 1. 匹配卡片页（01-discover-card.png）

**页面内容**：
- 顶部："寻觅 发现心动的人"、今日剩余 10 次
- 筛选栏：附近、不限、18-35岁、匹配度优先、全部筛
- 搜索框："搜索用户/标签/学校"
- 签到按钮："已签到"
- **卡片区域**：大部分空白，仅底部露出"小鹿 22岁 北京大学"
- 底部导航：首页、匹配（选中）、圈子、消息、我的

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 匹配页借鉴参考图，换排版 | ⚠️ 页面结构符合，但卡片区域大部分空白 | 部分符合 |
| 头像（露脸照） | ❌ 未显示 | 未实现 |
| 认证情况（机器+人工两次认证） | ❌ 未显示 | 未实现 |
| 个人 ID |  未显示（仅在底部露出名字） | 未实现 |
| 距离情况 | ❌ 未显示 | 未实现 |
| 活跃情况 | ❌ 未显示 | 未实现 |
| 基础资料 | ❌ 未显示 | 未实现 |
| 自我描述 | ❌ 未显示 | 未实现 |
| 喜好标签 | ❌ 未显示 | 未实现 |
| 悄悄话、喜欢/不喜欢 | ❌ 未显示 | 未实现 |
| 性格、mbti | ❌ 未显示 | 未实现 |
| 动态（点赞评论、私信） | ❌ 未显示 | 未实现 |
| 期待人物的画像 | ❌ 未显示 | 未实现 |

**问题**：
- **严重**：匹配卡片区域大部分空白，仅底部露出用户名字，核心匹配功能无法验证
- 与小程序端 02-discover.png 问题一致

---

### 2. 资料详情页-顶部（02-detail-top.png）

**页面内容**：
- 顶部导航："资料详情"、"主页"按钮
- 背景图：森林/自然风景
- 用户信息：小鹿 22岁
- 学校：北京大学·大三
- 匹配度 80%
- ID: CL-5、同校、离线
- 基础资料：22岁、165cm、本科、--月收入
- 个人简介：喜欢图书馆的下午和操场晚风
- 性格与MBTI：开朗外向、温柔体贴、幽默风趣、文艺安静
- 兴趣爱好：摄影、阅读、旅行
- 兴趣圈：摄影 113人加入、阅读 144人加入、旅行（部分可见）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 头像（露脸照） | ❌ 无头像显示 | 未实现 |
| 认证情况（机器+人工两次认证） |  无认证标识 | 未实现 |
| 个人 ID | ✅ 有 ID: CL-5 | 符合 |
| 距离情况 | ✅ 有"同校" | 符合 |
| 活跃情况 | ⚠️ 有"离线"，但反馈文档要求展示活跃状态 | 部分符合 |
| 基础资料 | ✅ 有年龄、身高、学历、月收入 | 符合 |
| 自我描述 | ✅ 有个人简介 | 符合 |
| 喜好标签 | ✅ 有摄影、阅读、旅行 | 符合 |
| 性格、mbti | ✅ 有性格标签（开朗外向等） | 符合 |
| 期待人物的画像 | ❌ 无期待画像 | 未实现 |
| 动态预览 | ❌ 无动态预览 | 未实现 |

**问题**：
- 缺少头像显示（背景图非用户头像）
- 缺少认证标识（机器+人工双重认证）
- 缺少期待人物画像
- 缺少动态预览
- 月收入显示"--"，数据缺失

---

### 3. 资料详情页-基础资料（03-detail-basic.png）

**页面内容**：与 02-detail-top.png 完全相同

**问题**：
- 截图重复，未展示不同内容
- 可能意图是展示基础资料区域，但与 02 截图完全一致

---

### 4. 资料详情页-性格MBTI（04-detail-personality.png）

**页面内容**：与 02-detail-top.png 完全相同

**问题**：
- 截图重复，未展示不同内容
- 可能意图是展示性格与MBTI区域，但与 02 截图完全一致

---

### 5. 资料详情页-兴趣圈（05-detail-circles.png）

**页面内容**：与 02-detail-top.png 完全相同

**问题**：
- 截图重复，未展示不同内容
- 可能意图是展示兴趣圈区域，但与 02 截图完全一致

---

### 6. 资料详情页-动态（06-detail-moments.png）

**页面内容**：与 02-detail-top.png 完全相同

**问题**：
- 截图重复，未展示不同内容
- 可能意图是展示动态区域，但页面未滚动到动态部分
- **严重**：无法验证动态功能是否实现

---

### 7. 签到弹窗（07-checkin-popup.png）

**页面内容**：
- 弹窗标题："每日签到"
- 积分：5000 分（大号黄色字体）
- 已连续签到 2 天
- 积分可在商城兑换权益
- 按钮："已签到"（绿色）、"去商城兑换"（白色）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 签到获得社交币/积分 | ✅ 有签到弹窗，显示 5000 分 | 符合 |
| 连续签到天数 | ✅ 显示"已连续签到 2 天" | 符合 |
| 积分兑换提示 | ✅ 有"积分可在商城兑换权益" | 符合 |
| 去商城兑换入口 | ✅ 有"去商城兑换"按钮 | 符合 |

**问题**：
- 基本符合反馈文档要求
- 5000 分数值过高，可能是测试数据

---

### 8. 匹配操作栏（08-discover-actions.png）

**页面内容**：
- 底部操作栏：三个圆形按钮
  - 左侧：白色圆形，灰色 × 图标（不喜欢）
  - 中间：绿色圆形，对话气泡图标（悄悄话/私信）
  - 右侧：粉色圆形，红色爱心图标（喜欢）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 喜欢/不喜欢操作 | ✅ 有 × 和爱心按钮 | 符合 |
| 悄悄话/私信 | ✅ 有对话气泡按钮 | 符合 |
| 底部操作栏布局 | ✅ 三按钮横向排列 | 符合 |

**问题**：
- 基本符合反馈文档要求
- 按钮尺寸和样式与参考图一致

---

## 二、问题汇总

### P0-严重问题（功能缺失/页面空白）

| 序号 | 截图 | 问题 | 影响 |
|------|------|------|------|
| 1 | 01-discover-card.png | 匹配卡片区域大部分空白，仅底部露出用户名字 | 核心匹配功能无法验证 |
| 2 | 06-detail-moments.png | 动态区域未展示，无法验证动态功能 | 功能缺失 |

### P1-重要问题（功能不符合要求）

| 序号 | 截图 | 问题 | 对照文档 |
|------|------|------|----------|
| 1 | 02-detail-top.png | 缺少头像显示（背景图非用户头像） | 反馈文档要求"头像（露脸照）" |
| 2 | 02-detail-top.png | 缺少认证标识（机器+人工双重认证） | 反馈文档要求"认证情况" |
| 3 | 02-detail-top.png | 缺少期待人物画像 | 反馈文档要求"期待人物的画像" |
| 4 | 02-detail-top.png | 月收入显示"--"，数据缺失 | 数据完整性问题 |
| 5 | 03/04/05/06 | 截图重复，未展示不同滚动位置的内容 | 验收不完整 |

### P2-次要问题（体验优化）

| 序号 | 截图 | 问题 |
|------|------|------|
| 1 | 02-detail-top.png | 活跃状态显示"离线"，反馈文档建议弱化离线状态 |
| 2 | 07-checkin-popup.png | 5000 分数值过高，可能是测试数据 |

---

## 三、验收结论

### 已实现功能（符合反馈文档）

1. ✅ 匹配页顶部结构（标题、筛选、搜索、签到）
2. ✅ 资料详情页基础信息（年龄、身高、学历、学校）
3. ✅ 个人简介/自我描述
4. ✅ 性格与 MBTI 标签
5. ✅ 兴趣爱好标签
6. ✅ 兴趣圈展示
7. ✅ 签到弹窗（积分、连续签到、兑换入口）
8. ✅ 底部操作栏（不喜欢、悄悄话、喜欢）
9. ✅ 底部导航栏

### 未实现/有问题功能

1. ❌ 匹配卡片区域空白（核心功能）
2.  资料详情页缺少头像
3. ❌ 资料详情页缺少认证标识
4.  资料详情页缺少期待人物画像
5. ❌ 资料详情页缺少动态预览
6. ❌ 月收入数据缺失
7. ❌ 截图重复，验收不完整

---

## 四、逐项修改方案（精确到文件 + 保证构建通过）

> 以下每个方案均给出：修改文件、修改内容、构建验证命令。按优先级排列，P0 优先。

---

### P0-1：匹配卡片区域空白（01-discover-card.png）

**根因分析**：
- 与小程序端问题一致：`apps/client/src/stores/discover/actions/fetch.ts` 中 `fetchCards` 返回空数据
- H5 端与小程序端共用同一套前端代码，问题根源相同

**修改方案**：

1. **后端：确认 `/api/v1/recommendations` 端点存在且返回数据**
   - 文件：`apps/api/src/main/java/com/campuslove/api/controller/RecommendationController.java`
   - 检查：`GET /api/v1/recommendations?userId=1` 是否返回 200 + 非空列表
   - 若端点不存在：需新增 Controller + Service

2. **前端兜底：fetchCards 返回空时展示 EmptyState**
   - 文件：`apps/client/src/pages/discover/index.vue`
   - 在 `CardSwiper` 组件外层增加 `v-if="cards.length > 0"` + `v-else` 展示 `EmptyState` 组件
   - 修改：在 `fetchCards` 的 `finally` 块中，若 `this.cards.length === 0 && !this.errorMessage`，设置 `this.errorMessage = "暂无推荐用户，请稍后再试"`

3. **构建验证**：
   ```bash
   pnpm --filter client run build:h5
   ```
   退出码 0 即通过。

---

### P0-2：动态区域未展示（06-detail-moments.png）

**根因分析**：
- 截图未滚动到动态区域，无法验证动态功能
- 可能原因：动态数据为空、组件未渲染、页面滚动问题

**修改方案**：

1. **检查动态数据源**：
   - 文件：`apps/client/src/pages/discover/detail.vue`（或对应详情页）
   - 检查是否调用 `fetchUserMoments(userId)` 获取用户动态
   - 若动态数据为空，需后端填充测试数据

2. **前端：动态区域增加空态提示**：
   - 若动态列表为空，展示"暂无动态"空态，而非空白
   - 文件：`apps/client/src/components/discover/UserMoments.vue`（或对应组件）

3. **构建验证**：
   ```bash
   pnpm --filter client run build:h5
   ```

---

### P1-1：资料详情页缺少头像（02-detail-top.png）

**根因分析**：
- 详情页使用背景图（森林风景）作为顶部视觉，未显示用户头像
- 反馈文档要求"头像（露脸照：可真可假、但一定要露脸的照片）"

**修改方案**：

1. **文件**：`apps/client/src/pages/discover/detail.vue`
2. **增加头像显示**：
   ```vue
   <view class="user-avatar">
     <SafeImage :src="user.avatarUrl" mode="aspectFill" />
     <view v-if="user.certificationStatus" class="certification-badge">
       <text>已认证</text>
     </view>
   </view>
   ```
3. **样式调整**：
   - 头像尺寸：80px 圆形，白色描边
   - 位置：悬浮在背景图下方，与昵称同行
   - 认证标识：头像右下角小图标

4. **构建验证**：
   ```bash
   pnpm --filter client run build:h5
   ```

---

### P1-2：资料详情页缺少认证标识（02-detail-top.png）

**根因分析**：
- 反馈文档要求"认证情况（机器认证+人工认证两次认证）"
- 当前页面仅显示"同校"，无认证标识

**修改方案**：

1. **文件**：`apps/client/src/pages/discover/detail.vue`
2. **增加认证标识**：
   ```vue
   <view class="certification-info">
     <view class="cert-badge machine">
       <text>机器认证</text>
     </view>
     <view class="cert-badge manual">
       <text>人工认证</text>
     </view>
   </view>
   ```
3. **后端：确保返回认证状态字段**：
   - 文件：`apps/api/src/main/java/com/campuslove/api/entity/User.java`
   - 字段：`machineCertified`（boolean）、`manualCertified`（boolean）
   - 接口：`GET /users/{id}` 返回认证状态

4. **构建验证**：
   ```bash
   pnpm --filter client run build:h5
   ```

---

### P1-3：资料详情页缺少期待人物画像（02-detail-top.png）

**根因分析**：
- 反馈文档要求"期待人物的画像（目前暂时不去分恋爱和交友）"
- 当前页面无此模块

**修改方案**：

1. **文件**：`apps/client/src/pages/discover/detail.vue`
2. **增加期待画像模块**：
   ```vue
   <view class="expectation-section">
     <text class="section-title">我期待遇见的你</text>
     <text class="expectation-text">{{ user.expectationProfile }}</text>
   </view>
   ```
3. **后端：确保返回期待画像字段**：
   - 文件：`apps/api/src/main/java/com/campuslove/api/entity/UserBasicProfile.java`
   - 字段：`expectationProfile`（text）
   - 若字段不存在，需新增

4. **构建验证**：
   ```bash
   pnpm --filter client run build:h5
   ```

---

### P1-4：月收入数据缺失（02-detail-top.png）

**根因分析**：
- 月收入显示"--"，数据缺失
- 可能原因：用户未填写、后端未返回

**修改方案**：

1. **后端：检查 `user_basic_profile` 表 `monthly_income` 字段**：
   - 文件：`apps/api/src/main/java/com/campuslove/api/entity/UserBasicProfile.java`
   - 字段：`monthlyIncome`（String 或 Enum）
   - 若字段为空，返回默认值或隐藏该字段

2. **前端：数据缺失时隐藏字段而非显示"--"**：
   ```vue
   <view v-if="user.monthlyIncome" class="info-item">
     <text>{{ user.monthlyIncome }}</text>
     <text class="label">月收入</text>
   </view>
   ```

3. **构建验证**：
   ```bash
   pnpm --filter client run build:h5
   ```

---

### P1-5：截图重复问题（03/04/05/06）

**根因分析**：
- 03、04、05、06 截图完全相同，未展示不同滚动位置
- 可能原因：截图时页面未滚动、自动化脚本问题

**修改方案**：

1. **重新截图**：
   - 03-detail-basic.png：应展示基础资料区域（年龄、身高、学历、收入）
   - 04-detail-personality.png：应展示性格与 MBTI 区域
   - 05-detail-circles.png：应展示兴趣圈区域
   - 06-detail-moments.png：应展示动态区域

2. **截图脚本优化**：
   - 每次截图前执行页面滚动
   - 文件：截图自动化脚本（若有）
   - 增加滚动逻辑：`window.scrollTo(0, scrollPosition)`

3. **验收标准**：
   - 每张截图展示不同区域
   - 动态区域需展示动态列表或空态提示

---

### P2-1：活跃状态显示优化（02-detail-top.png）

**根因分析**：
- 当前显示"离线"，反馈文档建议弱化离线状态
- 反馈文档："弱化离线状态的展示，优先展示「刚刚活跃」「今天活跃」"

**修改方案**：

1. **文件**：`apps/client/src/pages/discover/detail.vue`
2. **修改活跃状态显示逻辑**：
   ```ts
   function formatActiveStatus(lastActiveAt: string): string {
     const diff = Date.now() - new Date(lastActiveAt).getTime();
     const minutes = Math.floor(diff / 60000);
     const hours = Math.floor(diff / 3600000);
     const days = Math.floor(diff / 86400000);
     
     if (minutes < 5) return '刚刚活跃';
     if (minutes < 60) return `${minutes}分钟前活跃`;
     if (hours < 24) return `${hours}小时前活跃`;
     if (days < 7) return `${days}天前活跃`;
     return '离线'; // 仅超过 7 天才显示离线
   }
   ```
3. **样式调整**：
   - "刚刚活跃"/"X 小时前活跃"：绿色文字
   - "离线"：灰色小字，降低视觉权重

4. **构建验证**：
   ```bash
   pnpm --filter client run build:h5
   ```

---

## 五、修改优先级与执行顺序

### 第一批（P0，核心功能修复）

1. P0-1：匹配卡片空白 → 后端确认推荐接口 + 前端 EmptyState 兜底
2. P0-2：动态区域未展示 → 检查动态数据源 + 增加空态提示

### 第二批（P1，功能完善）

3. P1-1：资料详情页增加头像
4. P1-2：资料详情页增加认证标识
5. P1-3：资料详情页增加期待人物画像
6. P1-4：月收入数据缺失处理
7. P1-5：重新截图（03/04/05/06）

### 第三批（P2，体验优化）

8. P2-1：活跃状态显示优化

### 构建验证总命令

每批修改完成后，执行：

```bash
# H5 端构建
cd apps/client
pnpm --filter client run build:h5

# 若有后端修改
cd ../api
mvn clean package -DskipTests
mvn flyway:migrate  # 若有数据库变更
```

全部退出码 0 即构建通过。

---

## 六、验收标准（修改后重新截图验收）

修改完成后，需重新执行截图走查，验证以下页面：

1. **01-discover-card.png**：匹配卡片区域应显示完整用户卡片（头像、昵称、认证、距离、活跃、基础资料、自我描述、标签、MBTI、期待画像、动态预览）
2. **02-detail-top.png**：资料详情页应显示用户头像 + 认证标识
3. **03-detail-basic.png**：应展示基础资料区域（与 02 不同）
4. **04-detail-personality.png**：应展示性格与 MBTI 区域（与 02 不同）
5. **05-detail-circles.png**：应展示兴趣圈区域（与 02 不同）
6. **06-detail-moments.png**：应展示动态区域（动态列表或空态提示）
7. **07-checkin-popup.png**：签到弹窗功能正常（已验证）
8. **08-discover-actions.png**：底部操作栏功能正常（已验证）

---

**报告生成时间**：2026-08-08
**截图总数**：8 张
**验收页面**：8 个
**已实现功能**：9 项
**未实现/有问题功能**：7 项
**修改方案**：8 项（P0 二项、P1 五项、P2 一项）

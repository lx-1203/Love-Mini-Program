# 验证截图目录说明（2026-08-26）

## 状态：待 GUI 环境补拍

当前自动验证会话为**无头环境**（微信开发者工具 GUI 窗口无法创建，MainWindowHandle=0），
模拟器运行与截图不可行。本目录已按页面划分好截图存放位置，**在有桌面会话的机器一键补跑**。

## 一键补跑命令

```bash
# 前置：开发者工具已打开项目 + 扫码登录 + 「设置→安全设置」开启服务端口 + 自动化端口 9420 已监听
node scripts/qa-full-screenshot.cjs
# 可选：只截部分页面
node scripts/qa-full-screenshot.cjs pages/home/index,pages/chat-session/index
```

输出：本目录下 `<页面路径>/NN-说明.png` + `screenshot-summary.json`（PASS/FAIL 汇总）。

## 截图比对基准

| 页面 | 截图点 | 比对基准（参考素材） |
| --- | --- | --- |
| 登录/注册 | 正常态、注册表单、体验号入口 | `素材/` 登录页分目录 |
| 首页 | 帖子区三态（loading/正常/空）、社区动态 | `素材/` 首页分目录 |
| 附近 | 圈层列表、附近动态、未登录引导 | `素材/` 附近分目录 |
| 帖子详情 | 正文+图片、评论、骨架屏 | `素材/` 附近/帖子 |
| 消息 | 会话列表 | `素材/` 消息分目录 |
| 聊天 | 气泡 self/peer 同屏 3+3 条（量测外轮廓差 ≤2rpx） | `素材/` 消息分目录；`参考/` 65 张 |
| 我的 | 主页、认证中心、编辑名片 | `素材/` 主页分目录 |
| 边界 | 未登录访问、断网错误态+重试 | - |

## 已完成的代码级比对（无需 GUI）

- **R3 气泡尺寸**：`verify-bubble-token.cjs` 30 项断言全 PASS——self/peer 共享全部 12 个
  `--bubble-*` token（max-width 84% / padding 16/24rpx / 主圆角 20rpx / 尾巴 4rpx /
  阴影 none / 字号 30rpx / 头像 64rpx），**外轮廓与高度差 0rpx**（≤2rpx 达标）
- **R1 帖子字段**：`verify-post-images.cjs` 断言 posts/dashboard images 全 PASS

## 截图后动作

1. 每张截图对照上表基准逐项比对，记录差异
2. 发现 UI 差异：截图 + 差异说明回传主理人，转工程师修复
3. 补拍完成后，将比对结论回填 `报告/QA验收报告-系统修复-2026-08-26.md`（五、全页面截图 章节）

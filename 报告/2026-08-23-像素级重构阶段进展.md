# 像素级重构 · 阶段进展报告（2026-08-23）

## ✅ 阶段 0：进入即登录（已完成）
- 后端 `MockRuntimeState` 默认 `loggedIn=true`，`/auth/me` 返回 `{"userId":"user-1001","loggedIn":true,...}`。
- 小程序首屏即为**登录态首页**（无需手动登录）。
- 全接口 GET+POST **5xx=0**；前端 `pnpm typecheck`/`uni build --platform mp-weixin` 通过。

## ✅ 素材盘点与映射（源自 `素材/图标素材总索引.txt`）
- **首页**：`首页/最终/拆分图标-精修版/`（65 个，7 类：tabbar-top/buttons-badges/tags/photos-stats/task-cards/circle-entries/toolbar）。
- **登录页**：`登录页/拆分图标_登录页/`（52 个）。
- **匹配**：`匹配/最终/拆分图标_匹配/`（70 个）。
- **消息**：`消息/最终/拆分图标_消息/`（114 个）。
- **我的**：`主页/最终/个人/拆分图标_主页_个人/`（28 个）。
- **他人主页**：`主页/最终/他人/拆分图标_主页_他人1/`（64）、`拆分图标_主页_他人2/`（38）。
- **吉祥物**：`吉祥物/拆分图标_吉祥物参考/`（64 个 decor/expression）+ `吉祥物/xunmi_mascot_design_system_v2/`（mascot/expression/state/chat/nav/avatar/decor 全套 PNG）。
- 每个拆分目录含 `manifest.json`（程序可读）/ `图标清单.txt` / `图标预览_联系表.png`。

## 📷 截图 + 对比闭环（已就绪）
- `node scripts/mp-shoot-reference.cjs`：对页面矩阵逐页截「顶+底」，输出 `截图存档/<date>-重构N/`。
- 权威集：`截图存档/2026-08-22-重构3/`（32 张登录态）。
- 视觉对比：`D:\6\glm-vision-mcp`（已配 key，GLM-4.6V-Flash）可 `analyze_image`/`analyze_images` 做 OCR/差异描述；回退 `view_image`。

## 下一步（逐页重构）
按页面矩阵，把上述拆分 PNG 落进 `apps/client/src/static/` 并登记 `config/images.ts`，逐页「参考图→组件→拆分 PNG→重建→重截→对比」收敛；每页改后重截 `重构N` 并在本报告附「理想图 + 实际图 + 最小差异 + 修改建议」，末尾列「缺失素材清单」。
---

## 阶段 1A · 首页拆分素材集成（已完成，2026-08-23）
- **拷贝**：`素材/首页/最终/拆分图标-精修版/06-circle-entries/`（9 个兴趣圈入口/按钮/角标）、`07-toolbar/`（13 个社区工具栏图标）→ `apps/client/src/static/assets/images/home-split/{circle,toolbar}/`。
- **登记**：`config/images.ts` 新增 `HOME_SPLIT` 组（CIRCLE_PHOTO/TRAVEL/MUSIC/FOOD/SPORTS/READING、JOIN_BTN、MEMBER_BADGE、HOT_BADGE、LIKE/COMMENT/SHARE）。
- **验证**：`pnpm typecheck` 零错误；`uni build --platform mp-weixin` **DONE Build complete**；dist 已包含 `home-split/circle/*.png`（9 个）。
- **语义映射**（源自 manifest.json）：circle_row6_01=摄影圈卡、02=旅行、03=音乐、04=美食、05=运动、06=读书；07=加入按钮、08=成员角标、09=热度角标；toolbar_row7_01=点赞、02=评论、03=分享。

## 下一步
将 `HOME_SPLIT` 接入组件（兴趣推荐封面/加入、社区动态点赞/评论/分享），重建后重截 `重构8` 用 glm-vision 对照收敛。
---

## 阶段 1B · 登录页拆分素材接入（已完成，2026-08-23）
- 拷贝 `素材/登录页/拆分图标_登录页/登录页_r*.png`（52 个）→ `apps/client/src/static/assets/images/login-split/`。
- 登记 `images.ts` 新增 `LOGIN_SPLIT` 组（52 项，键=`rXX_cXX`，程序可引用）。
- 验证：`pnpm typecheck` 零错误；`uni build --platform mp-weixin` 成功；dist 已含 `login-split/*.png`（52 个）。
- 登录页逐像素重构将基于 `LOGIN_SPLIT` + 参考 `登录页.png` 逐区域接入（需视觉核对，避免错位）。

## 已接入的拆分素材（累计）
- 首页：社区工具栏 `toolbar_row7_*`（赞/评/享）、兴趣整卡 `HOME_SPLIT.CIRCLE_*`、社区配图 3 帖；`HOME_SPLIT` 全部登记。
- 登录页：`LOGIN_SPLIT` 52 项已登记并捆绑。
- 吉祥物：`拆分图标_吉祥物参考`(64) + `xunmi_mascot_design_system_v2` 已盘点，待接入（等待/消息/登录 hero）。
---

## 阶段 1 · 全页面拆分素材接入（全部完成，2026-08-23）
- **首页**：`HOME_SPLIT`（兴趣圈 9 + 工具栏 13 = 22）。
- **登录页**：`LOGIN_SPLIT`（52）。
- **消息页**：`MESSAGES_SPLIT`（114）。
- **主页（个人/他人）**：`PROFILE_SELF_SPLIT`(29) + `PROFILE_OTHER1_SPLIT`(65) + `PROFILE_OTHER2_SPLIT`(39) = 133。
- **匹配页**：`MATCH_SPLIT`（71）。
- **总计**：**392 个拆分 PNG** 已拷入 `apps/client/src/static/assets/images/`、登记 `config/images.ts`、并捆绑进 `dist/build/mp-weixin`。
- **吉祥物**：`拆分图标_吉祥物参考`(64) + `xunmi_mascot_design_system_v2`(mascot/expression/state/chat/nav/avatar/decor 全套) 已盘点；应用内 `mascot/*.png`(38 个) 已被等待/消息页正常引用。
- **验证**：`pnpm typecheck` 零错误；`uni build --platform mp-weixin` DONE Build complete；全页面素材均已在 dist 中。

## 素材接入完成度
全部页面的拆分素材已**可用并捆绑**，下一步是逐页接入组件（替换占位/emoji/SVG 为拆分 PNG），每页重截并用 glm-vision 对比收敛。
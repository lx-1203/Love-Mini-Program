# 2026-08-31 第六轮全面审查报告（商业化长期项目级）

- **审查对象**：`d:\6\恋爱小程序`
- **主扫描目录**：`apps/client/src`（components/pages/subpackages/stores/styles/utils/services）
- **辅助扫描**：`apps/api/src/main/java`
- **报告性质**：聚焦真实命中、宁缺毋滥，不做逐行吹毛求疵
- **总体结论**：前端工程治理度较高（无 `console.log`、无空捕获、ESLint 严格、主题 token 化基本完成），本轮无 P0 级阻断项；主要风险集中在**仓库卫生（.mimosa 残留已入库）**、**路由配置漂移**、**少数裸硬编码**三类。

---

## 一、审查范围与方法

| 维度 | 方法 |
|---|---|
| 硬编码（颜色/文案/数字） | 正则全局统计 `#[0-9a-fA-F]{6}` 出现频率 + 抽查关键文件上下文 |
| 技术债 | 统计 `TODO/FIXME/HACK/@ts-ignore/eslint-disable` + 磁盘残留目录/文件 |
| Bug 隐患 | 扫描空捕获、裸 catch、`navigateTo` 无兜底、mock 短路逻辑 |
| 安全合规 | 产物内 `console.*`、明文敏感文件（`.pem/.key`）、mock 数据进生产引用 |
| 功能完整性 | `pages.json` 全部主包/分包页面 ↔ `constants/routes.ts` 常量比对 |
| 后端隐患 | `@Scheduled`/ThreadLocal/Mock 服务抽样 |

> 说明：颜色 `var(--token, #hex)` 属 CSS 变量 + fallback 的标准用法，**不计为硬编码**；仅统计真正裸写的十六进制。

---

## 二、硬编码问题清单

### 2.1 颜色（专题统计）

- **频率**：`*.vue` 中共 40 个文件 / 630 处 `#[0-9a-fA-F]{6}`。
- **定性**：绝大多数为 `var(--c-xxx, #hex)` 带 fallback（规范用法，已 token 化）。真正裸写集中在登录页与个别旧页面。

| 文件 | 行号 | 硬编码内容 | 建议 |
|---|---|---|---|
| `pages/login/index.vue` | 776 / 814 | `color: #1A1E1C` | 用 `var(--c-text-primary)` |
| `pages/login/index.vue` | 794 | `color: #6B7571` | 用 `var(--c-text-secondary)` |
| `pages/login/index.vue` | 824 | `color: #36C99A` | 品牌色裸写，改用 `var(--c-brand)` |
| `pages/login/index.vue` | 1052 | `color: #ffffff` | 改用 `var(--c-neutral-0)` |
| `pages/login/index.vue` | 1144–1145 | `linear-gradient(135deg, #3B9DE5, #7C6CF0)` | 代码已注明“无对应 token 保留原值”--建议补 `--c-gradient-brand-purple` token |
| `pages/profile/index.vue` | 4460 | `var(--c-bg-page, #f4f6fa)` 与同文件 `#F7FAF9` 并存 | 页面底色两套值，核实统一 |
| `App.vue` | 743/751/761 | `var(--c-brand, #36C99A)` | fallback 合法，无需处理（供参考） |

> 备注：`campus/hub.vue`（54 处）、`pages/nearby/index.vue`（56）、`pages/messages/index.vue`（53）为 hex 高频文件，抽验后多为 `var()` fallback，未发现批量裸写。

### 2.2 文案（i18n）

- **定性**：项目已建 i18n（`i18n/locales/{zh-CN,en-US}.ts` + `t()`）与 `config/status-copy.ts`，绝大多数 `.vue` 走 `t('...')` 或 status-copy。本轮未发现模板中成规模绕过 i18n 的中文字面量。
- **残留**：`pages.json` 内为配置型中文（tabBar `text`），属恢复正常；`custom-tab-bar/index.js` 中 tab 文案为硬编码中文（随 `navigation.ts` 同步，非 i18n）--风险为仅覆盖中文、英文包不同步，标注 P2。

### 2.3 数字（magic number）

- 未发现明显失控的 `750/375/1000` 类设计稿尺寸散落；rpx 单位值属小程序合法写法。
- `utils/navigation.ts:762` 等 `calc(var(--status-bar-height,0px) + 88rpx)` 为状态栏兜底常量，合理。
- 建议：尺寸已 token 化（`--sp-*`），继续保持，无强制处理项。

---

## 三、技术债清单

### 3.1 冗余残留（已入库，重点）

| 路径 | 规模 | 说明 | 级别 |
|---|---|---|---|
| `apps/client/src/.mimosa/` | **324 文件 / ≈2.8MB，已被 git 跟踪** | AI 工具 hook 状态残留，不应入库/打包。`.gitignore` 写的是根锚定 `/.mimosa/`，**未覆盖深层级路径** | **P1** |
| `apps/client/src/=0` | 1 个 | 可疑空/误建文件，已 tracked | P2 |
| `apps/client/main.ts` | 40 字节 | 与 `src/main.ts`（5.3KB）重复的残次副本，已 tracked，无引用 | P2 |
| `apps/client/src/entry-server.js` | — | 全局无 `entry-server` 引用，死代码，已 tracked | P2 |
| `apps/admin-legacy/` | — | 历史被替换后台，未清理 | P3 |

### 3.2 TODO / FIXME / 禁用注释

- **FIXME（token 化缺口）**：
  - `components/login/LoginLogo.vue:71` -- 0.25 alpha 无精确 token
  - `components/home/HomeBanner.vue:207,215` -- 0.35 alpha 需新增 shadow 强 token
- **TODO(backend)（前端模拟、待后端补接口）**：
  - `stores/messages.ts:325/1152/1168` -- sourceUserId / 会话未读元数据 / markSessionUnread
  - `stores/likes.ts:1035` -- 批量 skip 接口缺失
  - `subpackages/chat/chat-session/index.vue:793` -- POST /users/{id}/block 缺失
- **TODO(dispose-接线)**：`stores/village/index.ts:1171`、`stores/discover/actions/storage.ts:129`、`stores/checkin.ts:547` -- 内存缓存 dispose 引用点待接线，属潜在内存增长点（P2）。
- **eslint-disable**：共 8 处，均带说明（`debounce.ts` 泛型 any、`CardDetailOverlay.vue` 共享引用 props、`fixtures.ts` mock），合理豁免，无需处理。

---

## 四、Bug 与隐患清单

### 4.1 已确认良好（无）项
- **空捕获 `catch{}`**：全仓=0（所有 `catch (e)` 均接变量，注释明确规避 mp-weixin 不兼容写法）。
- **`console.log`**：全仓=0；`console.error/warn/info/debug` 共 363 处/92 文件（多为 websocket/sentry 日志链路）。
- **导航封装**：`utils/navigation.ts` 的 `openAppPath`/`switchTabWithQuery` 均带 `fail` 回调 + tab query storage 桥接，治理良好。

### 4.2 需修复项

| 文件 | 行号 | 问题 | 级别 |
|---|---|---|---|
| `pages/profile/index.vue` | 802 | **绕过 `openAppPath` 直接裸 `uni.navigateTo({ url: "/pages/login/index" })`**，无 `fail` 兜底，且用裸路径字符串（未用 `ROUTES.LOGIN`，与 3 抽象口径不一致） | P2 |
| `utils/navigation.ts` | `replaceAppPath` 240–249 | `uni.switchTab`/`redirectTo` 未透传 `fail`（`openAppPath` 有而 `replaceAppPath` 无），调用方无法感知失败 | P2 |
| WebSocket 链路 | `stores/session.ts`(22 处)、`services/websocket/` | 大量 `console.warn` 未统一收敛到 sentry/logger，生产日志噪音与敏感信息泄漏风险 | P2 |
| `stores/village/discover/checkin` | 内存缓存 dispose 未接线 | 页面反复进出可能累积缓存，内存风险低但待接线（见 3.2） | P2 |

---

## 五、功能完整性核对（pages.json ↔ ROUTES.ts 漂移）

- **主包 6 页**：全部在列。
- **分包共 16 个 root**：整体匹配度高。

**经比对，以下 3 个已注册页面未在 `constants/routes.ts` 登记 ROUTES 常量，业务中以裸字符串跳转，构成漂移：**

| 页面（pages.json 存在） | 裸字符串引用位置 | 建议 |
|---|---|---|
| `subpackages/profile-extra/profile/favorites`（收藏页） | `pages/profile/index.vue:862`（`favorites: "/..."` 菜单映射） | 补 `ROUTES.PROFILE.FAVORITES` |
| `subpackages/setup/interest/index`（兴趣设置） | `pages/profile/index.vue:859,2356` | 补 `SUBPACKAGE_ROUTES.SETUP_PROGRESS.INTEREST` |
| `subpackages/market/detail/index`（商品详情） | `subpackages/market/shop/index.vue:236`（`/${id}` url 拼接） | 补 `ROUTES.MARKET.DETAIL` |

> 这些页面当前靠硬编码路径跳转，未统一经 ROUTES 常量，未来改路径时易遗漏。属 P2 一致性/可维护性漂移，不影响运行。

---

## 六、安全/合规快速扫描

| 项 | 结论 | 级别 |
|---|---|---|
| 明文密钥入库 | **`scripts/dev-proxy/cert.pem` 与 `key.pem` 已被 git 跟踪**（`.gitignore` 无 `*.pem` 规则）。私钥入库为真实风险，需确认是否为自签测试证书并移交 gitignore | **P1** |
| mock 数据进生产 | `pages/profile/index.vue:24` 等**生产页面直接 `import mockFixtures`**；`guards/campus-gate.ts`、`stores/vip.ts` 用 `useMock()` 短路后返回裸真（数据绕过安全门控）。若误配置生产 `mode=mock` 会泄露演示数据/绕过认证门 | P2 |
| 生产 console | 无 `console.log`；websocket 链路 `console.warn` 集中在 `session.ts`/`store-dispatch.ts` | P2 |
| 后端 Mock 服务 | `main`（非 test）下存在 `shop/MockProductService.java`、`consulting/MockConsultingService.java`，未见 `@ConditionalOnProperty`；`campus` 的 simulate 已正确用条件注册（`DemoCertificationSimulateController`）--商品/咨询 Mock 是否按环境隔离需核实 | P2 |
| `RealCampusCertificationService.simulateApprove` | 固定抛 `UnsupportedOperationException`，若 flag/env 误配对会 500 | P3 |

---

## 七、问题分级汇总

| 级别 | 数量 | 说明 |
|---|---|---|
| **P0** | **0** | 无崩溃/资损/阻断类问题 |
| **P1** | **2** | ① `.mimosa` 324 文件（≈2.8MB）已入库且深层路径未被 gitignore；② `scripts/dev-proxy/key.pem` 私钥入库 |
| **P2** | **~9** | 路由漂移 3 处；裸 `navigateTo` 无兜底 1 处；`replaceAppPath` 无 fail；登录页裸色值 5 处；mock 进生产引用；websocket 日志噪音；dispose 未接线 3 处；`main.ts`/`=0`/`entry-server.js` 残留等 |
| **P3** | 若干 | `admin-legacy`、商品/咨询 Mock 激活条件核实、`tabBar` 中文 i18n 缺口、尚未清理的临时脚本 |

---

## 八、修复优先级建议

1. **P1 立即**：
   - 将 `apps/client/src/.mimosa/` 全量 `git rm -r --cached`，并在 `.gitignore` 追加不含根锚定的 `**/.mimosa/`，防止再入库与打包体积污染。
   - 审计 `scripts/dev-proxy/key.pem` 生成方式，确认自签性质后加入 gitignore 并为其撤销跟踪，避免私钥外泄。
2. **P1→P2 尽快**：
   - 为 3 个漂移页面补 `ROUTES` 常量，替换裸字符串跳转。
   - 登录页 5 处裸色值 + 渐变统一收敛到 token。
   - `pages/profile/index.vue:802` 改走 `openAppPath`；`replaceAppPath` 补 `fail` 透传。
3. **P2 排期**：
   - 清理 `=0`、`apps/client/main.ts`、`entry-server.js` 死代码副本。
   - mockFixtures 的页面级 import 收敛到 dev-only 环境（构建剔除）。
   - 完成 `dispose` 接线、统一 websocket 日志到 sentry logger。
4. **P3 备忘**：`admin-legacy` 归档、后端 Mock 服务激活条件复核、`tabBar` 文案纳入 i18n。

---

*本报告由 2026-08-31 第六轮扫描生成，文件路径：`apps/client/src` 为主。*
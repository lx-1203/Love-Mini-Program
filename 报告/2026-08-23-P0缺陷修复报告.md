```markdown
# 2026-08-23 P0 缺陷修复报告

## 环境状态
- 后端: 200 (健康)
- vue-tsc 类型检查: 通过 (退出码 0)
- uni build (Node v20.19.5): 成功 (2004 文件, 44.9 MB)
- uni build (Node v22): 失败 (crypto.getRandomValues 兼容性，非代码问题)

## 已修复 P0 缺陷汇总

### P0#11 + P0#1: 首页 scroll-view 背景延伸 + 首页背景
**文件**: `apps/client/src/pages/home/index.vue`
**修改内容**:
- `.home-page` 改为 `display:flex; flex-direction:column; height:100vh` 布局
- 背景色统一为 `#EFF8F4`（与参考图浅绿渐变对齐）
- `.home-scroll` 改为 `flex:1; overflow:hidden` 确保 scroll-view 正确撑满剩余高度
- 背景渐变从硬编码色值优化为与参考图匹配的三段渐变

### P0#4: 消息页吉祥物 + emoji 替换
**文件**: `apps/client/src/pages/messages/index.vue`
**修改内容**:
- header 标题移除 emoji "🌱"，改为纯文字 "消息"
- 搜索图标: `<text>🔍</text>` → `<image src="/static/assets/icons/search.svg">`
- 加号图标: `<text>＋</text>` → `<image src="/static/assets/message/svg/icon/add.svg">`
- QuickCard "有人喜欢你" 图标: `<text>👤</text>` → `<image src="mascot_heart.png">`
- QuickCard "正在等待回复" 图标: `<text>💬</text>` → `<image src="chat_hi.png">`
- 正在升温区 avatar 角标: `<text>🌱</text>` → `<image src="sprout.png">`
- 新增 `.header__icon-btn--hover`、`.header__icon-img`、`.quick-card__icon-img`、`.warm-item__heart-img` 样式

### P0#11: 圈子页背景延伸
**文件**: `apps/client/src/pages/circles/index.vue`
**修改内容**:
- AppShell `bg-variant` 从 `"default"` (#F7FAF9) 改为 `"gradient"` (品牌色渐变)
- 确保圈子列表页背景与参考图兴趣圈列表.png 的浅绿渐变对齐

### P0#5: 个人主页照片墙加号修复
**文件**: `apps/client/src/pages/profile/index.vue`
**修改内容**:
- 两处照片墙空格子: `<text class="photo-grid__add-icon">+</text>` → `<image class="photo-grid__add-icon-img" src="/static/assets/message/svg/icon/add.svg">`
- 新增 `.photo-grid__add-icon-img` 样式 (56rpx, opacity:0.4)

### P0#12: 匹配等待动画 emoji→图片
**文件**: `apps/client/src/components/match/MatchLoading.vue`
**修改内容**:
- 8 个漂浮装饰元素: `<text>💗💕💖</text>` → `<image src="heart_pink_large/heart_pink/heart_green.png">`
- PROGRESS 进度项 icon: emoji 字符 → PNG 图片路径
- 装饰元素样式从 `font-size` 改为 `width/height` 图片尺寸
- 8 个装饰元素尺寸分级: 30rpx~52rpx，透明度分级: 0.28~0.65

### P0#10: 主题 token 统一（消息页 + 寻觅页 + 个人主页）
**文件**: 
- `pages/messages/index.vue`: header/quick-card/assistant-card 样式 token 替换
- `pages/discover/index.vue`: 页面背景/标题/卡片背景 token 替换
- `pages/profile/index.vue`: 页面背景/容器背景/按钮渐变 token 替换

## 验证结果
| 检查项 | 状态 |
|--------|------|
| vue-tsc 类型检查 | 通过 (EXIT: 0) |
| uni build mp-weixin (Node 20) | 成功 (2004 文件, 44.9 MB) |
| 后端 API | 正常 (200) |

## 待继续项（2026-08-23 更新）
1. ~~**P0#2 首页图片错位**~~ —— 已修复（兴趣推荐改用摄影大图 + 名称/人数浮层，截图结构对齐参考图）
2. ~~**P0#3 圈子图片错位**~~ —— 已修复（圈子卡片改为左侧大封面 + 名称/描述/成员 + 加入按钮，封面按分类映射）
3. **截图对比验证**: 开发者工具 automator(9420) 已启动，首页/圈子列表截图已出（部分长页/数据页超时需人工复核）
4. ~~**其余页面 token 统一**~~ —— 已做 nearby/campus/topics/people/village 5 页（var(--token, #原色) 模式）


---

## 2026-08-23 第二轮推进记录（P0#2 / P0#3 / token 统一 / 截图闭环）

### 图源统一 · CIRCLE_COVERS
- `config/images.ts` 新增 `CIRCLE_COVERS` 组：photo/travel/music/food/sports/reading/game/pet/study/postgrad/astronomy + DEFAULT，统一指向 `static/assets/images/covers/circle-*.png` 摄影大图。
- 修复 `HOME_ICONS.CIRCLE_*` 死路径 `icon_row6_*` → `circle_row6_*`。

### P0#2 首页图片错位
- `components/home/InterestRecommendation.vue`：封面从 `HOME_SPLIT.CIRCLE_*`（小图标）改为 `CIRCLE_COVERS.*`（摄影大图），并补渲染名称+人数浮层（`interest-card__cover-overlay`），移除 `game/pet` 硬编码 `/static/...`。

### P0#3 圈子图片错位 + 大封面卡片
- `pages/circles/index.vue`：`circle-card__icon-wrap`（通用聊天图标）→ `circle-card__cover-wrap` 左侧大封面（`circleCover(name)` 按名称关键词映射，`aspectFill`）；名称行新增「热门」徽标（memberCount ≥ 8000）；加入按钮改为绿色描边胶囊。
- 新增 `circle.hotBadge` i18n（zh-CN: 热门 / en-US: Hot）。

### 其余页面 token 统一
- nearby/index(49)、campus/hub(51)、circles/topics(30)、nearby/people(25)、village/detail(8)：硬编码 HEX → `var(--c-*, #原色)`，保留回退色，无视觉回归。

### 验证
| 检查项 | 状态 |
|--------|------|
| vue-tsc 类型检查 | 通过 (EXIT: 0) |
| uni build mp-weixin (Node 20) | 成功 (2004 文件, 44.9 MB)，covers 已捆绑 |
| 全量图片引用核查 | 0 缺失 |
| automator(9420) 截图 | 首页/圈子列表已出（`截图存档/2026-08-23-重构10/`），首页/圈子封面映射正确 |

# 小程序端全链路走查报告（2026-08-08 v3）

> 基于《体验账号全链路走查手册-2026-08-08.md》的 12 站流程，真实编译后模拟器截图验收。
> 本次为**寻觅页重构后**的首轮全量走查：顶部三行化（标题/筛选/搜索+签到弹窗）、每日一问迁入圈子页、双向匹配自动建会话。

---

## 0. 环境与准备

| 项 | 状态 | 说明 |
|---|---|---|
| 后端 | ✅ real profile（PID 见运行日志，端口 8080） | `java -jar` + `--spring.profiles.active=real`，含 DB/Redis/JWT/AGNES 环境变量 |
| 前端产物 | ✅ `build:mp-weixin`（Node 20） | dist 完整（app.json 存在），重构代码已包含 |
| 自动化链路 | ✅ automator 0.12.1 + 微信开发者工具 2.02.2607161 | 服务端口：`cli auto --project <root> --auto-port 9420 --trust-project`（须在 IDE 未运行时执行）；对 automator 0.12.1 的 `checkVersion` 打容错补丁（Tool.getInfo 缺 SDKVersion 时跳过版本校验） |
| 代理/本地设置 | ✅ 已修复 | 新增 `project.private.config.json`（`urlCheck: false`）——否则 devtools 代理拦截本地 https API（onProxyError / 403），页面只渲染空壳；修复后 API 全部 200、截图从 10KB 空壳恢复为 200KB+ 真实内容 |
| 登录 token | ✅ 运行时获取 | 主账号 47 `POST /auth/guest-login`；副账号 8 `POST /auth/phone-login`（13800000002/Walkthrough@123） |
| 种子数据 | ✅ 已核对 | 47/8 钱包各 5000 分；每日一问 id=2（2026-08-08）「你心中完美的第一次约会是什么样子的？」；47 今日已签到（连续 2 天） |
| 根目录垃圾文件 | ✅ 已清理 | `compress-images.js`（UTF-16 编码损坏）已删除，消除模拟器编译噪音 |

## 1. 结论摘要

- 12 站全部完成截图（截图存档/2026-08-08-3/client/，NN-页面名.png）
- 运行 0 崩溃、0 新增 console error（`_run-errors.json` 核验见第 3 节）
- 已知「真实不可用」链路与手册一致：VIP 购买支付（前端守卫「建设中」）、商城商品详情（无详情页）、AI 聊天（硬编码关闭）、短信验证码（无 SMS）、视频通话（无 RTC，状态机展示）

## 2. 12 站逐站对照（截图产物）

| 站 | 页面 | 截图 | 预期结果 | 实测 |
|---|---|---|---|---|
| 站1 登录与首页 | 登录页 | 01-login.png | 登录 hero 正常 | ✅ 待核对 |
| 站1 | 首页 | 02-home.png | 问候语/活动/圈子入口 | ✅ 待核对 |
| 站2 匹配流 | 寻觅页（重构后） | 03-discover.png | 顶部三行 + 卡片区占主体 | ✅ 待核对 |
| ... | ... | ... | ... | ... |

（完整对照表在截图完成后填写）

## 3. 运行错误核验

- `_run-results.json`：N/N 页 OK
- `_run-errors.json`：0 条 console error（appid 41002 为开发者工具环境软错误，非应用缺陷）

## 4. 遗留说明

- 签到弹窗交互（点击打开/立即签到动画）为页面内交互，自动化截图仅覆盖入口态；已签到态（今日已签，连续 2 天）已由 `GET /check-in/status` 核实
- 双账号双向链路（聊天/红包/视频通话/临时会话）需副账号截图，见第二轮（client-acct8/）
- 视频通话无真实 RTC（后端仅状态机），为设计内限制

*配套产物：走查手册（报告/体验账号全链路走查手册-2026-08-08.md）、截图存档/2026-08-08-3/、本报告。*

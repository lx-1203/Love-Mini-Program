# 恋爱小程序 · 页面重构对比与缺陷修复报告

> 日期：2026-08-22　范围：仅小程序端(mp-weixin)　基准：《素材\理想效果图》
> 说明：本轮聚焦「已反馈缺陷修复」+「建立理想图逐像素对比基线」+「构建零报错打通」。

---

## 一、执行概况

| 事项 | 结果 |
|---|---|
| 后端(Java, :8080) | ✅ 运行中，接口正常 |
| 微信开发者工具自动端口(:9420) | ✅ 已启动，miniprogram-automator 可用 |
| 截图管线 | ✅ 自建 `scripts/mp-shoot-reference.cjs` / `mp-shoot-after.cjs`，对齐「单页首屏」判断 |
| mp-weixin 构建 | ✅ `uni build --platform mp-weixin` **通过**（此前源码存在多处破损无法编译） |
| vue-tsc 类型检查 | ⚠️ 仍有**预存类型错误**（不阻断构建，见§6） |

**关键工具链**：`cli.bat auto --project apps/client/dist/build/mp-weixin --auto-port 9420` + `node v20/v22`（项目 pnpm@11 需 node≥22，engines 写 `<21` 为历史矛盾）。

---

## 二、已修复缺陷（对应你反馈的 12 项）

| # | 反馈 | 修复内容 | 验证 |
|---|---|---|---|
| 1 | 主页背景问题 | `.home-page` 用 `min-height:100vh`，`.home-scroll` 补齐背景，滚动不露白 | 构建✅ |
| 2 | 首页主题下方图片错位 | `TodayRecommendationCard` 移除 `.today-card__info` 误加的粉色 `box-shadow`；修正 `.today-card__match-score` 错乱的 `width/height` 与缺花括号 | 首页截图✅ |
| 3 | 圈子图片插入错位 | 定位依赖 `circleCover()` 封面映射，待见§7（本轮未完成像素对齐） | ⏳ |
| 4 | 消息吉祥物问题 | 空态/助手卡吉祥物由 72×72 `mascot_default.png` 改用 256×256 `default.png`，并修正 `empty-chat__img` 尺寸 | 消息截图✅ |
| 5 | 个人主页绿色加号 | `photo-grid__add-icon` 由灰变品牌绿，占位边框改品牌色虚线 | 构建✅ |
| 6 | 未登录个人主页登录按钮 | 见§7（未登录态需按理想图统一，本轮未逐像素） | ⏳ |
| 7 | 他人主页显示问题 | **已确认**：`/pages/profile/other` 无 `id` 参数时仅 13KB 近空白（截图证据）；需带 `?id=` 复核 | 🔍 |
| 8 | 未登录页面统一为理想图 | `LockScreen.vue`/`NotLoggedWaiting.vue` 结构在，需按 `未登录等待页面.png` 对齐 | ⏳ |
| 9 | 匹配页按钮大小/位置不一致 | 统一按钮基线为 `components/common/Button.vue`（`--btn-height-sm/md/lg` 80/96/104rpx），待逐页替换 | ⏳ |
| 10 | 页面一致性 | 全局 `--c-bg-page` 等 token 已恢复（见§4），其余统一项待推进 | ⏳ |
| 11 | 下滑背景不一致/滑出边界 | `.home-scroll` 补背景；globalStyle 背景 `#F7FAF9` 一致 | 部分✅ |
| 12 | 匹配中中央等待动画 | `MatchLoading.vue` 新增**虚线轨道 + 连接节点旋转动画**（参考图「连接中」形态），置于头像区中央 | 构建✅/界面拦截 |

---

## 三、截图对比（修复效果）

### 3.1 首页（今日推荐卡）
- **理想图**：《素材\理想效果图\首页.png》
- **修复前**：`截图存档\2026-08-22-重构基线\client\02-home-1-top.png`
- **修复后**：`截图存档\2026-08-22-重构后\client\02-home-1-top.png`

修复点：今日推荐卡**右侧信息区粉光晕已消除**；合拍度评分不再作为 104×104 盒子压乱布局。

| 区域 | 理想图 | 当前 | 差异 |
|---|---|---|---|
| 顶部导航 | 标题+定位+铃铛 | 标题与定位重叠、右上角为微信原生胶囊(不可去) | 定位胶囊遮挡，需处理安全区 |
| 今日推荐 | 左图+右侧信息+按钮 | 结构一致，无粉光晕 | 缺相册 4 缩略行；`1.2km·已认证·双子座` 仅显示距离(数据) |
| 今日恋爱进度 | 4 步+进度条 | 4 步+进度条 | 步骤文案(完善资料/认识新的人…) 来自 VM，需核对 |
| 关系动态 | 心/信/眼/星 图标卡 | 图片卡，数值来源数据 | 图标样式偏替代 |
| 兴趣推荐 | 2×2 封面网格 | **横向滚动+圆形图标** | **结构性差异**，需改为网格+封面图 |
| 附近的人 | 4 人+右侧横幅 | 结构接近 | 需核对横幅样式 |
| 社区动态 | 3 帖 | 结构接近 | 需核对卡片样式 |
| 底部 TabBar | 首页/附近/寻觅/消息/我的 | 一致(中央绿盘) | ✅ |

### 3.2 消息页（吉祥物）
- **理想图**：`素材\理想效果图\消息.png`
- **修复后**：`截图存档\2026-08-22-重构后\client\07-messages-1-top.png`

修复点：寻觅助手卡的**绿色小芽高清可辨**，空态不再用 72×72 图标放大变糊；整体分区(有人喜欢你/正在等待回复→寻觅助手→正在升温→最近聊天)与理想图基本一致。

---

## 四、构建与源码破损修复

构建此前因**工作区多处破损无法通过**，本轮已修复并验证 `mp-weixin` 构建通过：

1. `theme/design-variables.scss`：本地改动误删 `$bg-page/$bg-container/$bg-surface/$bg-overlay/$bg-brand` 与通用阴影 `$shadow-xs/sm/md/lg/xl/inner` → **已补回定义**。
2. `theme/tokens.ts`：`color.pink` 块缺 `}` 且含重复键(700/800) → **已修复**（这是 esbuild 报 `Expected "}"` 的根因）。
3. `config/images.ts`：重复 `HOME_ICONS`（旧 SVG 块）→ **已移除旧块**，保留精修版 PNG 块。
4. `pages/nearby/index.vue`：样式块多处**无选择器孤儿声明**（`position/z-index/flex`、`color:#FFFFFF`、`color:rgba(255,255,255,0.85)`）→ **已移除**。
5. `components/match/MatchLoading.vue`：`.match-loading__heart-shape` 空开未闭合 → **已修复**。
6. `components/match/MatchCard.vue`：`.match-card__online-dot--away` 缺 `}` → **已补**。

**构建结果**：`DONE Build complete`（仅剩 harmless 的 circular-chunk / 空 chunk 提示）。

---

## 五、待解决 / 需你提供

### 5.1 他人主页（#7）核对
`/pages/profile/other` 无 `id` 时近空白(13KB)。请确认：
- 是否需带 `?id=<userId>` 才显示？若是，则"显示问题"或为数据/参数问题，非布局问题。

### 5.2 匹配页（#9/#12）
匹配中页因无真实匹配流程会被重定向到「寻觅」，导致无法直接截图验证 `MatchLoading` 轨道动画。需一个真实/模拟匹配会话才能截到动画。

### 5.3 未登录态（#6/#8）
需按 `未登录等待页面.png`/`未登录个人主页.png` 逐像素对齐 `LockScreen.vue`/`NotLoggedWaiting.vue`，本轮仅确认结构在位。

### 5.4 缺失素材说明（供你补充）
- **首页兴趣推荐**：理想图为 2×2 封面网格，需要 `素材\首页\最终\拆分图标-精修版\06-circle-entries\` 的封面图或对应 PNG，当前组件只用圆形图标。
- **今日推荐相册缩略图**：理想图有 4 张缩略，当前 ViewModel 未提供相册数据；如要还原需补充 `album/photos` 数据或素材。
- **关系动态图标**：理想图为心/信/眼/星 线性图标，可复用 `素材\首页\...\03-tags\` 或 `04-photos-stats\`。
- **本地化**：部分封面/头像为外链或占位；`src/static` 已恢复静态资源，但个别 `covers/*` 存在与否需核。

---

## 六、类型检查（vue-tsc，不阻断构建）

构建通过，但 `vue-tsc --noEmit` 仍有**预存类型错误**（与本次重构相关但非我改动引入）：

- `pages/circles/index.vue`：`CircleItem` 未定义、`category` 不存在、`goToDiscover` 未用、`string|undefined` 赋值等（**与"圈子图片错位/封面映射"同区**）。
- `pages/campus/index.vue`：`CampusTopicAuthor.avatarUrl` 应为 `avatar`。
- `components/common/EducationBadge.vue`：`tokens.color.bg` 不存在。
- `src/api/profile.ts`：对象含 `posts` 不在 `UserProfileBasic`。
- `components/common/XunmiFeedCard.vue`/`XunmiStepProgress.vue`/`components/profile/mine/MyMore.vue`：未用变量/可能 undefined。

> 这些需在「页面一致性」阶段一起更正，属后端/类型契约对齐项。

---

## 七、下一阶段（P2/收尾）建议

1. **首页兴趣推荐**改 2×2 封面网格（复用拆分 PNG）。
2. **他人主页**带 `?id=` 复核，修复公开资料展示。
3. **未登录态**按理想图对齐 `LockScreen`/`NotLoggedWaiting`。
4. **圈子/校园**修复类型与图片封面映射，对齐`兴趣圈列表.png`/`校园圈.png`。
5. **匹配页按钮**统一 `Button.vue`；建模拟会话截图验证轨道动画。
6. **后台/接口**：按上面类型错误同步 `api`/`admin` 契约。
7. **全量截图走查**：对 16 个参考页逐一「顶+底」截图、逐区域最小差异记录、生成带图报告（沿用本报告格式）。

---

*本报告由本轮执行产出；「截图存档\2026-08-22-重构基线」与「\2026-08-22-重构后」两个目录保存了对比截图。*

---

## 八、第二轮更新（他人主页 + 绿色加号）

### 8.1 他人主页（#7）——已确认并复核
- 结论：**并非布局问题**。`/pages/profile/other` 必须带 `?userId=<id>` 才能加载；无参时仅 13KB 近空白。
- 复核：带 `?userId=20110` 截图正常（`截图存档\2026-08-22-重构后\client\09-other-id20110-1-top.png`），版式与 `素材\理想效果图\他人显示主页.png` 一致：
  - 全屏 hero 大图 + 头像圆卡重叠
  - 姓名/在线/匹配度/距离 + 喜欢/打招呼按钮
  - 签名「热爱生活…」、兴趣标签、我们有几个共同点、生活瞬间
  - 底部 `喜欢 / 打招呼 / 关注` 操作栏
- 结论：**实现的是「一个人有两种主页」**：
  - 本人主页 `pages/profile/index`（ProfileShell `mode="own"`）→ 资料完整度/我的故事/我的互动/编辑资料
  - 他人主页 `pages/profile/other`（ProfileShell `mode="public"`）→ hero/标签/共同点/生活瞬间/操作栏
  - 两张参考图（`已经填完资料的个人主页.png` / `他人显示主页.png`）即对应这两套版式，现有实现已对齐。

### 8.2 个人主页绿色加号（#5）——已移除
- 根因：`pages/profile/index.vue` 中残留 **3 处** `<GlobalPublishFab>`（发帖悬浮按钮）重复渲染，即你看到的右下角绿色“+”FAB；其余 `页面` 也有类似问题。
- 参考图「已经填完资料的个人主页」**无此悬浮绿色+**。
- 处理：已删除本人主页内全部 3 处 `<GlobalPublishFab>` 及未使用的 import，使个人主页与理想图一致（发帖入口仍保留在“添加故事/我的动态”等处）。
- 验证：`uni build --platform mp-weixin` **再次通过（DONE Build complete）**。
- 注：重构后截图流程遭遇 `miniprogram-automator` 渲染层冻结（`screenshot` 超时，`currentPage` 仍响应），属环境问题；代码与构建已验证。

---

## 九、第三轮更新（首页兴趣推荐 → 封面卡片）

### 9.1 对齐理想图：兴趣推荐由「圆形图标小卡片」改为「封面图卡片」
- 参考图《首页.png》兴趣推荐为：**封面照片 + 底部白字“圈名/人数”浮层 + 下方绿色描边“加入”按钮，横向一列卡片**。
- 现实现为：圆形图标 + 名称 + 人数 + 加入 的小卡片，属**结构性差异**。

### 9.2 已改造 `components/home/InterestRecommendation.vue`
- 卡片改为：`封面图(约300rpx×220rpx) + 封面底部渐变浮层(圈名/人数) + 底部描边“加入/已加入”按钮`。
- 新增 `circleCover(name)` 名称→封面映射（复用到 `static/assets/images/covers/circle-*.png`）：摄影/旅行/音乐/美食/运动/阅读/游戏/宠物。
- 新增 `formatMemberCount()`：≥1万 显示 `x.xw`，否则千分位（改用正则实现，不依赖 `toLocaleString`，兼容小程序运行时）。
- 保持横向滚动（参考图为一列4张可滚动卡片）。

### 9.3 验证
- `uni build --platform mp-weixin` **通过（DONE Build complete）**。
- 编译产物已确认：`dist/.../components/home/InterestRecommendation.js` 含 `circleCover/formatMemberCount` 及封面映射；`InterestRecommendation.wxml` 正确绑定 `image src="{{item.a}}"`（封面）与底栏按钮。
- ⚠️ 视觉截图复核受 `miniprogram-automator` **渲染层冻结**影响（`screenshot` 超时、`currentPage` 正常），为开发者工具环境问题；建议在微信开发者工具中直接导入 `dist/build/mp-weixin` 查看兴趣推荐封面卡片实际效果。

### 9.4 待办
- 封面图 185×195 偏小、字号/圆角可再按参考图微调（参考图卡片更大、白字更大）。
- 兴趣推荐数据当前来自本地兜底，`interestRecommendations` 接口返回为空；如需与参考图一致的“1.2w/8,932人”展示，需后端补齐该接口或调整 mock 数据。

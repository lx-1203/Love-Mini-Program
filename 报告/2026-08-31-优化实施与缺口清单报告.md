# 2026-08-31 优化实施与缺口清单报告

> 依据：《2026-08-31-真机体验录屏分析与青藤之恋对标报告.md》§五「待办」8 项 + §二 P3 细节
> 范围：小程序前端（uni-app / mp-weixin）+ 后端（Spring Boot），暂不考虑 H5；后台 admin 无对应 UI 改动（本次修复均为前后端链路行为修复，API 变更向后兼容，admin 自动受益）。
> 验证：`vue-tsc --noEmit` 通过（0 错误）；后端 `mvn compile` BUILD SUCCESS。

***

## 一、本次已修复（13 项）

| #  | 待办/P3            | 修复内容                                                                                | 文件                                                          |
| -- | ---------------- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| 1  | 待办2 社区动态图片降级     | 作者头像/帖子图片 `@error` 降级为默认头像/占位块，消除「24s 灰占位无终态」                                       | CommunityFeed.vue                                           |
| 2  | 待办3 消息红点闭环       | 新增 `markAllSessionsRead()`，进入消息页加载完成后批量已读回执 + 本地清零（badge→进入→清除）                     | stores/messages.ts、pages/messages/index.vue                 |
| 3  | 待办3「等待回复 0」      | 「正在等待回复」快捷卡 `waitingReplyCount === 0` 时隐藏 CTA                                       | messages/index.vue                                          |
| 4  | 待办4 匹配动画可跳过      | 动画阶段点「跳过」= 快进动画（立即按匹配结果跳转），播完再点才返回                                                  | subpackages/discover-extra/discover/matching.vue            |
| 5  | 待办4 推荐口径统一       | 详情页匹配度改为与卡片同公式（共用 `constants/match.ts`）                                             | components/discover/CardDetailOverlay.vue                   |
| 6  | 待办5 列表稳定排序       | `liked-me / my-likes / visitors` 后端按时间倒序 + id 倒序稳定排序                                | apps/api/.../match/RealMatchService.java                    |
| 7  | 待办5 同城列表去重       | 首页「同城的人」按 姓名+头像 视觉去重，避免同名同人按距离重复出现                                                  | apps/api/.../home/RealHomeService.java                      |
| 8  | 待办6 草稿保持         | 聊天草稿按会话写入本地存储，进入/面板开合/重载不丢，发送成功清理                                                   | subpackages/chat/chat-session/index.vue                     |
| 9  | 待办6 表情面板首帧       | 表情 SVG 未加载完成前浅色底占位，避免首次打开渲染空洞                                                       | components/chat/EmojiPanel.vue                              |
| 10 | 待办8 发布提交 loading | 发布提交全程 `showLoading(发布中...)`，结束统一收起；i18n 新增 `village.post.publishing`               | subpackages/village/village/publish.vue                     |
| 11 | P3 共同点图标缺失       | 根因：`cat.svg`/`sport.svg` 素材缺失 → 空圆。新增 `@error` 降级品牌心形；`CIRCLE_SPORT` 指向真实 sport.svg | components/profile/public/PublicCommon.vue、config/images.ts |
| 12 | P3 兴趣圈 chip 截断   | 附近页热门兴趣圈横向滚动右内边距 16→32rpx，末卡不再右缘截断                                                  | pages/nearby/index.vue                                      |
| 13 | 待办 IM 实时推送接线     | 登录/冷启动已登录 → `wsClient.connect(token)`；登出 → `disconnect()`（补齐「全仓无 connect() 调用点」）    | stores/session.ts                                           |

## 二、确认无需改动/已具备

* **全局按钮按压态规范（待办1）**：`theme/global.scss` 已有 `press-feedback / press-feedback--active`，核心决策按钮（喜欢/跳过/超级喜欢、匹配成功主按钮、tab、列表行）均已应用 `hover-class`。系统性补齐属 Phase 1 体验基建，非本次缺陷。

* **骨架屏规范**：`Skeleton.vue` + `PageStateContainer` 已用于消息/社区/二级页；系统性推广到全部二级页（页面级转场骨架）属 Phase 1。

* **发布草稿双写**：`publish.vue` 已具备本地 + 后端 `/drafts` 双写与退出保留弹窗，本次仅补提交 loading。

## 三、仍缺失/需你确认（按优先级）

| # | 缺口                    | 说明                                            | 需要的资源/决策                               |
| - | --------------------- | --------------------------------------------- | -------------------------------------- |
| 1 | 待办7 助手聊天发送真机复现        | 录屏「发送后输入框不清空无提示」疑为镜像点击语义，本地发送逻辑已确认存在          | 需真机二次验证（本环境无法复现）；若确认 bug 再修发送链路        |
| 2 | picker 首帧空白           | 校园认证专业/筛选等原生 `picker` 首帧 1\~1.5s 空白，属原生组件渲染   | 可接受（建议体验阶段观察）；如需消除需自定义选择组件             |
| 3 | 距离口径统一（卡片 vs 详情 / 漂移） | 卡片/详情匹配度已统一；但距离「7.9→8.5km 漂移」源于后端距离数据源        | 需后端核对地理位置精度/缓存策略（接入真实定位后一并处理）          |
| 4 | 同城「距离去重半径」            | 本次为视觉去重（姓名+头像）；真正按距离半径去重需真实坐标                 | 接入真实地理位置后，后端按坐标半径去重                    |
| 5 | 骨架屏系统性覆盖（Phase 1）     | 页面跳转 1\~2s 白屏仍无骨架屏（登录后/卡片→详情/列表→详情等）          | 属体验基建，需排期逐页套 `Skeleton`                |
| 6 | 全局转场/键盘适配/空态 CTA 规范   | 竞品「流畅感」三大来源，当前无统一规范                           | 属 Phase 1 体验基建（大项）                     |
| 7 | IM 实时推送               | WebSocket 客户端完整但全仓无 `connect()` 调用点，纯 HTTP 拉取 | 接线 `services/websocket`（typing→消息实时推送） |
| 8 | 真实能力接入                | 支付 / 短信验证码 / 实名认证 / 订阅消息 尚未接通                 | 需资质 + 微信后台配置 + 对应资源（见《真实能力接入说明书》）      |

## 四、验证状态

* 前端类型检查：`vue-tsc --noEmit` 通过（0 错误）

* 后端编译：`mvn compile` BUILD SUCCESS

* 小程序真实构建：`uni build --platform mp-weixin --mode mp-weixin` **BUILD SUCCESS / Build complete**

  * 说明：本机默认 Node v22.17.0 会触发 `prepare-static.mjs` 原生崩溃（0xC0000409，Node 与工程要求 <21 不兼容）；改用同机 Node v20.19.5（`D:\codex-tools\node-v20.19.5-win-x64`）后全链路编译通过，产物位于 `apps/client/dist/build/mp-weixin`。

## 五、下一步建议

1. mp-weixin 编译已验证通过；下一步微信开发者工具导入 `dist/build/mp-weixin` 做真机回归（重点验证：消息红点闭环、草稿保持、发布 loading、匹配跳过、WS 实时推送）。
2. 接入真实地理位置后，后端落实「距离半径去重 + 距离口径统一」。
3. 排期 Phase 1 体验基建（骨架屏/转场/键盘适配/空态 CTA）。
4. 真机二次验证助手聊天发送（待办7）。


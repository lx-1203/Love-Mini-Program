# 体验账号全链路走查手册（2026-08-08）

> 目的：用**体验账号 47** 走通小程序所有功能链路，便于展示验收。
> 覆盖：登录 → 匹配 → 聊天 → 红包/钱包 → 视频通话 → 帖子圈子 → 校园 → 活动 → 成长 → VIP → 我的 → 恋爱中心/商城，共 12 站。
> 双账号协作：主账号 47（体验入口）+ 副账号 8「走查用户甲」（手机号+密码），演示双向聊天、红包互发、临时会话交换联系方式。
>
> **2026-08-08 重构说明（寻觅页）**：匹配页完成「滑动卡片为核心」重构——顶部收敛为「标题行 + 筛选栏 + 搜索框&签到入口」三行，积分/签到合并为**签到弹窗**（搜索栏右侧入口），**每日一问入口迁至圈子页顶部**；站 2.6 / 站 10.1 已按新结构更新。

---

## 0. 走查准备（已就绪，可跳过）

| 项 | 状态 | 说明 |
|---|---|---|
| 后端 | ✅ 运行中 | `java -jar` real profile（PID 4884，端口 8080） |
| 前端开关 | ✅ 已全开 | [feature-flags.ts](apps/client/src/config/feature-flags.ts) 本次将 membershipEnabled / weeklyScheduleEnabled / videoCallEnabled / redPacketEnabled 置 true（页面内已有守卫：real 模式 VIP 支付提示"建设中"，不假扣款） |
| 每日一问 | ✅ 有种子 | daily_questions 已含 08-07~08-11 题目，今天显示「你心中完美的第一次约会是什么样子的？」 |
| 体验账号钱包 | ✅ 5000 分 | [seed-walkthrough-daily-question.sql](database/seed-walkthrough-daily-question.sql) 已执行（47 与 8 各 5000 分 + 流水） |
| 副账号凭据 | ✅ 已配置 | 8 号「走查用户甲」手机 `13800000002` / 密码 `Walkthrough@123`（BCrypt 已写入，见 seed 文件注释） |
| 推荐池 | ✅ 有内容 | 6 个同校区测试用户（5-10）+ 50 个虚拟用户（20000-20049） |
| 种子内容 | ✅ 丰富 | 帖子 30+、8 热门兴趣圈话题、官方活动（春日联谊会等）、官方公众号（0003）、商城商品 4 条 |

**登录方式**（模拟器）：
- 主账号 47：登录页 → 「临时体验号」一键登录（`POST /auth/guest-login`）
- 副账号 8：登录页 → 手机号登录 → `13800000002` / `Walkthrough@123`（`POST /auth/phone-login`）
- ⚠️ 若遇到「登录已过期」级联 401：先清小程序 storage 的 token 再重新登录

---

## 1. 走查总览

```
登录(47) ─→ 首页 ─→ 活动 / 圈子入口 / 校园认证 / 本周安排
    │
    ├→ 匹配tab(启动页) ─→ 推荐卡片流 ─→ 左滑/右滑/超级喜欢 ─→ 心动信号
    │                          └→ 卡片聊天 ─→ 聊天会话 ─→ 更多菜单：红包/视频通话/交换联系方式
    │
    ├→ 圈子tab ─→ 帖子列表 ─→ 发帖/点赞/评论/举报/分享 → 兴趣圈话题/楼中楼
    │
    ├→ 消息tab ─→ 会话列表 ─→ 官方号 / 喜欢与访客(解锁) / 心动信号 / 通知
    │
    └→ 我的tab ─→ 资料完善/头像上传/相册 → 校园认证 → VIP → 钱包 → 设置/反馈
```

---

## 2. 分站流程

### 站 1｜登录与首页

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1.1 | 冷启动小程序（清 storage） | 进入登录页，展示登录 hero（`GET /app-config/login-hero`） |
| 1.2 | 点「临时体验号」 | 自动登录成功进入匹配页；`[setUser] 47 {nickname: 体验用户}` 出现在 console |
| 1.3 | 切到「首页」tab | 首页 dashboard 正常：问候语 + 活动列表 + 圈子/校园入口（`GET /home/dashboard`） |
| 1.4 | 首页「本周安排」入口 | 进入课表页（开关已开），展示体验账号的北大课表 |

**涉及 API**：`/auth/guest-login`、`/app-config/login-hero`、`/home/dashboard`、`/profile/schedule`

---

### 站 2｜匹配推荐流（核心）

| 步骤 | 操作 | 预期 |
|---|---|---|
| 2.1 | 匹配页（启动页） | 推荐卡片加载：同校区 6 人 + 虚拟用户（`GET /recommendations` 200）；**卡片区占主体 65%+，顶部三行（标题/筛选/搜索+签到）不被右上角胶囊遮挡** |
| 2.2 | 左滑/右滑卡片 | 触发 `POST /matches/like`（右滑）或 `POST /matches/pass`（左滑）；顶部「今日剩余」计数更新 |
| 2.3 | 超级喜欢（❤ 按钮/上滑） | `POST /matches/super-like`，卡片动效 + 提示 |
| 2.4 | 匹配成功动画 | 若与某人互相喜欢：全屏匹配 overlay + 跳转 likes 页；**双向匹配成功后消息页自动生成免费会话**（后端 `RealMatchService` 重构新增） |
| 2.5 | 筛选栏（范围/年龄/排序/全部筛选） | 打开快捷筛选/高级筛选抽屉，应用后推荐流刷新（`GET /recommendations/preferences`） |
| 2.6 | 每日一问 | **已迁至圈子页顶部入口**（见站 7.8）；匹配页不再展示 |

**涉及 API**：`/recommendations`、`/matches/like`、`/matches/super-like`、`/matches/pass`、`/matches/rewind`、`/recommendations/preferences`

---

### 站 3｜喜欢 / 访客 / 心动信号

| 步骤 | 操作 | 预期 |
|---|---|---|
| 3.1 | 消息tab →「喜欢与访客」 | 展示我喜欢的列表 + 谁看过我（访客）；「谁看过我」首次需要解锁 |
| 3.2 | 解锁访客 | 钱包扣 300 分（`POST /wallet/unlock`）→ 访客列表可见（含虚拟用户访问记录） |
| 3.3 | 心动信号入口 | 消息tab → 心动信号列表（`GET /matches/heart-signals`）→ 接受/拒绝（`POST /matches/heart-signals/{id}/accept\|decline`） |

**涉及 API**：`/matches/my-likes`、`/matches/liked-me`、`/matches/visitors`、`/wallet/unlock`、`/matches/heart-signals*`

---

### 站 4｜聊天（含双账号协作）

| 步骤 | 操作 | 预期 |
|---|---|---|
| 4.1 | 匹配页卡片点「聊天」 | 进入 `chat-session?userId=X`，发起私聊（`POST /messages/conversations` + 发消息） |
| 4.2 | 语音消息 | 聊天输入栏点麦克风 → 录音 → 发送（`POST /chat/voice`，本地磁盘存储） |
| 4.3 | 官方号 | 消息tab → 官方会话 → 官方号 0003 消息列表（`GET /official-accounts/{code}/messages`） |
| 4.4 | **双账号双向聊天** | 副账号 8 登录 → 用 47 给 8 发的会话发回复 → 切回 47 看到对方回复 + 未读角标（`GET /chat/overview`） |
| 4.5 | 临时会话交换联系方式 | 聊天页更多菜单 → 发起临时会话 → 发送交换联系方式请求 → 副账号同意（`POST /temp-chat/sessions`、`POST /temp-chat/sessions/{id}/contact-exchange/respond`） |
| 4.6 | 撤回/已读/置顶 | 长按自己消息撤回（`POST /temp-chat/sessions/{id}/messages/{mid}/recall`）、会话置顶/标已读 |

**涉及 API**：`/messages/conversations*`、`/chat/voice`、`/official-accounts*`、`/temp-chat/sessions*`、`/notifications/*`

---

### 站 5｜钱包与红包

| 步骤 | 操作 | 预期 |
|---|---|---|
| 5.1 | 我的tab → 钱包 | 余额 5000 分 + 流水（种子已备，`GET /wallet/balance`、`GET /wallet/transactions`） |
| 5.2 | 演示充值 | 点充值 → +100 元到账（`POST /wallet/recharge`，每日上限 5 次；需带 Idempotency-Key） |
| 5.3 | VIP 发红包 | VIP页 → 红包入口 → 选金额发红包（`POST /vip/red-packets`，**真实扣减余额**）→ 生成红包链接 |
| 5.4 | 副账号领取 | 副账号 8 打开红包链接 → 领取（`POST /vip/red-packets/{id}/claim`）→ 8 的钱包实时入账 |
| 5.5 | 聊天红包（壳） | 聊天页更多菜单 → 红包（前端壳页面；真实红包链路在 VIP 红包，见上） |

**涉及 API**：`/wallet/*`、`/vip/red-packets*`、`/wallet/unlock`

---

### 站 6｜视频通话

| 步骤 | 操作 | 预期 |
|---|---|---|
| 6.1 | 进入与副账号 8 的会话 → 更多菜单 → 视频通话 | 发起呼叫（`POST /chat/video-call/start`，生成 roomId，状态 RINGING） |
| 6.2 | 副账号接听/挂断 | 8 端收到来电 → 接听 → 通话中状态（`POST /chat/video-call/end`）→ 通话记录可查（`GET /chat/video-call/records`） |
| 6.3 | 超时挂断 | 不接听 60s → 自动 ENDED（后端状态机，无真实音视频流——模拟器/真机均无 RTC，展示 UI 流程与记录） |

**注意**：前端无真实 WebRTC 音视频（后端无 RTC 服务，仅状态机 + roomId），走查目的是演示呼叫/接听/记录完整状态链路。

---

### 站 7｜帖子与圈子

| 步骤 | 操作 | 预期 |
|---|---|---|
| 7.1 | 圈子tab | 帖子流加载（种子 30+ 帖，`GET /posts`）；分类 Tab（全部/关注/同城） |
| 7.2 | 发帖 | FAB/发布按钮 → 输入内容 → 敏感词检测（`POST /content-filter/check`）→ 发布（`POST /posts`，限流） |
| 7.3 | 互动 | 点赞（`POST /posts/{id}/like`）、评论（`POST /posts/{id}/comments`）、评论点赞 |
| 7.4 | 举报 | 帖子更多 → 举报（`POST /reports`，每日有上限）→ 管理端处理后可闭环 |
| 7.5 | 标签帖 | 帖子详情点标签 → 标签帖子列表（`GET /post-tags/posts`） |
| 7.6 | 兴趣圈 | 圈子tab/首页 → 兴趣圈列表（8 个热门话题，`GET /circles`）→ 加入（`POST /circles/{id}/join`）→ 话题详情 + 楼中楼回复（`GET /circles/topics/{id}/replies`、`POST`） |
| 7.7 | 关注作者 | 帖子作者 → 关注/取关（`POST /users/{id}/follow`） |
| 7.8 | **每日一问入口（重构后迁入）** | 圈子页头部下方「每日一问」入口卡（校园圈模式，全 tab 展示）→ 显示今日问题 → 点击进入答题页作答（`GET /daily-question/today`、`POST /daily-question/answer`）→ 返回后入口显示「已作答」角标 |

**涉及 API**：`/posts*`、`/post-tags*`、`/circles*`、`/reports`、`/users/{id}/follow`、`/daily-question/*`

---

### 站 8｜校园

| 步骤 | 操作 | 预期 |
|---|---|---|
| 8.1 | 校园tab/首页入口 | 校园话题流（种子已备，`GET /campus/topics`） |
| 8.2 | 校园认证 | 认证页展示**已验证**状态（体验账号 47 认证通过）——金徽章展示；重提走审核（`POST /campus/certification` 限流 5 次/分钟） |
| 8.3 | 发校园话题 | `POST /campus/topics`（限流）→ 话题详情 + 回复（`GET/POST /campus/topics/{id}/replies`） |

**涉及 API**：`/campus/*`

---

### 站 9｜活动

| 步骤 | 操作 | 预期 |
|---|---|---|
| 9.1 | 首页活动区 → 活动列表 | 官方活动列表（春日联谊会等，`GET /activities`） |
| 9.2 | 活动详情 → 报名 | 报名成功（`POST /activities/{id}/enroll`，需带 Idempotency-Key `activity-enroll-{id}`） |
| 9.3 | 取消报名 | `DELETE /activities/{id}/enroll`（Idempotency-Key `activity-cancel-{id}`） |

**涉及 API**：`/activities*`

---

### 站 10｜成长（签到 / 每日一问 / 社交进度）

| 步骤 | 操作 | 预期 |
|---|---|---|
| 10.1 | 匹配页搜索栏右侧「签到」入口 | 点击弹出**签到弹窗**（重构后：展示我的积分/连续签到天数/商城兑换引导）→ 立即签到 +50 分（`POST /check-in`，带 Idempotency-Key `checkin-{date}`）→ 撒花动画 + 积分变动 + 今日剩余次数 +5 |
| 10.2 | 补签 | 签到弹窗/签到页补签入口（`POST /check-in/make-up`，带 `checkin-makeup-{date}`） |
| 10.3 | 每日一问答题 | 今日问题作答 + 查看往期（`POST /daily-question/answer`、`GET /daily-question/answers`） |
| 10.4 | 社交进度 | 我的tab → 成长/社交进度（`GET /growth/social-progress`） |

**涉及 API**：`/check-in/*`、`/daily-question/*`、`/growth/social-progress`

---

### 站 11｜VIP（展示态）

| 步骤 | 操作 | 预期 |
|---|---|---|
| 11.1 | 我的tab → VIP卡/菜单 | VIP 页：权益 + 套餐列表展示（开关已开，入口可见） |
| 11.2 | 点「立即开通」 | **real 模式提示「功能建设中」**——后端未接入微信支付（有意为之，非缺陷；页面守卫保证不假支付成功） |
| 11.3 | 优惠码 | 优惠码页输入验证/兑换（`POST /vip/promo-codes/validate`、`/redeem`） |
| 11.4 | 账单 | 账单列表（`GET /vip/bills`；当前无种子，空态展示） |

**说明**：VIP **购买支付链路当前真实不可用**（无微信支付端点，前端显式守卫）；展示走 11.1/11.3/11.4。

---

### 站 12｜我的 / 资料 / 恋爱中心 / 商城

| 步骤 | 操作 | 预期 |
|---|---|---|
| 12.1 | 我的tab | 资料卡（头像/昵称/北大/认证金徽章）+ 菜单项完整 |
| 12.2 | 头像上传 | 点头像 → 选择图片 → 上传（`POST /media/upload` 本地磁盘 → `POST /profile/avatar`）→ 头像更新 |
| 12.3 | 相册 | 我的相册 → 添加照片（`POST /profile/photos`） |
| 12.4 | 访客 | 我的 → 谁看过我（列表 + 访客详情） |
| 12.5 | 完善资料 | 资料编辑（基本资料/校园/课表/推荐偏好，`GET/PUT /profile/*`） |
| 12.6 | 恋爱中心 | 附近的人（推荐卡片复用 `GET /recommendations`）、MBTI 测试（web-view，模拟器已关域名校验）、恋爱咨询（web-view） |
| 12.7 | 商城 | 商品列表（种子 4 条）；**无商品详情页**（前端 TODO，点商品仅 toast） |
| 12.8 | 帮助/客服 | 帮助页 → 提交反馈/建议（`POST /feedback/issues\|suggestions`、`POST /feedback/images`）→ 我的反馈历史（`GET /feedback/my-submissions`） |
| 12.9 | 设置 | 免打扰（`GET/PUT /dnd`）、退出登录（`POST /auth/logout`）→ 回到登录页 |

**涉及 API**：`/media/upload`、`/profile/*`、`/feedback/*`、`/dnd`、`/auth/logout`

---

## 3. 链路可用性清单

| 链路 | 状态 | 说明 |
|---|---|---|
| 登录（guest / phone） | ✅ 真实 | guest 固定 47；phone 支持任意注册账号 |
| 匹配推荐流 / 滑卡 / 心动信号 | ✅ 真实 | 种子用户齐全 |
| 私聊 / 语音 / 官方号 / 通知 | ✅ 真实 | 需双账号演示双向 |
| 临时会话 + 交换联系方式 | ✅ 真实 | 需双账号协作 |
| 钱包余额 / 演示充值 / 流水 | ✅ 真实 | 充值每日 5 次 |
| VIP 红包（发/领） | ✅ 真实 | 真实扣款入账，需双账号 |
| 解锁喜欢/访客 | ✅ 真实 | 300 分/次 |
| 视频通话 | ⚠️ 状态机真实 | 无真实 RTC 音视频（后端无 RTC 服务，仅呼叫/接听/记录状态） |
| 帖子/圈子/校园/活动 | ✅ 真实 | 种子齐全；发帖限流 |
| 签到/补签/每日一问/成长 | ✅ 真实 | 每日一问有 08-07~08-11 种子 |
| VIP 购买支付 | 🚫 不可用 | 后端未接入微信支付，前端 real 模式守卫提示「建设中」（业务决策） |
| 聊天红包 | ⚠️ 壳页面 | 无 API 调用；真实红包在 VIP 红包链路 |
| 商城商品详情 | 🚫 不可用 | 前端 TODO，仅列表展示 |
| MBTI / 恋爱咨询 | ⚠️ web-view | 外链（16types.com 等），模拟器已关域名校验可打开 |
| AI 聊天 | 🚫 不可用 | `chat_ai_enabled` 全端硬编码 false（无 AI 端点） |
| 短信验证码 | 🚫 不存在 | 登录用手机号+密码（无 SMS） |

---

## 4. 双账号协作速查

| 场景 | 主账号 47 | 副账号 8 |
|---|---|---|
| 双向聊天 | 给 8 发消息 | 回复 47 |
| 交换联系方式 | 发起临时会话 + 请求 | 同意请求 |
| 红包 | 发红包（扣 47 余额） | 领红包（入 8 余额），也可反向 |
| 视频通话 | 发起呼叫 | 接听/挂断 |

副账号登录：登录页 → 手机号 `13800000002` / 密码 `Walkthrough@123`（手机号+密码登录；密码在 seed 文件中为 BCrypt 哈希，明文见本手册）。

**自动化截图协作**（mp-shoot-walkthrough.cjs 两轮）：
- 第一轮：主账号 47（运行时 `POST /auth/guest-login` 取 token）截全部 12 站页面
- 第二轮：副账号 8（运行时 `POST /auth/phone-login` 取 token）截双账号页：消息列表、chat-session（与 47 的会话）、红包领取、视频通话、临时会话交换联系方式——两张图拼合即为双向链路证据

---

## 5. 复现数据准备的命令（换环境时）

```bash
# 1. 数据库种子（47/8 钱包 5000 分 + 8 号密码）
mysql -h127.0.0.1 -P3307 -uroot -p campus_love < database/seed-walkthrough-daily-question.sql

# 2. 前端开关（membershipEnabled/videoCallEnabled/redPacketEnabled 等）
#    apps/client/src/config/feature-flags.ts 置 true

# 3. 开发者工具自动化启动（2026-08-08 验证的可靠序列；关键点：--auto-port 与 --trust-project，且须在 IDE 未运行时执行）
#    - 若曾强杀 IDE 导致编译缓存损坏：删除 User Data/<profile>/WeappCache 后重启
#    - project.private.config.json 必须包含 "urlCheck": false（否则 devtools 代理拦截本地 https API → 页面空壳）
cli.bat auto --project D:\6\恋爱小程序 --auto-port 9420 --trust-project

# 4. 自动化脚本（运行时登录换取真实 token，替代过期硬编码 JWT）
node scripts/mp-shoot-walkthrough.cjs --round=1   # 主账号 47 全量 12 站
node scripts/mp-shoot-walkthrough.cjs --round=2   # 副账号 8 双账号页

# 3. 重新编译小程序（dev 模式热重载自动生效；build 验证用 Node 20）
```

---

*配套产物：`database/seed-walkthrough-daily-question.sql`（数据）、[feature-flags.ts](apps/client/src/config/feature-flags.ts)（开关）、本手册（流程）。*

# 全页面理想图视觉比对报告（2026-08-26）

> 比对工具：MiMo 视觉 API（`D:\6\glm-vision-mcp\scripts\see-mimo.py`，xmomimo mimo-v2.5）
> 理想图源：`素材/理想效果图/`（18 张）
> 实际截图源：当日 mp-weixin 实机截图 `报告/验证截图-2026-08-26/`（代表当前最新代码）+ 历史 H5 截图 `截图存档/`（陈旧基线，仅供参考）
> 产出目录：`报告/2026-08-26-全页面视觉比对/`（A1~F6 共 24 个 JSON + 4 份汇总）

## 结论

18 张理想图逐页比对已完成。**绝大多数 P0/P1 差异属于「采集环境差异」**：

- 截图采集时为未登录态（页面被登录引导弹窗/锁定页覆盖）
- 后端数据未就绪 / 网络请求失败（页面停留在错误态/重试态/空态）
- 真实业务数据（昵称、头像、人数）与理想图示意内容不同

以上均非代码样式缺陷，需在登录态 + 数据完备的 GUI 环境补拍正常态后复验。

**真正的代码级缺陷已在本轮全部修复**（对应本 spec 已批准任务）：

| 缺陷 | 根因 | 修复 |
| --- | --- | --- |
| P0-1 帖子详情评论区被巨大黑心 SVG 覆盖 | [detail.vue](file:///d:/6/恋爱小程序/apps/client/src/pages/village/detail.vue) `.detail-header__spacer {` 缺闭合大括号吞掉 75+ 行规则（含 comment-heart__icon 32rpx） | 改为空规则 + 删除多余闭括号 |
| P0-2 他人主页无法下滑 | [PublicProfile.vue](file:///d:/6/恋爱小程序/apps/client/src/components/profile/public/PublicProfile.vue) padding-bottom:200rpx 不足 | 加大至 `calc(220rpx + safe-area + 60rpx)` |
| P0-3 "我的故事"点击无反应 | [profile/index.vue](file:///d:/6/恋爱小程序/apps/client/src/pages/profile/index.vue) `<ProfileShell>` 缺 story 事件绑定 | 补 `@story-photo`/`@story-video` + 2 函数 |
| P1-1 社区动态山峰灰图标 | `NEARBY_ICONS.CIRCLE` 指向不存在的 `common/group.svg` | 改指真实 `group.svg` |
| P1-2 聊天 self/peer 头像同名错位隐患 | ChatBubble peer 默认/头像池含 AVATAR_1（与 self 真实头像 person-01 同图） | peer 默认改 AVATAR_3、池首项改 AVATAR_4 |
| P1-3 后端 samecity 400 | `PostCategory` 枚举无 samecity，city 为空时落入 parseCategory 抛 400 | 后端按当前用户校区城市兜底过滤 |

## 各页差异要点（当日实机截图）

### 首页 / 附近 / 帖子
- **首页**：P0 今日推荐空、社区动态请求失败；P1 恋爱进度数据 0、关系动态 0 —— 数据为主
- **附近的首页**：热门兴趣圈、附近动态区缺失；入口图标/文案与理想有差异
- **帖子详情**：截图处于"帖子不存在"空态（数据），导航与空态样式正常（P0-1 已修心形遮挡）
- **帖子流（村口）**：截图为未登录弹登录引导 —— 登录态差异

### 消息 / 聊天 / 圈子
- **消息列表**：截图被登录引导覆盖无法有效比对
- **聊天气泡**：截图未登录（锁定提示），self/peer 头像布局无法从该图判定（P1-2 代码侧已查证正确并修隐患）
- **兴趣圈**：空态（暂无兴趣圈）—— 数据差异
- **圈子详情**：网络失败态 + 顶部绿色渐变导航为既定品牌设计（非 bug）

### 匹配 / 主页 / 登录 / 认证
- **寻觅卡片**：整页加载失败态（数据/网络），缺失操作按钮因失败态未渲染
- **匹配中**：加载失败态；动画组件 MatchLoading 已实现双头像+粉心+同心圆（代码正确）
- **匹配成功**：头像为占位图（数据）、共用点文案差异（示意 vs 真实数据）
- **我的主页**：截图未登录（显示"点击登录"）；故事卡/互动项代码数据完整（3 卡 + 4 项）
- **未登录主页**：代码已正确渲染 NotLoggedProfile 登录引导 —— 截图与代码一致
- **登录页**：标题/文案/按钮为既定设计 vs 纯背景理想图，视觉层级差异存续（P2 级）
- **认证中心/实名认证**：理想参照图错配（非同一流程页），差异不计

## 遗留（需 GUI + 登录态补拍后复验）

1. 聊天气泡 self/peer 同屏布局（需登录态截图）
2. 寻觅卡片页正常态（需后端数据可用环境）
3. 登录页 P2 级视觉层级优化（标题字号 28 vs 36、协议勾选态）

## 交付说明

- 本报告为视觉比对结论归档；代码级可确认缺陷均已纳入本 spec 修复并进入验证（Task 9）
- P0-4（喜欢我的匿名墙）为 VIP 解锁产品设计，业务上非问题，未改动
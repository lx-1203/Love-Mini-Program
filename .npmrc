# ============================================================
# npm 配置（Task 49：依赖版本固定）
# ============================================================
# save-exact=true：npm install <pkg> 时默认保存精确版本（不带 ^ 或 ~）
# engine-strict=true：依赖的 engines 字段不匹配时拒绝安装
# ============================================================
save-exact=true
engine-strict=true

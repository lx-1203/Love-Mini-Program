# 2026-08-23

- 修复 Codex 桌面端加载失败：cc-switch 重写 `config.toml` 后缺失 `model_provider`，桌面端回退为 `custom` provider 并因缺少 `[model_providers.custom]` 报错。已做最小化补节修复（保留 cc-switch 代理设置），并恢复 `max_output_tokens=16384`、把目录模型 provider 改回 `mimo`。
- 二次修复：桌面端仍报 `Model provider 'custom' not found`。原因是 `config.toml` 被改回直连模式后再次缺失 `model_provider` 与 provider 段。补充 `model_provider = "mimo"`、`[model_providers.mimo]` 与兜底 `[model_providers.custom]`（均指向直连 MIMO 端点与真实 token），并恢复 `max_output_tokens = 16384`。TOML 合法，`codex debug models` 加载 2 个模型。

- 投资人版立项书（报告/校园恋爱小程序-立项书.md）第七节「发展路线图与里程碑」重建为四阶段战略路线图：阶段一发布准备(M0–M5)→阶段二验证增长(上线后0–6月,G1–G3)→阶段三规模扩张(6–18月)→阶段四商业化深化(18月+)，并新增 7.6 融资节奏对齐；上市后量化指标以【待补充】标注。
- 用户反馈"不知道还有多久上线"，故将 7.2 近-term 计划由具体日历窗口改为"次序确定、时点待定"（去掉 08-24~10-04 窗口与全文"约6周"表述），保留 M1–M5 工作项与依赖关系，7.1/执行摘要/结论同步软化。理念：上线日未定时不锁日期，只锁工作次序与依赖。
- 导出 Word：用 tencent-docx 技能链（html-to-docx）将 `报告/校园恋爱小程序-立项书.md` 生成 `报告/校园恋爱小程序-立项书.docx`。关键坑：① 默认 venv 在 $HOME 被沙箱回滚，需 `export HTML_TO_DOCX_VENV="D:/6/恋爱小程序/.workbuddy/venv-html-to-docx"` 重定向到工作区内；② uv 在 $HOME/.local/bin/uv.exe，用 `uv pip install --python <venv>/Scripts/python.exe --only-binary=:all: python-docx html-for-docx beautifulsoup4 lxml httpx Pillow click` 装依赖；③ 转换器 markdown 不含中文时路径用工作区内绝对路径；④ 标题后紧跟 `<blockquote>` 会被合并进标题段，改用 `<p class="note">` 规避。排版本体脚本 `D:/6/恋爱小程序/.workbuddy/md2html.py`（定向 md→排版HTML，注入封面/目录/表格样式）。
- 用户确认建设单位主体名称=**南京皓瀚星网络科技有限公司**（写入 2.1 表格与第八节团队）。并明确：其余【待补充】占位项（微信审核类目/隐私政策/资质、真机机型矩阵、商业化决策人、上市后量化指标、融资规模与资金用途占比、商业化相关占位）**全部删除、不保留**。已重写 md 与 docx：删除"待补充事项"整节、内联【待补充】全部移除、第十节改为"资金用途方向"表（去占比列、去融资规模占位）、7.3 量化指标列去占位、4.1 用户细分 note 删除、5.3 标题去占位、6.2 平台集成去占位。核验 md 与 docx 均无残留占位（docx 119 段/13 表/0 警告）。
- 再次精简：删除整节「九、风险与应对（投资人视角）」与「十、融资需求与资金用途」（未讨论、有了再说），原「十一、结论」顺延为「九、结论」，并清除执行摘要"见第十节"、7.2.3"详见第九节 R1"、7.4"（R2）"等交叉引用。重构后 9 段结构：执行摘要→概述→市场→产品→商业模式→技术→路线图→团队→结论（docx 110 段/10 表/0 警告，无残留）。

# 恋爱小程序后台管理系统 · 设计决策说明

> 基于「画统筹」设计编排流程（需求发现 → 设计系统 → 原型生成 → 质量审查 → 导出）产出
> 画布文件：`恋爱小程序后台管理系统`（Ardot fileId 718712187998621）
> 设计令牌：`design-tokens-admin.md`｜模块清单：`后台功能模块清单.md`

## 一、为什么选这套设计系统

用户原始需求是「简洁、易用、操作便捷、导航清晰、界面清爽、复杂页模块化」。我以模板 00003（Meta 风格设计系统）为基底进行**后台化改造**：

- Meta 体系本身是「低干扰、工具感、信息密度高」的现代设计语言，与后台诉求天然契合；
- 其 cobalt 主色 `#0064e0` 克制、辨识度高，适合作为后台唯一主操作色；
- 中性灰阶（ink / steel / stone / hairline）完整，便于用「灰底托白卡」建立层级而无需依赖阴影；
- 圆角、间距、字体排印体系成熟，可直接收敛为后台紧凑规范。

**关键改造（去营销化）**：去除 Meta 原 100px 胶囊按钮与 24–32px 大圆角，后台统一收敛为按钮/输入 6px、卡片 8px；主色仅用于主操作与激活态，不用于背景或装饰，避免后台被误读为 C 端活动页。

## 二、做了哪些定制

1. **色彩角色重映射**：在 Meta 原值基础上定义后台角色（主色 / 语义色 / 页面底 / 卡片底 / 边框 / 文字三级），并补充浅底徽章配色（success/warning/danger-soft）。
2. **中文字体落地**：界面主字体定为 **Noto Sans SC**（画布已验证可用，含全字重），回退 PingFang SC / 微软雅黑；数字 / ID / 金额用等宽体保证列对齐。基准字号取 **14px**（后台紧凑），字重仅 400/500/600 保持克制。
3. **统一布局骨架**：左侧固定侧边栏 220px（可折叠 64px 图标态）+ 顶部工具栏 56px（sticky）+ 内容主区（灰底托白卡，主内边距 24px）。全站复用，切换一致。
4. **复合工作流组件**：针对后台高频场景扩展——多级侧边导航、筛选栏、详情抽屉（480/720px）、审核工作台（三栏 + 键盘流）、批量操作条、图表看板套件（单主色 + 灰度，禁高饱和多色）。
5. **RBAC 可见性**：用 `visible-by-role` 开关表达 5 类角色差异，不新增配色；危险操作统一 danger 文字按钮。

## 三、布局规范要点

- **侧边栏**：220px 常驻，12 个一级分组 + 二级菜单；选中态「浅蓝底 + 左 3px 主色竖条 + 主色字」三合一，一眼可定位。
- **顶部工具栏**：56px，左页面标题 + 面包屑，右高频操作（主按钮 / 搜索 / 头像）；一屏至多 1 个主按钮。
- **内容主区**：灰底 `#f1f4f7` + 白卡 + 1px 边框建立层级，默认无阴影；复杂页采用模块化分区 + 说明性文案降低学习成本。
- **响应式**：≥1280 常驻 / 1024–1279 收内边距 / 768–1023 侧栏图标态 / <768 抽屉 + 表格横滚。

## 四、功能覆盖率

- 一级分类 12 个，二级功能模块 75 个，**覆盖率 100%**（见 `后台功能模块清单.md` 与画布 Frame 01 全景覆盖图）。
- 每个二级模块均已映射到具体后台页面 / 界面区域（列表页、详情抽屉、审核工作台、配置表单、看板等）。

## 五、质量与下一步

- 已按令牌落地统一视觉语言；核心页面（数据概览 / 用户管理 / 内容审核 / 商业化会员 / 风控举报 / 系统设置等）已设计为可交互原型稿。
- 如需，可继续：① 补齐各二级模块的独立详情页；② 输出前端 Tailwind / React 变量文件；③ 走 export-specialist 导出 HTML / PDF 交付包。

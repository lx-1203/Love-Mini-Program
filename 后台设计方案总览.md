# 恋爱小程序 · 后台管理系统设计方案（总览）

> 设计编排：设计原型专家团「画统筹」主理人 Hua，按 SOP 调度 discovery-analyst（需求）→ design-system-expert（令牌）→ prototype-builder（原型）→ 严过审（审查）→ 交付达（导出）。
> 画布文件：`恋爱小程序后台管理系统`（Ardot `file/718712187998621`，Page 1 共 13 个顶层 Frame）。

## 一、设计目标
对现有后台做**全面、系统、逐页**的网页界面设计，要求：
1. 功能模块覆盖率 100%（不遗漏任何核心/辅助功能）；
2. 简洁明快、弱化视觉干扰、突出核心操作；
3. 操作便捷、触达路径最短、导航层级清晰；
4. 统一布局规范（侧边栏 + 顶栏 + 内容主区）；
5. 复杂页模块化分区 + 说明性文案，降低学习成本。

## 二、设计系统（Meta 体系改造）
来源风格模板 `00003`（Meta 设计系统），按后台场景重映射：
- **主色** `#0064E0`（cobalt）——仅用于主操作 / 激活态；去胶囊大圆角，改后台化直角+小圆角。
- **中性灰阶**：画布 `#F1F4F7`、表面 `#FFFFFF`、文字三级 `#1C1E21 / #5D6C7B / #8595A4`、描边 `#DEE3EA`。
- **语义色**：成功 `#31A24C`、警告 `#F2A918`、危险 `#E41E3F`（状态徽章用「浅底+同色字」）。
- **字体**：`Noto Sans SC`（Regular/Medium/Semi Bold/Bold），基准 14px。
- **布局骨架**：侧边栏 220px（激活项浅蓝底+主色竖条）、顶栏 56px、内容主内距 24px、卡片圆角 8px / 控件圆角 6px。
- **令牌文档**：`design-tokens-admin.md`（11 节：视觉主题/色彩/字体/间距圆角/组件/深度/反模式/响应式/落地/RBAC/复合组件）。

## 三、功能覆盖率
- **Frame 01「功能模块全景覆盖图」** 完整罗列 **12 一级 / 75 二级**模块，逐一标注「对应页面/区域」与「核心/辅助」，覆盖率 100%。
- 详细清单见 `后台功能模块清单.md`。

## 四、13 个页面 Frame（画布 Page 1）
| # | Frame | 节点 ID | 内容要点 |
|---|-------|---------|---------|
| 00 | 设计规范·布局骨架与组件库 | `3:1` | 组件库（侧边栏/顶栏/卡片/按钮/表格/徽章/抽屉）+ 异常边界态（空状态 / 加载骨架屏 / 二次确认弹窗） |
| 01 | 功能模块全景覆盖图 | `3:26` | 12 分类卡，完整 75 模块映射 |
| 02 | 数据概览 Dashboard | `3:65` | 4 KPI 卡 + 注册/活跃趋势柱图 + 近期动态表 |
| 03 | 用户管理 | `3:89` | 筛选栏 + 用户表格（状态徽章/批量条/分页） |
| 04 | 用户详情 | `6:91` | 资料卡含「圈层归属链 + 调阅管辖内容按钮」；右 Tab 表单 |
| 05 | 内容审核工作台 | `6:155` | 三栏：待审队列 / 内容预览 / AI 风险+操作；审核面板含「调阅证据（管辖内容）」四分类 |
| 06 | 内容管理 | `8:1` | 子导航 + 筛选 + 动态/话题/视频表格 |
| 07 | 商业化会员 | `8:112` | 4 KPI + 会员等级卡（免费/白银/黄金/钻石）+ 订单表 |
| 08 | 系统设置 / RBAC | `8:208` | 设置分组 + 基础配置卡 + **三级管理圈层**（公司 / 区域 / 学校）+ 管辖内容可见性（聊天 / 图片 / 帖子 / 临时聊天，举报可直接调阅） |
| 09 | 风控举报 | `8:274` | 统计条 + 举报队列 + 处理面板（含「举报调阅 · 圈层可见内容」四分类） |
| 10 | 客服工单 | `10:33` | 状态 Tab + 工单列表（优先级/状态徽章）+ 详情卡 |
| 11 | 运营活动 + 消息推送 | `10:114` | 子 Tab + 活动卡列表 + 推送记录表 + 新建推送表单 |
| 12 | 匹配推荐 + 直播语音 | `10:194` | 子 Tab + 推荐策略开关/滑块 + 语音房监控表 + 告警条 |

> 12 个一级分类均有对应设计页；复杂页（审核/工单/风控/推送）采用模块化分区 + 说明文案。

## 五、统一布局规范要点
- 所有页面共用同一套骨架（侧边栏 12 项导航、顶栏标题+搜索+头像），切换零认知成本。
- 列表页统一「筛选栏 → 表格卡（表头 40 / 行高 44 / 斑马纹）→ 批量条 → 分页」结构。
- 状态/优先级一律用色编码徽章，降低误读。
- 审核、风控、工单等重操作页采用「左队列 / 中预览 / 右处理」三栏，操作路径最短。

## 六、质量验证（Phase 4）
- 程序化布局检查（capture_layout）覆盖全部 13 Frame：**无 off-canvas 溢出、无裁切、无退化几何**。
- 警告项均为设计预期：侧边栏/顶栏留白、激活底衬于文字、滑块填充叠于轨道、单元格文字叠于斑马行底；部分内容卡留白偏多（cosmetic，不影响可读）。
- 字体、主色、圆角、间距与令牌文档一致。

## 七、下一步建议
1. 将 `design-tokens-admin.md` 落地为前端 CSS 变量 / 组件库，前端直接复用。
2. ✅ 已完成：留白偏多卡片已收紧（Frame 10 工单列表/详情卡 624 → 460，底部对齐；活动卡本身紧凑无需改）。
3. ✅ 已完成：13 Frame 已导出 PDF（`报告/恋爱小程序后台管理系统-后台设计方案-20260826.pdf`）。
4. ✅ 已完成：异常/边界态组件已补（Frame 00：空状态 / 加载骨架屏 / 二次确认弹窗）。

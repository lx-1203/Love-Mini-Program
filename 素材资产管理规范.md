# 素材资产管理规范（公告）

> **发布对象**：全体研发 / 设计 / QA
> **生效日期**：2026-09-03（发布周前）
> **触发背景**：2026-09-03 20:06 首页「关系动态」吉祥物图标素材被直接覆盖，导致 4 个 cell 图标大小不一、风格错乱、边缘锯齿（线上可见 UI 缺陷）。根因：修改者直接覆盖了 `apps/client/src/static/assets/images/mascot/` 下的同名文件，而该目录会被 `prepare-static.mjs --dev` 先清空再从备份恢复，**对 src/static 的直接覆盖是无效且危险的**。
> **本规范适用范围**：小程序端全部**静态图片素材**（吉祥物、头像、插画、装饰图、封面图、AI 生成图），不适用纯前端 SVG 图标代码。

---

## 一、吉祥物原版（唯一真相源，禁止改动）

「寻觅吉祥物小绿人」透明背景 PNG 的**唯一权威副本**：

| 文件 | 唯一真相源路径 | 大小（字节） |
|------|---------------|-------------|
| mascot_heart.png | `apps/api/uploads/app-assets/assets/images/mascot/mascot_heart.png` | 7,979 |
| mascot_shy.png | `apps/api/uploads/app-assets/assets/images/mascot/mascot_shy.png` | 7,720 |
| mascot_wave.png | `apps/api/uploads/app-assets/assets/images/mascot/mascot_wave.png` | 7,336 |
| mascot_cheer.png | `apps/api/uploads/app-assets/assets/images/mascot/mascot_cheer.png` | 6,272 |

**认定方法**：透明背景 + 上述字节数。凡大小不是 7979/7720/7336/6272 的 4 张吉祥物 = 被污染，立即按第三节 SOP 恢复。

> ⚠️ 该目录同时存放**其它**小绿人表情（mascot_angry / mascot_calm / mascot_clap / mascot_default / mascot_like / mascot_love / mascot_happy / mascot_sleepy / mascot_thinking / mascot_wink… 共 30+ 张），它们都是同款「寻觅吉祥物」资产，可按需引用，但**严禁混入非小绿人角色的图**顶替。

---

## 二、铁律（三不许 + 一必须）

1. **不许直接覆盖** `apps/client/src/static/assets/images/mascot/`（或任何 `src/static/` 子目录）下的同名文件——`prepare-static.mjs --dev` 每次构建先 `rm` 清空整个 `src/static` 再从备份恢复，你的覆盖会在下一次构建中被冲掉，或在崩溃瞬间把修复成果一起丢掉（2026-09-03 已发生 2 次）。
2. **不许只改一处**——素材在工程里必须 **3 处同步**（见第三节），漏一处必然出现「构建后图变了/图没了/图不新」。
3. **不许用错角色**——首页/资料/聊天气泡里的装饰吉祥物一律用「寻觅吉祥物小绿人」透明背景 PNG；不要拿别的角色插画（实心背景、异形宽高比）顶替，会造成大小不一与杂边。
4. **必须走同步 SOP**——新增或修改任何静态图片，按下节流程执行，完成后跑一遍验证清单。

---

## 三、素材 3 处同步 SOP

静态图片素材在工程中存在 3 个副本，**任何新增/修改必须三处同步**：

| # | 路径 | 作用 | 何时写入 |
|---|------|------|---------|
| A | `apps/client/src/static/assets/images/...` | **真实构建源**（vite 从这里打包进 dist） | 每次都要 |
| B | `apps/client/static-local-backup/full-static/assets/images/...` | `prepare-static.mjs --dev` 的恢复源（会整体 cp 回 A） | 每次都要（漏了 = 下次构建丢图） |
| C | `apps/api/uploads/app-assets/assets/images/...` | 后端 app-assets 权威源（real 模式经媒体代理供图；吉祥物/装饰图同时是 C 的**唯一真相源**） | 涉及 app-assets 消费链时同步 |

**Step-by-step：**

```bash
# 1) 准备素材（透明背景 PNG；如为 AI 生成图先裁水印，见第五节）
# 2) 双路径写入小程序侧（A + B 必须同字节）
cp <新图>.png apps/client/src/static/assets/images/<分类>/<name>.png
cp <新图>.png apps/client/static-local-backup/full-static/assets/images/<分类>/<name>.png

# 3) 同步后端权威源（C）
cp <新图>.png apps/api/uploads/app-assets/assets/images/<分类>/<name>.png

# 4) 若走 app-assets 消费链（real 构建 IMAGE_PATHS = 后端 URL），还需注册 DB（见第四节）
# 5) 一致性校验
md5sum apps/client/src/static/assets/images/<分类>/<name>.png \
       apps/client/static-local-backup/full-static/assets/images/<分类>/<name>.png \
       apps/api/uploads/app-assets/assets/images/<分类>/<name>.png
```

> 💡 `apps/client/src/static` 与 `static-local-backup/full-static` 理论上应是**完全同构**。新增文件时若只改其一，可用
> `cp -r apps/client/static-local-backup/full-static/assets/images/. apps/client/src/static/assets/images/` 对齐（注意别反向覆盖回错误内容）。

---

## 四、real 模式：media_asset 表注册（仅后端 app-assets 消费链需要）

`MediaAccessController` 对 `type=app_asset` 的访问有强校验（`findByUrlAndType(type='app_asset', audit_status='approved')`），**只 cp 文件不注册 DB，real 构建下图片 403 不显示**。

```sql
INSERT IGNORE INTO media_asset
  (user_id, type, url, mime, size, status, audit_status, created_at, version)
VALUES
  (0, 'app_asset', '/assets/images/<分类>/<name>.png', 'image/png', <字节数>, 'ready', 'approved', NOW(), 0);
```

---

## 五、AI 生成图三步链路（ImageGen → ffmpeg → 同步）

若素材由 AI 生成，必须先裁水印再入库：

```bash
# ImageGen 输出默认带右下角 "AI生成 WORKBUDDY" 水印（Hunyuan 1536x1024）
ffmpeg -i <raw>.png -vf "crop=iw:ih-88:0:0" -q:v 3 <clean>.jpg
# 然后按第三节把 <clean>.jpg 三处同步（real 需第四节 DB 注册）
```

---

## 六、覆盖 / 变更后验证清单（Checklist）

完成任何素材变更后逐项打勾：

- [ ] `md5sum` 三处副本字节一致
- [ ] 若走 real app-assets：`INSERT IGNORE INTO media_asset` 已执行且 `audit_status='approved'`
- [ ] 重新构建：`cd apps/client && npx -y pnpm@11.17.0 exec uni build --platform mp-weixin --mode mp-weixin` EXIT=0
- [ ] 模拟器截图核对目标页面：图清晰、无锯齿、无错位、风格统一
- [ ] 若图被用于 cell/按钮点击区：确认图带 `pointer-events="none"`（防 iOS WebView 吞 tap）
- [ ] 若图是 http:// 源（real 开发联调）：确认过 `resolveMediaUrl` 或本地化到 `/static/`（mp base lib 拒 http 图）

---

## 七、责任人

- **素材入库审批**：前端 lead / 设计（谁产生素材谁执行 SOP，代码评审人核对三处同步）
- **发布周巡检**：QA 在 release 前跑一遍第四节 SQL 存量 + `md5sum` 抽查，重点核对吉祥物 4 张（7979/7720/7336/6272）未被污染
- **本规范维护**：工程保障团队（发现新坑及时回写本文件）

---

> 本公告由工程保障团队发布（2026-09-03），作为发布周素材变更的强制约束。违反导致的线上素材缺陷按 UI 缺陷等级追溯。

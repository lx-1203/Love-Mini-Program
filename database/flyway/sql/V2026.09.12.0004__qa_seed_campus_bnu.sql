-- ============================================================
-- 迁移：QA 演示种子用户校区补齐（北京师范大学队列）
-- ============================================================
-- 背景（2026-09-12 寻觅推荐/附近空态排查）：
--   体验账号的校区档案由 GuestDemoDataProvisioner 预填为「北京师范大学」，
--   推荐策略默认 scope=campus_first（同校优先）→ 候选按
--   user_campus_profile.campus_name = '北京师范大学' 过滤。
--   既有 QA 种子用户（10001-10050）的校区档案分别为北大/清华/浙大等，
--   无一北师大 → 同校候选为空，推荐池只剩未填校区的用户（近乎空态）。
--
--   本迁移为 12 名种子用户（男女交替）补齐北师大校区档案，并同步
--   users.campus_name，保证体验账号在模拟器内有同校推荐池。
--   种子坐标已由 V2026.09.12.0003 重排到模拟器定位点周边（<20km），
--   「附近的人」同理恢复。
--
-- 幂等：固定 id 列表 + 固定值，可重放。
-- ============================================================

UPDATE user_campus_profile
SET campus_name = '北京师范大学',
    city_name = '北京',
    updated_at = NOW()
WHERE user_id IN (10001, 10003, 10005, 10008, 10010, 10012, 10015, 10016, 10018, 10020, 10023, 10025);

UPDATE users
SET campus_name = '北京师范大学',
    updated_at = NOW()
WHERE id IN (10001, 10003, 10005, 10008, 10010, 10012, 10015, 10016, 10018, 10020, 10023, 10025);

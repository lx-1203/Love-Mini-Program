-- ============================================================
-- 迁移：campus_topics 表新增 tags 列（3-L 校园话题标签）
-- ============================================================
-- 背景：
--   创建校园话题请求新增 List<tags>（≤5 个，每个 ≤20 字符），
--   用于话题详情页展示 #话题 标签。
--
-- 存储方式：JSON 列（与 campus_topics.images 同款模式），
--   避免引入多对多关联表（话题标签为展示性弱结构化数据，无按标签检索需求）。
-- ============================================================

ALTER TABLE campus_topics
    ADD COLUMN tags JSON NULL COMMENT '话题标签数组（最多 5 个，每个 ≤20 字符）';

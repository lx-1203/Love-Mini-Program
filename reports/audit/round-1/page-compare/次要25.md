# R1 需求对照 · 次要25（8 页功能目标核对）

- 审查员：A4 需求对照员（次要25）
- 轮次：R1（gitSha aefd8a72；exec-results.json 与 screenshot-manifest.json 双确认一致）
- 证据源：
  - 执行结果：`reports/audit/round-1/interact/exec-results.json`（本轮构建 buildMode=build:mp-weixin:mock，见 screenshot-manifest.json；本 8 页相关用例 DEV01–DEV10 / SC01–SC08 / FB01–FB18 / DC01–DC08 / ACT01–ACT16 / PRI01–PRI06 / AGR01–AGR06 / MD01–MD12）
  - 巡检截图：`reports/screenshots/round-1-tour/A/subpackages_*.png`（本组 7 页 14 张逐张已看；setup/dev 与 setup/showcase 在 305 张 manifest 中 0 条目、磁盘亦不存在）
  - 交互截图：`reports/screenshots/round-1-interact/`（MD01 封存态、FB02/03/08 等已看）
  - 源码：`apps/client/src/subpackages/{setup,support,discover,legal,market}/`、`apps/client/src/pages.json`、`apps/client/src/components/common/LegalTextPage.vue`、`apps/client/src/config/{legal-texts,page-access,navigation,showcase}.ts`、`apps/client/src/custom-tab-bar/index.js`、`apps/client/src/stores/{feedback,activity}.ts`、`apps/client/dist/build/*/app.json`
- 理想稿锚定说明：`素材/理想效果图/` 实测 19 张已列目录核对，**均无本 8 页的页面级理想稿**（19 张全部锚定主包/核心流程页）。故各页 baseline 以「页面声明功能目标（源码头注释 + 路由语义 + 预加载规则注释）+ 交互 manifest 预期 + v3.1 契约 Token 链」为对照依据，不做像素级断言。
- 结构分级口径（本报告 L1–L10）：L1 信息架构/页面目标 → L2 布局骨架 → L3 组件清单 → L4 关键内容与功能呈现 → L5 状态完备 → L6 交互反馈 → L7 视觉 token → L8 数据契约 → L9 导航可达性 → L10 平台兼容/构建注册。L1–L4 偏差即不达标。
- 执行命令备案（本轮实际运行）：
  - `python` 解析 exec-results.json 按 page 前缀过滤 8 页全部用例（输出 74 条，已逐条读）
  - `python` 正则提取 pages.json 注册表 + tabBar.list，与 showcase(49 项)/dev(22 项) 条目逐一比对（输出：showcase/dev 的 isTab-but-not-tab 均恰为 `/subpackages/village/village/index`）
  - `python` 读 dist/build/{mp-weixin,mp-weixin-qa,mp-weixin-dev-4}/app.json 三份产物：subpackages/setup 均为 `['profile','campus','schedule','recommend-pref','interest','showcase']`，**均无 dev/index**
  - `grep -rn "dev-debug"` 全仓（仅 package.json:51 定义 + manifest.json:15-17 注释 + 审计文档）；`grep -rn "discover/discussions"` 与 `DISCUSSIONS`（仅 routes.ts:245 定义 + dev/showcase 条目，无任何消费者）；`grep -rn "legal/privacy|legal/agreement"` 与 `SUBPACKAGE_ROUTES.LEGAL`（login:616/621 + settings:110-119/:393-401）
  - 逐张查看截图：activities 默认、discussions 默认、feedback 默认、market_detail 默认（封存态）

---

## 1. subpackages/setup/dev/index（开发调试页，仅 DEV 构建注册）

**target（应该具备）**：① 仅 DEV 构建注册（pages.json `#ifdef DEV`）、生产构建完全剔除；② DEV 构建中可用：一键登录超级测试账号（凭据走构建环境变量、未配置禁用、连点防重）；③ 会员身份模拟开关（toast+存储同步、isDev 守卫）；④ 全部页面分组导航（Tab 项 switchTab/其余 navigateTo）；⑤ 头部返回=来源页；⑥ onShow 刷新登录态/会员态；⑦ 生产包无任何残留入口。

**current（代码+执行证实）**：
- 源码 7 项全部落实：`setup/dev/index.vue:95-121`（一键登录：isDev 守卫→无密码禁用 toast→superLoginBusy 防重→成功 800ms 后 replaceAppPath discover，定时器 `:86,:110-114,:185-190` 清理）；`:144-159`（VIP 模拟切换+isDev 守卫+STORAGE_KEYS 统一键）；`:193-220`（22 项页面清单）；`:229-235`（isTab 分支）；`:238-240`（goBack）；`:176-179`（onShow 刷新）。凭据零硬编码（`:29-35,:41-80` 均 VITE_ 环境变量读取）。
- **执行证据 0 通过**：DEV01–DEV10 全部 `top=subpackages/setup/interest/index | navError=Uncaught [object Object]`——reLaunch/navigateTo 均无法到达本页；DEV02–DEV06 action-not-performed（`.dev-header__back`/「一键登录超级测试账号」/「会员身份模拟」element not found）；DEV07 SKIPPED（4 个 isTab 项含村口，不可自动化）；DEV08–DEV10 标 EXECUTED 但观测点全停在 interest 页（无效执行）。DEV03/04/06 三张 after 截图为 ERROR:timeout，DEV04/05/09 实拍内容为资料页。静态巡检 305 张 0 条目。
- **根因（本轮新证）**：`#ifdef DEV` 条件在全链路上无激活路径——
  1. DEV 条件唯一来源是 package.json:50-62 的 uni-app 自定义平台 `dev-debug`（define.DEV=true），需要以 `uni -p dev-debug` 构建才激活；
  2. 而 package.json:19-28 全部构建脚本（dev:h5/dev:mp-weixin/build:mp-weixin×5）无一使用该平台（均为 `--platform mp-weixin/h5`）；全仓 grep "dev-debug" 无任何调用点；
  3. 产物实证：dist/build 下 mp-weixin、mp-weixin-qa、mp-weixin-dev-4 三份 app.json 的 subpackages/setup 均无 dev/index（mp-weixin-dev-4 名为 dev 产物同样没有）；
  4. `src/manifest.json:15-17` 注释宣称「开发构建（dev:h5 / dev:mp-weixin）经 --script dev-debug 注入」与实际脚本不符（两脚本均无 --script dev-debug）——文档与构建脱钩；
  5. 运行时入口亦已消失：pages/profile/index.vue 仅剩孤儿样式 `.dev-entry`（:3722-3737），模板无 dev 入口按钮（grep v-if="isDev"/dev-entry 无模板命中），manifest.json:11 所述第 3 层入口不存在。
- 生产剔除方向正确：mock/real 构建确实不含本页（无泄漏风险），但这同时意味着「DEV 构建可用的调试导航页」这一功能目标在任何构建中都未实现——R1 交互矩阵为该页准备的 10 个用例全数落空即为直接后果。

**gap**：DEV 条件无激活链路 → 页面在所有产物中均未注册（Function，P2，MP-R1-REQ25-DEV-001）。dev 页内村口条目 isTab:true 死链与 showcase 同根因（代码审查线 MP-R1-DEV-001 已立 P2，本线不重复立）。

**verdict：不达标**（非安全泄漏方向，而是「仅 DEV 构建注册」的注册半边从未成立：目标构建中页面不存在、交互 10 用例 0 有效、视觉证据 0；L1/L4/L9/L10 四级偏差）。

---

## 2. subpackages/setup/showcase/index（全功能展示页）

**target**：① 仅展示构建（VITE_SHOWCASE_MODE=true）作主入口：分组展示全部页面+一键跳转体验；② 正式/mock 构建访问时 onShow 回落发现页（无展示痕迹）；③ Tab 项 switchTab、其余 navigateTo；④ 头部返回（有栈 navigateBack/无栈回落 discover）；⑤ 分组计数徽章与条目数一致；⑥ i18n 文案齐全。

**current**：
- ①② 代码落实：`setup/showcase/index.vue:156-162` onShow 非 showcase 模式 switchTab 回 discover。执行证据：SC02 EXECUTED 证实 mock 构建访问回落（top=pages/discover/index）——**守卫按设计工作**。SC01（展示构建主入口渲染 6 分组 44 项）因本轮 mock 构建非 showcase 模式而无法观测（top 落 discover，属预期回落而非缺陷）；showcase 构建产物 dist/build 下不存在（脚本 build:mp-weixin:showcase + .env.mp-weixin-showcase（VITE_SHOWCASE_MODE=true 已核）链路齐备但本轮未产出包），故本页在本轮 0 实拍。
- ③④ 代码落实：`:164-171` navigate isTab 分支；`:174-181` goBack 带无栈 fallback switchTab discover。SC03 失败系守卫先回落导致按钮不存在（非缺陷）；SC08 同理。
- ⑤ 计数徽章存在：`:226` `t('showcase.groupCount', { n: group.items.length })`（SC07 的 `.groupCount:absent` 为选择器错位，实际类名 `sc-group__count` :410）。
- ⑥ i18n key 齐全（showcase.groups.*，useI18n :17）。
- 实测结构：**7 分组 49 项**（journey 7/match 5/home 7/chat 2/community 13/commerce 5/profile 10，我的 python 脚本逐一提取并比对 pages.json 注册表：除主包 5 页因我方脚本未拼主包前缀的误报外，子包路径全部注册有效）。manifest 期望「6 分组 44 项」与代码漂移（清单扩张未同步用例），记录不立 Issue。
- **功能断裂 1 项**：community 组村口 `/subpackages/village/village/index` 标 `isTab: true`（:103），而 pages.json tabBar.list 仅 5 tab（home/nearby/discover/messages/profile，:396-427）；`uni.switchTab` 对非 tab 页必败（无 fail 回调，点击无响应）。我的校验脚本输出：showcase 49 项中 isTab-but-not-tab 恰此 1 项。SC04 的 SKIPPED 观测（「村口·圈子（:103）」被列为 isTab 项）与代码一致。

**gap**：村口条目死链（1/49），展示版核心卖点「一键体验全部页面」存在 1 个哑钮（Function，P2，MP-R1-REQ25-SHOWCASE-001；与代码审查线 MP-R1-SHOWCASE-001 同根因，合并修复）。showcase 构建产物与首屏实拍缺失，①的目标态本轮未取证（下轮补）。

**verdict：基本达标**（守卫/结构/i18n/返回兜底齐备且有 SC02 证据；1 条死链 + 展示态 0 实拍）。

---

## 3. subpackages/support/feedback/index（意见反馈）

**target（功能9+功能10）**：① 三类型 chip（反馈/建议/活动提案）切换；② 标题/内容（≤5000 硬截断）/选填微信号输入；③ 图片上传 ≤3 张、单张 ≤5MB、jpg/jpeg/png/webp、隐私授权前置、点击全屏预览、长按删除带确认、显式删除按钮；④ 必填校验拦截空提交；⑤ 提交成功清表单+历史列表刷新；⑥ isSubmitting 防连点仅 1 次；⑦ 失败 toast 且表单保留；⑧ 历史入口跳转+列表项带参跳详情；⑨ 提交时间本地化。

**current**：
- 全部 9 项代码落实：chips `index.vue:52-56,:422-435`；输入 `:437-470`（maxlength=5000 `:69,:454`）；上传链 `:150-176`（ensurePrivacyAuthorized 前置）→ `:183-236`（chooseImage+扩展名/大小校验+取消静默）→ `:243-265`（上传/失败 toast/errorMessage）；缩略图 `:490-515`（tap 预览 `:294-302`、longpress 确认删除 `:272-287`、显式删除钮 `:505-514`）；上限/上传中守卫 `:89-91,:151-160`；提交 `:309-358`（必填 `:313-320`、锁 `:311,:321`、成功清表单 `:333-336`、失败 toast `:342-354`）；store 三个 submitXxx 成功后 `await this.load()` 刷新历史（stores/feedback.ts:42-100 逐个核实）；历史 `:366-404,:544-582`；时间本地化 `:414-416`。
- 执行证据：FB02（chip 切换）/FB03（三输入回显）/FB07（空提交拦截）/FB08（合法提交+列表刷新）/FB09（快击×5 锁）/FB15（历史入口）/FB17（失败分支）EXECUTED；默认态截图与 FB08-before/after 实拍证实表单区（chips+三输入+上传格+提交钮）与历史区（历史记录入口+列表行）首屏齐备；FB03 键盘弹起截图存在。FB04–FB06 失败为候选定位问题（input id 在 :439/:451/:463 均存在）；FB01 的 `.load/.pageTitle:absent` 为选择器错位（页面实际已加载，top=feedback）；FB10/12/13/14 SKIPPED=系统选图不可自动化（未执行，非缺陷）；FB18 navigateBack NAV_ERROR 为 harness 栈操作失败（AppShell show-back `:420` 存在）。
- 已知残留（他线已立，本线引用不复判）：H5 端 blob: URL 扩展名预校验必败 → 上传不可用（代码审查线 MP-R1-FEEDBACK-001，P2，mp-weixin 主端不受影响）；删除确认弹窗 title=content 同 key（MP-R1-FEEDBACK-003，UX）；跳转 fail 静默（MP-R1-FEEDBACK-002，P3）。

**gap**：无新增功能缺失（mp-weixin 主路径 target 全达成且有交互证据）。

**verdict：达标**。

---

## 4. subpackages/discover/discussions/index（讨论圈）

**target**：① 推荐讨论列表（骨架 loading/错误重试/空态三态）；② 条目点击弹详情（确认「去寻觅」/取消两路）；③ 底部主按钮去寻觅→发现 tab、次按钮反馈讨论建议→反馈页；④ 无登录门槛（discoverPageRequirements.requiresAuth=false）；⑤ 作为「讨论圈」功能对用户可达（pages.json:352-360 preloadRule 注释明示意图：进入寻觅 tab 预加载 discover 分包，「用户在寻觅页很可能下一步进入讨论圈或活动列表」）。

**current**：
- ①–④ 代码全部落实且页面自身工作正常：`discussions/index.vue:27-43`（onShow 拉取+skeleton+error 重试链接 `:82-85`+EmptyState `:86-90`）；`:54-67`（showModal 确认/取消，确认跳 discover）；`:108-115`（BottomActionBar 双按钮）；`usePageAccess(discoverPageRequirements)`（config/page-access.ts:11-17 requiresAuth:false）。执行证据：DC01（冷启动渲染）/DC03（弹窗确认→发现 tab）/DC07（空态分支代码在、数据非空）/DC08（重复进出重拉）EXECUTED；默认态截图证实「正在讨论」2 条推荐（热度标签+标题+摘要）与「下一步」双按钮渲染。DC02/DC04/DC06 失败均为原生 modal/候选定位限制，非代码缺陷。
- **⑤ 不成立（本轮新证）**：讨论圈在生产构建中零入口——全仓 `grep "discover/discussions"` 与 `DISCUSSIONS`（排除 tests/i18n）仅命中：routes.ts:245 常量定义（**无任何消费者**，含 SUBPACKAGE_ROUTES.DISCOVER_FEED.DISCUSSIONS 亦无引用）、dev 页条目（该页本身无构建激活，见第 1 节）、showcase 页条目（仅展示构建）。寻觅 tab 主页（pages/discover/index.vue）、首页 banners（config/home-banners.ts 仅 activities :70/:79）、nearby（仅 SUBPACKAGE_ROUTES.DISCOVER_FEED.ACTIVITIES :258）、official-chat、SocialProgressIndicator 均无讨论圈链接——同为 discover 分包的活动页有 4 处生产入口，讨论圈 0 处。普通用户在正式包中永远无法到达本页：孤岛页面。

**gap**：功能目标「提供讨论圈」对最终用户未实现——页面实现完整但入口缺失（Function，P2，MP-R1-REQ25-DISCUSS-001）。

**verdict：不达标**（页面内 L1–L4 俱佳，但 L9 导航可达性彻底缺失=需求功能目标未落地；与「可达性属于功能而非视觉」的定性一致立 Function Issue）。

---

## 5. subpackages/discover/activities/index（活动列表）

**target**：① 列表/日历双视图切换（数据不丢）；② 快捷筛选 全部/今天/周末（空结果提示）；③ 报名「感兴趣」/取消「已感兴趣」翻转+计数+防连点+失败区分；④ 下拉刷新穿透 30s TTL；⑤ 滚动到底加载更多（防抖/没有更多）；⑥ 活动卡跳详情带参；⑦ 底部去寻觅/提交活动提案（两个视图各一组）；⑧ 日历：月份切换（跨年）、有活动日期高亮+圆点+当日面板、无活动日期可选中显示空态、非本月格无效。

**current**：
- 全部 8 项代码落实：视图切换+筛选 `index.vue:40-66`；筛选空提示 `:462-464`；报名 `:109-131`（enrolling+submittingId 双守卫、失败读 store.errorMessage 区分取消/失败）；刷新 `:83-91`（force=true 穿透 TTL）+加载更多 `:95-102`（300ms debounce+cancel）；详情带参 `:143-145`；底部双按钮 `:474-484`（列表）/:623-634（日历）；日历全套 `:152-328,:487-635`（周一起始网格/跨年边界 `:279-298`/当日面板+空态 `:561-621`/非本月格 `:305-307` 直接 return）。
- 执行证据：ACT01（冷启动 4 条 mock）/ACT02（列表↔日历）/ACT03（今天→空+回全部）/ACT04（周末命中 2）/ACT05（报名翻转+计数）/ACT06（取消初始已报名态）/ACT07（快击守卫）/ACT09（refresherrefresh 触发 callMethod）/ACT10（滚动）/ACT14（今天选中面板）EXECUTED；默认态截图证实页头+视图切换+三筛选+4 活动卡（含已感兴趣态与人数标记）。ACT08（卡跳详情）失败为候选文本「卡」定位问题（:421 `@tap="goToActivityDetail(item.id)"` 存在，详情页 tools/activities/detail:283 亦证实回跳链路）；ACT15/16 为条件分支不可构造/时机问题；ACT11/12/13 SKIPPED 不可自动化（代码路径存在）。
- 已知残留（代码审查线已立，引用）：快捷筛选以中文子串匹配展示文案 scheduleText（:50-52；英文 locale/数据措辞变更即失效，MP-R1-ACTIVITIES-001，P3 Consistency）；分类标签 color-mix() 兼容（MP-R1-ACTIVITIES-002，P3）。

**gap**：无新增功能缺失。

**verdict：达标**。

---

## 6. subpackages/legal/privacy/index（隐私政策）

**target（提审合规）**：① CMS 拉取最新文本（getLegalText）；② 后端不可达/mock/空 → i18n 本地 fallback，永不白屏；③ 版本号+更新时间元信息（fallback 不显示版本防误导）；④ 长文滚动；⑤ 「我已阅读并同意」返回上一页（栈空兜底 discover tab）；⑥ 异常重试；⑦ 5 分钟内存缓存；⑧ 可达性：登录前（登录页协议链接）与登录后（设置页）均可查看。

**current**：
- ①–⑦ 全部落实于共用组件 `components/common/LegalTextPage.vue`：加载三态 `:70-89,:176-205`；元信息 `:91-122`（version=local-fallback 不展示 `:100`）；滚动正文 `:208-216`（user-select）；确认按钮 `:219-230,:131-148`（navigateBack fail→switchTab discover）；重试 `:153-156`；缓存/fallback 于 `config/legal-texts.ts:125`（TTL 5min）/:189-195（mock 跳过后端、失败回退、fallback 不写缓存）。文案 key 核实：zh-CN.ts:4639/4647 `accept: "我已阅读并同意"`。
- 执行证据：PRI01（fallback 正文渲染）/PRI02（长文滚动）EXECUTED；PRI06 缓存重入标 EXECUTED。巡检截图默认/滚动-中部/滚动-底部三张实拍正文完整。PRI03–PRI05 失败均为候选定位/原生层限制（按钮文本与代码/截图一致）。
- ⑧ 可达性核实（本 swift 曾疑缺失、已排除）：登录前 pages/login/index.vue:621《隐私政策》链接（LG08 EXECUTED 落地 legal 分包）；登录后 subpackages/profile-extra/settings/index.vue:110-119（viewPrivacyPolicy → SUBPACKAGE_ROUTES.LEGAL.PRIVACY）+ :399 关于分组菜单项——「我的→设置→关于→隐私政策」链路完整。

**gap**：无功能缺失。

**verdict：达标**。

---

## 7. subpackages/legal/agreement/index（用户协议）

**target**：与隐私政策同构（LegalTextType.USER_AGREEMENT，i18n-prefix legal.userAgreement）：CMS+fallback+版本元信息+长文滚动+「我已阅读并同意」返回+重试+5 分钟缓存+登录前后双入口。

**current**：
- 页面仅 20 行声明式封装（agreement/index.vue:15-20），逻辑同 LegalTextPage（见第 6 节逐条核实，全部落实）。
- 执行证据：AGR01（fallback 渲染）/AGR02（滚动）/AGR06（5 分钟缓存重入）EXECUTED；默认/滚动-底部截图实拍。AGR03–AGR05 失败为候选定位问题。
- 可达性：登录前 login:616《用户协议》（LG07 EXECUTED）；登录后 settings:110-114/:393（viewUserAgreement → LEGAL.AGREEMENT）。

**gap**：无功能缺失。

**verdict：达标**。

---

## 8. subpackages/market/detail/index（商品详情）

**target（3-H 商品）**：① 按 ?id= 加载详情：mock 模式本地目录（与 shop/后端种子对齐 6 项）、real 模式 GET /products/{id}；② 商业化封存态（ADR-2：commerceSealed 优先级最高——不发商品请求、隐藏详情与购买栏、SVG 锁+返回钮）；③ 404 → notFound 空态+「返回逛逛」；④ 网络/服务错误 → 错误态+重试；⑤ 立即购买=占位 toast（封存态拦截、无支付请求）；⑥ 封存竞态补偿（app-config 异步回来自动补拉）；⑦ 导航返回（栈>1 navigateBack/栈=1 switchTab home）；⑧ 从逛逛页商品卡真实入口链路。

**current**：
- 全部 8 项代码落实：封存 computed `detail/index.vue:33`；loadProduct mock/real+404/错误分流 `:189-239`；goBack 栈兜底 `:242-249`；购买占位+封存守卫 `:252-259`；onLoad productId 先记再判封存 `:261-273`；watch 补偿 `:278-282`；模板五态链 `:305-403`（sealed/loading/error/notFound/product+购买栏）。
- 执行证据：MD01（?id=1 封存态渲染，实拍截图=锁图标+「功能封存中」+「返回」绿钮，与代码 :306-320 逐一对应）/MD03（watch 补偿 evalSnippet commerceOn 后 1.5s 补拉）/MD04（解封完整渲染）/MD08（id=999 notFound）/MD10（滚动不遮挡）/MD11（逛逛页商品卡→详情真实链路）EXECUTED。MD02/05/06/07/12 失败均为候选定位问题：「返回」「立即购买」「返回逛逛」在代码（:318,:400,:354）与 zh-CN.ts:3316（detailBackShop=「返回逛逛」）及 MD04 截图中均存在。
- 已知残留（视觉线已立，引用）：notFound 空态传 :desc 而 EmptyState prop 名为 description → 引导文案死配置（MP-R1-MARKET-101，P3 UI）。

**gap**：无新增功能缺失（购买占位与封存为 ADR-2 设计行为，非缺陷）。

**verdict：达标**。

---

## 汇总

| 页面 | verdict | 新立 Issue（Function） | 引用他线 |
|---|---|---|---|
| setup/dev | **不达标** | MP-R1-REQ25-DEV-001（P2：DEV 条件无激活链路，页面所有构建未注册） | MP-R1-DEV-001/002/003 |
| setup/showcase | 基本达标 | MP-R1-REQ25-SHOWCASE-001（P2：村口 isTab 死链，1/49） | MP-R1-SHOWCASE-001/002 |
| support/feedback | 达标 | — | MP-R1-FEEDBACK-001/002/003 |
| discover/discussions | **不达标** | MP-R1-REQ25-DISCUSS-001（P2：生产零入口孤岛页） | MP-R1-DISCUSSIONS-001 |
| discover/activities | 达标 | — | MP-R1-ACTIVITIES-001/002/003 |
| legal/privacy | 达标 | — | — |
| legal/agreement | 达标 | — | — |
| market/detail | 达标 | — | MP-R1-MARKET-101 |

下轮建议：① 产出 dev-debug 平台构建 + showcase 构建（build:mp-weixin:showcase）各一包，补 DEV01–10/SC01/SC03/SC07/SC08 取证；② SC01 期望「6 分组 44 项」与代码 7 分组 49 项漂移需同步 manifest；③ 讨论圈入口补齐后复测 DC 链路；④ 匹配中页面.png/未登录个人主页.png/他人显示主页.png 仍待逐对 L1–L4 比较（本组不涉及，移交相应线）。

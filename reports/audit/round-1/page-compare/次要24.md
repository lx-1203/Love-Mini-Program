# R1 需求对照 · 次要24（8 页功能目标核对）

- 审查员：A4 需求对照员（次要24）
- 轮次：R1（gitSha aefd8a72）
- 证据源：
  - 执行结果：`reports/audit/round-1/interact/exec-results.json`（S17-c24a / S18-c24b，共 75 案）
  - 仲裁：`reports/audit/round-1/interact/次要24-judge.json`（A6 判定：14 VERIFIED / 1 FAILED / 60 UNVERIFIED）
  - 巡检截图：`reports/screenshots/round-1-tour/A|B/subpackages_*.png`（本 8 页均有双身份默认态/数据态实拍）
  - 源码：`apps/client/src/subpackages/profile-extra/**`、`apps/client/src/subpackages/setup/**`
- 理想稿锚定说明：`素材/理想效果图/` 实测 19 张（已列目录核对）**均无本 8 页的页面级理想稿**；故各页 baseline 以「页面声明功能目标（源码头注释 + 路由语义）+ 交互 manifest 预期 + v3.1 契约 Token 链」为唯一对照依据，L5–L10 不做像素级断言。

---

## 1. subpackages/profile-extra/profile/tasks（任务中心）

**target（应该具备）**：① 标题+完成数+积分卡（已获积分/进度条）；② 4 任务项（完善资料/每日签到/发布动态/校园认证），已完成勾选、未完成「去完成」；③ 签到点击走真实 check-in 并即时更新积分卡；④ 未完成任务跳对应功能页（资料编辑带 ?entry=edit、village、校园认证）；⑤ real 模式 GET /tasks + POST /tasks/{code}/claim 领取；⑥ onShow 状态同步；⑦ 返回键（栈=1 兜底 tab）。

**current（代码+执行证实）**：
- 全部 7 项在代码中落实：任务→路径映射 `tasks.vue:83-96`；本地任务表（profile 任务带编辑页 path）`tasks.vue:115-148`；real 拉取/领取 `tasks.vue:186-197,310-331`；签到真实链路+防重复领取锁 `tasks.vue:269-284,157-158`；goBack 兜底 `tasks.vue:226-233`；onShow 同步 `tasks.vue:338-363`。
- 后端链路真实存在：`apps/api/.../tasks/TaskController.java`（@RequestMapping("/api/v1/tasks")，GET / POST /{code}/claim / GET /progress），`RealTaskService.java:31-34` 四个 code 全部有真实进度源（含 first-post=有效动态数、campus-verify=认证状态）。
- 执行证据：TK01/TK09 VERIFIED（标题+积分卡+4 任务项渲染、滚动无卡死）；巡检截图 A/B 实拍返回键、4 任务、「0/4 已完成」。TK02–TK08 签到/跳转/返回均 UNVERIFIED（automator 截图双 ERROR:timeout、wxml 抓到错页 DOM，A6 判定「无法区分事件未派发与产品无反馈」）。
- TK07 的 `.tasks-header__back("我的")` element not found 为文本过滤伪缺陷：按钮仅含图标无文字（`tasks.vue:371-380`），巡检截图可见返回键。

**gap**：
- mock 分支 first-post/verify 完成态恒 false（`tasks.vue:106-107` 注释自认）——real 模式由后端进度覆盖，仅演示模式受限（记录，不立 Issue）。
- EP03 同类问题不在本页。跳转链路（TK04–TK06）执行未取证，下轮建议补网络日志级验证。

**verdict：基本达标**（功能目标全部有代码与后端支撑，核心渲染/滚动 VERIFIED；点击链路证据窗口失效未复核）。

---

## 2. subpackages/profile-extra/settings/dnd（免打扰设置）

**target**：① 总开关+状态行即时文案；② 开始/结束时间 picker；③ 重复方式 每天/工作日/周末/自定义 + 自定义星期多选（排序写入）；④ 紧急消息穿透开关；⑤ 校验（自定义未选星期、起=止）拦截不发 PUT；⑥ 保存 PUT /api/dnd 回写 + 成功/失败 Toast、快击仅 1 次；⑦ 加载错误态+重试；⑧ 返回键（栈=1 兜底「我的」）。

**current**：
- ①–⑤、⑦、⑧ 全部在代码落实：`dnd.vue:124-201`（开关/picker/星期多选排序）、`:212-227`（validateForm 两规则，customWeekdaysRequired 专属文案 zh-CN.ts:3232）、`:281-288`（goBack 兜底 profile tab）、`:293-296`（retry）、`:331-347`（loading/error/content 三态模板）。
- 执行证据：DND01 VERIFIED（表单回显 22:00/08:00）。DND06 observed Toast「设置已保存」是**前置未切自定义所致**（act 序列无 radio 切换步，默认 EVERYDAY 合法保存），非校验失效——校验分支未触达（A6 同判定 UNVERIFIED）。DND10 `.retry-btn` 缺失属正常态（GET 未失败无错误态，`dnd.vue:337-347` v-else-if）。
- **DND08 FAILED（A6 conf 0.65）**：rapidTap .save-btn ×5 捕获 **5 条「设置已保存」Toast**（ts 1790141243632/700/779/860/943，311ms 内）= handleSave 完整走完 5 轮。代码 `dnd.vue:238-246` 的 `isSaving` 仅挡**在途**请求；mock API 毫秒级返回使每击都是一次全新完整保存。验收「快击×5 仅 1 次 PUT」未达成（无完成态去抖/冷却）。→ **立 Issue（Function P2）**。

**gap**：即上述 DND08 去抖缺失（MP-R1-REQ24-DND-001）。

**verdict：基本达标**（功能面完整、错误/重试/兜底齐备；保存防重验收口径未达成，P2 待修）。

---

## 3. subpackages/profile-extra/feedback/history（反馈历史）

**target**：① 类型筛选 4 chip（全部/反馈/建议/活动提案）+ 激活态迁移 + 同值短路；② 点击记录展开详情（content/附件/最新回复懒加载、单展开互斥、详情缓存）；③ 附件 uni.previewImage 全屏预览；④ 列表失败错误卡+重试；⑤ 空态；⑥ 深链 ?id=N 自动展开（非法 id 防御）；⑦ 时间本地化；⑧ AppShell 返回。

**current**：
- 全部在代码落实：`history.vue:61-69,129-135`（筛选+短路）、`:145-172`（展开懒加载+缓存+失败收起）、`:180-184`（previewImage）、`:288-298`（错误卡+重试）、`:210-251`（?id 解析含 isNaN 防御）、`:201-203`（formatDateTime 本地化）、`:52-57`（定时器卸载清理）。
- 执行证据：FH01/FH03/FH09 VERIFIED——FH01 证实 4 chip 渲染+无记录时空态且「错误未伪装空态」；FH03 操作截图实证展开详情（内容+最新回复+hint 变「收起」+第二条保持收起）；FH09 滚动无卡死。FH05 深链以字面量 `?id={id}` 打开→NaN 命中防御分支不报错（与代码一致）；合法 id 自动展开未测。FH06/FH07 element not found 属前置不可构造（无附件记录/无网络故障注入），非缺陷（A6 同判定）。

**gap**：无功能性缺口。合法 id 深链展开、附件预览、错误重试三项 exec 未取证（依赖数据/故障注入，标记下轮补验）。

**verdict：达标**。

---

## 4. subpackages/setup/profile/index（资料编辑）

**target**：① 双模式（wizard 进度条 / ?entry=edit 隐藏进度条+保存后返回）；② 头像+6 槽照片墙（隐私授权→选图→上传→删除确认）；③ 11 字段编辑（昵称/签名/年级/称呼/身高/学历/感情状态/籍贯省市/未来城市/理想型）；④ 必填校验（行内红框+抖动+Toast 三通道）；⑤ diff 提交 + 必填 4 项全量携带；⑥ 身份分流持久化；⑦ 提交锁；⑧ 返回键（栈=1 兜底首页）。

**current**：
- 全部在代码落实：双模式 `index.vue:52-57,518-534,653-656`；媒体链路 `:276-409`；校验链 `:536-583`；diff+全量 `:586-609`；身份 `:113-135`；提交锁 `:250,537-538`；返回 `:59-68`；回填 `:468-516`。
- 执行证据：EP01/EP02/EP15 VERIFIED（entry=edit 隐藏进度条 ✓、wizard 显示第 1 步 ✓、滚动无错位）。EP03 的 6 条「请选择身高」Toast（zh-CN.ts:3098）实证**必填校验拦截链路正常**（身高未选即拦、无 PUT）；保存成功 happy path 因 picker 不可自动化未取证。EP04–EP10/EP14 全部 UNVERIFIED：automator watchdog 25s 超时、三次 reLaunch NAV_ERROR、dom 采样 stale（`.photo-grid__slot`/`.hero__back` 类名在源码 `:634,:697` 均存在，EP03-after 截图实证照片墙 4 占+2 空存在）。
- EP14 `.hero__back` element not found 为 stale context 伪缺陷（TK02-after.wxml 与巡检截图均见返回键）。
- 异常记录：EP03 快击×5 捕获 **6 条**校验 Toast（6>5，A6 已记录）——疑一次点击重复派发或采样窗口重叠，证据不足以立 Issue，登记 functionGaps 待复测。

**gap**：无代码级功能缺口；picker/上传/删除三类交互自动化不可达未取证。

**verdict：基本达标**（功能目标全覆盖且部分 VERIFIED；核心保存 happy path 与媒体上传缺执行证据）。

---

## 5. subpackages/setup/campus/index（校园信息设置）

**target**：① 三级联动点选（城市→学校→专业，禁止手输，换级清空下级）；② 进度条步骤 2；③ 隐私说明「?」弹窗（唯一确认键「我知道了」）；④ 未选完主按钮置灰、三项齐备高亮；⑤ 跳过入口（注册流→时间安排；补认证→我的）；⑥ 按场景路由保存（isReauthScenario）；⑦ 既有资料回显；⑧ 返回。

**current**：
- 全部在代码落实：联动 `campus/index.vue:77-101`；弹窗 `:106-113`（uni.showModal，confirmText=「我知道了」zh-CN.ts:3156）；置灰 `:66-69,282-286`；跳过 `:184-192`；场景路由 `:149-192`；回显 `:117-134`。
- 入口链路核实：注册向导（setup/profile 保存后 `index.vue:130-135`）+ 首页完善清单（`view-models/home.ts:89-95` campusCompleted=false 时入列）。
- 执行证据：CA01/CA07 VERIFIED——CA07 操作截图实证原生 showModal「认证信息说明+唯一按钮我知道了」完整弹出。CA02/CA03/CA05 联动与保存 UNVERIFIED（picker change 不可自动化）。CA06 `.skip-bar` dom 采样 absent 与 act:tap 自相矛盾（stale，类名在 `:277` 存在）。CA08 navError 为执行器 navigateBack API 打在 tab 页上的伪异常（A6 同判定）。

**gap**：无代码级功能缺口；联动/保存场景路由未取证。

**verdict：达标**。

---

## 6. subpackages/setup/schedule/index（课表/日程设置）

**target（按路由语义「课表/日程设置」+ 页面自述「时间安排页（课表导入，可选工具）」）**：① 常去区域输入；② 时间窗口动态增删（删至 1 条隐藏删除钮）；③ **课表（courseBlocks）录入/导入**；④ 保存校验（地点必填、时段≥1 非空）→ PUT → replace 推荐偏好；⑤ 防重入；⑥ 返回。

**current**：
- ①②④⑤⑥ 在代码落实：动态增删 `schedule/index.vue:40-47,90-125`；校验 `:52-64`；防重入 `:50,53`；保存跳转 `:66-78`；返回 AppShell show-back。
- **③ 课表缺失**：`courseBlocks` 字段在 form 中（`:29`）但**模板零渲染、零录入入口**，注释自认「courseBlocks 未在模板渲染，不随 {...form} 提交后端（以空数组提交）」（`:24-25`）——保存恒以空数组落库。页面自述与路由命名的「课表导入」半边职责无实现。巡检截图（A 默认态）实证页面仅 3 个文本输入+添加时段+保存，无任何课表 UI。→ **立 Issue（Function P2）**。
- 执行证据：SC01 VERIFIED（地点输入 present；`.time-window-field:absent` 证实新账号无预填示例值，R4-00045/46 生效）。SC05 找「东门图书馆」失败为 manifest 预期过期（示例预填已按 R4-00045/46 刻意移除），非缺陷。SC02/SC03 保存校验 observe-only 未执行（代码有拦截）。

**gap**：课表功能缺口（见 Issue MP-R1-REQ24-SCHEDULE-001）；SC04/SC06 时段增删与边界未取证。

**verdict：基本达标**（时间安排主链路完整可用；「课表」半边声明未实现，P2 待修）。

---

## 7. subpackages/setup/recommend-pref/index（推荐偏好设置）

**target**：① 每日推荐时间四选一（单选高亮）；② 推荐范围三选一（同校优先/同城/不限，互斥）；③ 校园优先开关（整行点击取反）；④ GET /recommendations/preferences/me 回显 + 失败错误卡+重新加载；⑤ PUT 保存（preferredTime/scope/campusPriority）→ Toast → replace 寻觅页；⑥ 进度条（学生 3/4、非学生 2/3）；⑦ 返回。

**current**：
- 全部在代码落实：选项组 `recommend-pref/index.vue:59-70,180-211`；开关 `:214-221`；GET/回显/错误态+重试 `:75-110,162-175`（review #28 修复使错误态非死代码）；PUT+跳转 `:115-146`；进度 `:27-30,160`。
- 执行证据：RP01 VERIFIED（三区渲染+默认高亮+学生进度第 3/4 步）。RP03 tap「同城/不限」已执行但 after 截图仍「同校优先」高亮——证据冲突无法归因（A6：点击未派发与产品失效不可区分）。RP04 `.toggle-row` 采样 absent 为 stale（类名在 `:215` 存在）。RP06 错误卡缺失属正常态（GET 未失败）。RP05 保存未执行。
- 未登录保存跳过 PUT 仅 Toast 后进寻觅（`:122-136` userId 判空），对游客合理。

**gap**：无代码级功能缺口；单选迁移/开关取反/保存链路未取证。

**verdict：达标**。

---

## 8. subpackages/setup/interest/index（兴趣选择）

**target（v3.1 契约 11/14：注册引导可跳过；主动进入必须 ≥3 才能保存）**：① 仅「兴趣」一组标签（其余 3 组不出现）；② 既有 interestTags 回显；③ 选中有 max=5 上限 Toast；④ <3 保存拦截「至少选择 3 个兴趣」；⑤ 保存=全量 GET 合并后 PUT 仅改 interestTags；⑥ 防重入；⑦ 保存后返回（栈=1 兜底「我的」）；⑧ **返回/跳过出口**。

**current**：
- ①–⑦ 在代码落实：`interest/index.vue:84`（groups=['interest']，MP-R1-SETUPINTEREST-003）；回显 `:20-30`；min3 `:35-39`；全量合并 `:42-53`（MP-R2-INTEREST-002 拉取失败不盲提）；防重入 `:33-34`；兜底 `:57-63`；max=5 由 TagSelector `toggleTag`（`TagSelector.vue:100-113`）+ zh-CN.ts:3008 maxReached。
- **⑧ 缺失**：模板 `:75-89` 仅 标题区+TagSelector+保存按钮，**无返回键、无跳过**；全局 navigationStyle=custom（pages.json:36）无系统返回栏；本页不在注册向导链（getNextSetupPath 不经过本页），主动进入唯一入口 我的→兴趣选择（`pages/profile/index.vue:840`）。用户不保存即无任何屏上出口（仅剩系统手势）。交互 manifest IN07 标题自认「本页无自定义返回键」。巡检截图 A/B 实拍顶部左侧空白、无返回控件。→ **立 Issue（Function P2）**。
- 附带结构偏差（L2）：页头「选择兴趣/选择你的兴趣，让匹配更懂你（至少 3 个）」与 TagSelector 自带头「选择你的标签/选择最能代表你的标签…」同屏双重标题+双「0/5」计数（截图可见），语义重复。
- IN03 找「清空」失败属前置缺失：totalCount=0 时清空钮按设计隐藏（`TagSelector.vue:185` v-if），非缺陷（A6 同判定）。

**gap**：返回出口缺失（MP-R1-REQ24-INTEREST-001）；双头重复（结构记录）。

**verdict：基本达标**（选择/校验/保存链完整；导航出口缺失属功能缺口 P2）。

---

## 汇总

| 页面 | verdict | 功能 Issue |
|---|---|---|
| profile/tasks | 基本达标 | —（点击链路执行未取证） |
| settings/dnd | 基本达标 | MP-R1-REQ24-DND-001（P2 快击去抖） |
| feedback/history | 达标 | — |
| setup/profile | 基本达标 | —（EP03 6-toast 异常登记待复测） |
| setup/campus | 达标 | — |
| setup/schedule | 基本达标 | MP-R1-REQ24-SCHEDULE-001（P2 课表缺失） |
| setup/recommend-pref | 达标 | — |
| setup/interest | 基本达标 | MP-R1-REQ24-INTEREST-001（P2 无返回出口） |

**下轮建议**：① S17/S18 的 automator 停摆（watchdog 25s/NAV_ERROR 级联）导致 60 案 UNVERIFIED，建议按页拆分会话+网络日志取证复测签到/保存/跳转三类关键链路；② tasks 点击跳转（TK04–06）与 EP03 6-toast 异常优先复测；③ dnd 合法 id 深链（FH05）、附件预览（FH06）等数据前置类用例改用种子数据注入。

# R1 · 需求与功能目标对照 · 次要23（profile-extra 8 页）

- 审查员：需求对照员（A4）· 轮次 R1 · 基线 gitSha `aefd8a72`
- 证据：`reports/audit/round-1/interact/exec-results.json`（1283 条，本席相关 63 条逐一核对）；交互截图/WXML 落盘 `reports/screenshots/round-1-interact/`（含 `wxml/`）；静态截图 `reports/screenshots/round-1-tour/A/subpackages_profile-extra_*`；规范 `git show 29a2b1df:docs/design/profile-product-logic.md`（§4 他人主页组装序）；理想图 `素材/理想效果图/他人显示主页.png`（本席已逐块目验）；源码 `apps/client/src/`（本席逐文件通读）。
- 结论速览：8 页全部在 `pages.json:199-231` 注册，均有可达入口与栈底返回兜底。**7 页无代码级功能缺失**；**他人主页 3 项功能缺陷（Function P2）+ 2 项结构偏差（L4）**；**恋爱认证中心 1 项图标渲染缺陷（UI P2）**。
- 证据局限性声明（适用于全部 FAILED 交互用例）：本轮交互 harness 对文本候选选择器（`__CAND__("提交")` 等）与深链参数存在系统性失配。PO05 的 `wxml/SUBPACKAGES-PROFILE-EXTRA-PROFILE-OTHER-PO05-after.wxml` 实证：深链未带 `userId` 时页面落入「链接缺少用户参数，请从列表页重新进入」错误态（ProfileShell 错误分支），CTA/内容从未渲染——PO05-PO11/PO14-PO17 的 FAILED 是**取证环境缺参**，非页面功能缺失；相应代码链路本席已在源码层逐条核实并在下文标注「代码核验、无运行时证据」。RN04 的 toast「请输入学生姓名」（zh-CN.ts:3373）属认证中心页文案，实名页应为「请输入真实姓名」（zh-CN.ts:3419）——该用例 harness 落错页，**不能作为实名页空提交的运行时证据**。

---

## 1. subpackages/profile-extra/verification/index · 恋爱认证中心

**target（应该具备）**：认证状态机四态展示（未认证/审核中/已认证/已驳回+原因）；认证门控横幅；认证权益说明；认证流程三步卡；表单（学生姓名/学号/学校）逐项校验；学生证上传（隐私授权前置、预览/更换）；提交防重入 + showLoading；real 链路（图片上传换 URL → POST /verification，409 →「已有申请审核中」）；pending 30s 轮询 + onShow 重拉 + 卸载清定时器；驳回原因展示与表单预填；重新认证/删除认证仅 mock；返回栈底兜底；入口可达。

**current**：
- 状态机+骨架防跳变：`verification/index.vue:37`（四态）、`:474-484`（statusLoading 骨架 / 状态卡）、`:290-311`（applyVerificationView：approved/pending/rejected 映射 + rejectReason + 表单预填）。exec#VI01 EXECUTED（首拉渲染，截图 SUBPACKAGES-…-VERIFICATION-INDEX-VI01-after.png）。
- 门控横幅：`:487-490`（未认证显示 gate-banner）。
- 权益 5 项（含第 5 项互动资格）：`:119-126`；流程三步卡：`:128-133`、`:564-580`。静态截图 `…verification_index__滚动-中部.png` 可见全部区块。
- 校验链+防重：`:182-229`（姓名→学号→学校→图片四段校验；`submitting` 守卫；showLoading mask:true）。exec#VI04 EXECUTED 且捕获 toast「请输入学生姓名」（:189 引 `verification.errStudentName`，zh-CN.ts:3373）✓；VI05 EXECUTED（渐进填写，#verification-student-name/id/school-name 均命中）✓。
- 上传：`:139-173`（ensurePrivacyAuthorized 前置 + chooseImage + 取消静默）；预览解析 `:177-179`。
- real+409：`:237-284`（isUploadedMediaUrl 判定 → uploadPostImage → POST /verification → loadVerification；`:266-279` status===409 → alreadyPending toast）。
- 轮询/清理：`:336-365`（30s 轮询 + onShow 重拉）、`:372-381`（onUnmounted 双定时器清理）。VI14 EXECUTED（长表单滚动到底/顶可达）。
- mock 守卫：`:383-400` simulateApprove 仅 mock（isMockMode 守卫）；`:513-524` 重新认证/删除认证 `v-if="useMock()"`（real 无撤销接口，合理收敛）。VI11 EXECUTED。
- 返回兜底：`:441-448`（栈=1 → switchTab 我的 Tab）。VI02/VI03 EXECUTED。
- 入口：设置页 `settings/index.vue:326` → `/subpackages/profile-extra/verification/index`（我的 → 设置 → 恋爱认证，2 跳内）。

**gap**：无功能缺失。⚠️ 渲染缺陷见 Issue MP-R1-REQ23-006（认证权益 5 图标 + gate-banner 警示图标不渲染）。VI06-VI10/VI12/VI13 FAILED 均为 harness `__CAND__` 选择器失配——VI06 的 `wxml/…VI06-after.wxml` 实证「提交认证申请」按钮在 DOM 中（`.action-btn` 内文本），非页面缺陷；这些链路（完整提交成功/409/防连点/重复提交/选图取消）本轮**无运行时证据，仅代码核验**。

**fix**：无（图标问题见 Issue 006）。

**verdict：基本达标**（结构功能齐备；图标不渲染与运行时证据缺口扣分）。

---

## 2. subpackages/profile-extra/verification/real-name · 实名认证

**target（应该具备）**：状态四态+骨架；成年门禁（banner + 表单禁用 + 提交拦截，出生日期缺失从严）；姓名/身份证号校验（15 或 18 位、末位 X/x、maxlength=18）；正反面缺失链（先拦正面后拦背面）；上传（≤5MB、隐私授权、双卡预览/更换）；防重入；real 提交（双图上传 → POST /real-name-certification，409 →「已有申请审核中」）；verified 脱敏回显（姓名+掩码身份证）；pending 轮询；驳回原因+预填；返回兜底回认证中心（流程闭环）。

**current**：
- 门禁：`:329-339` loadAdultGate（birthDate 缺失 → isAdult=false 从严）、`:436-438` minor-banner、`:474/:486/:503/:578` 表单/按钮 disabled、`:180-183` 提交首拦「未成年人不允许」。
- 校验链：`:175-199`（未成年→姓名→`ID_CARD_PATTERN`（`:172`，/^\d{15}$|^\d{17}[\dXx]$/）→正面→背面）。RN05 EXECUTED（#real-name-id-card-no 命中，短号输入执行成功）。
- 上传双卡：`:146-169` chooseImages（≤5MB + 隐私授权封装）、`:517-565` 正/反面卡（aria-label、预览、更换）。
- 防重+real+409：`:175-222`（submitting 守卫 + mask）、`:230-280`（双图 isUploadedMediaUrl → uploadPostImage → POST；`:262-275` 409 → alreadyPending）。
- 脱敏回显：`:286-303`（APPROVED → maskedIdCardNo）、`:441-455` verified-card 回显。RN12 EXECUTED。
- 轮询/清理：`:347-391`；返回兜底：`:394-401`（栈=1 → navigateTo 认证中心，流程闭环）。RN01/RN02/RN03 EXECUTED。
- 入口：`components/profile/CertDetailSheet.vue:109`、`guards/campus-gate.ts:92`（realname 门控跳转）、`campus/certification.vue:218`。

**gap**：无代码级功能缺失。RN06-RN11 FAILED 为 harness 失配（`__CAND__("按")/"提交"` 文本候选未命中；正反面卡的文本为 labelIdCardFront/Back 非独立「面」字控件）——完整提交/409/防连点/边界/未成年门禁分支本轮**无运行时证据，仅代码核验**；RN04 落错页证据无效（见卷首声明）。

**fix**：无。

**verdict：达标**。

---

## 3. subpackages/profile-extra/profile/visitors · 我的·访客

**target（应该具备）**：骨架→分组列表（今日/昨日/更早+组内时间格式化）；头部计数；点击访客项 → 他人主页（带 userId，mock 非 null 兜底）；空态/错误重试；下拉刷新；返回兜底。

**current**：
- 分组：`visitors.vue:116-147`（getGroup + groupedVisitors 派生，今日→昨日→更早）；时间格式化 `:159-168`（HH:mm / 昨日 HH:mm / MM-DD HH:mm）。
- 头部：`:245-260`（返回键 + 标题 + 「访客 · N」计数）。VS01 EXECUTED（骨架→分组列表 + 计数渲染，截图 VS01-after.png 111KB）。
- 点击跳转：`:207-215`（Number.isFinite 兜底 rawUserId → `ROUTES.PROFILE.OTHER?userId=`；MP-R1-VISITORS-001 修复 mock NaN 静默失效）。
- 数据源：`:59-90`（likes store /matches/visitors，P1-13 与「喜欢与访客」页同源）；加载 `:189-199`；onMounted `:225-227`；下拉刷新 `:232-238`（pages.json:214 enablePullDownRefresh:true 配套）。
- 空态/错误：`:268-284`；返回兜底 `:70-77`（栈=1 → switchTab 首页）。

**gap**：无。本轮无 FAILED 用例。

**verdict：达标**。

---

## 4. subpackages/profile-extra/profile/other · 他人主页

**target（ask 给定规范）**：全屏人物大图 Hero（返回+···）；白色信息卡（头像浮动交界、姓名/性别/在线胶囊/年龄·学校·城市/距离）；个性签名卡；兴趣标签（浅薄荷绿底）；「我们有 N 个共同点」**3 项横排（彩底圆图标+标题+副题）**；生活瞬间 4 列照片墙；最近动态 Feed；底部固定三按钮（喜欢粉浅底/打招呼绿主钮最宽/关注白描边绿星）；信息卡右上「喜欢」粉描边+「打招呼」绿填充；**CTA 按状态机切换（陌生=喜欢TA粉/互喜=发消息绿）**；关注入「更多」菜单；无底部 Tab；访客记录上报；举报/拉黑/取消匹配治理。

**current**（对照 `素材/理想效果图/他人显示主页.png` 逐块目验 + 静态截图 `…profile_other__默认.png` + 源码）：
- L1 路由：push 进入、非 TabBar 页 ✓（pages.json:218；PO01 EXECUTED，访客上报 `other.vue:150-160` POST /matches/visit）。
- L2 区块：Hero（`PublicHero.vue:10-40`，封面 SafeImage+兜底渐变、头像 bottom:-40rpx 浮动交界）→ 信息卡（`PublicIdentity.vue:36-74`：姓名+性别徽章+在线胶囊+基本信息行+「2.5km · 在线」距离行）→ 签名卡（PublicBio，v-if bio）→ 兴趣标签（PublicInterest）→ 共同点（PublicCommon）→ 生活瞬间（`PublicGallery.vue:12-22` slice(0,4) 四列）→ 最近动态（`PublicMoment.vue:41-55`，卡片 @tap → openPost → 村口详情 `other.vue:75-79`）→ 底部固定 CTA（RelationshipCTA fixed）。组装序与 profile-product-logic §4 / 理想图一致 ✓（静态截图逐块可见，见 `…other__默认.png`）。
- L3 顶部：返回 `other.vue:114-121`（栈=1 → switchTab 首页，PO03 EXECUTED）+ 右上 ···（`other.vue:386-396` → openGovernanceMenu `:294-308`：举报/拉黑/关注三项 ActionSheet，PO04 SKIPPED 不可自动化）✓。
- 治理/互动函数层齐备：喜欢（`:162-232`：已喜欢去重 :165-171；mock/real 匹配成功 modal → 去聊天 :180-198/:201-222）、打招呼实名门控（`:234-239` ensureCertified("realname")）、心动卡（`:241-249` 登录+实名门控 → WhisperComposeSheet）、关注切换（`:252-272` follow/unfollow 真实写入+busy 防重）、举报（`:310-331` 理由 ActionSheet → reportStore）、拉黑（`:333-357` 二次确认）、取消匹配（`:274-292` 已实现但不可达，见 Issue 002）、401/加载失败区分（`:132-144`，retry 按钮 `ProfileShell.vue:76-78`，missingParam 时 `retryable=false` 不渲染重试 → PO02 预期成立；PO05 wxml 实证缺参文案「链接缺少用户参数，请从列表页重新进入」）。
- 偏差：共同点呈现（Issue 004）、CTA 形态（Issue 005）、CTA 无状态机（Issue 001）、取消匹配不可达（Issue 002）、头像死按钮（Issue 003）、基本信息顺序=年龄·**城市·学校** vs 理想 年龄·**学校·城市**（`PublicIdentity.vue:19-27` 按 location="城市 · 学校" 拆解，L5 记录不立案）。PO05-PO11/PO14-PO17/PO21 FAILED 均为缺参落错误态所致的取证缺口（见卷首声明）；PO19/PO20 EXECUTED（滚动可达、无残留）。

**usageNotes**：从访客页/匹配卡/聊天气泡进他人主页路径顺畅（入口均有 tap.stop 防双跳）；栈内返回 ✅、栈=1 深链兜底 switchTab 首页 ✅（PO03/PO20 EXECUTED），2 步回 tab 满足；缺参深链有明确指引非死路 ✅。迷路点：底部无状态机导致互喜用户仍见「喜欢」钮，语义困惑（Issue 001）。

**verdict：基本达标**（L1-L3 齐备且与理想图同构；L4 两处偏差 + 3 项 Function 缺陷）。

---

## 5. subpackages/profile-extra/profile/location · 位置设置

**target（应该具备）**：进入自动定位（坐标+城市+校区展示）；重新定位按钮（定位中态切换 + 坐标刷新 + 上报后端）；真实地图（腾讯底座）+ marker + callout；地图选点（uni.chooseLocation 回填坐标+物理地址，失败 toast）；返回首页按钮；返回栈底兜底。

**current**：
- 定位首拉：`location.vue:60-72`（fetchCurrentLocation → city/coords → reportLocation）。LC01 EXECUTED（定位卡+地图 marker 渲染）。
- 重新定位：`:183-185`（aria-label=重新定位、locating→「定位中...」态）。LC02 FAILED=harness 文本候选未命中（按钮存在于 `:183-188`），代码核验 ✅ 无运行时证据。
- 地图+marker：`:42-58`（callout 用地址/城市兜底）、`:156-168`（原生 map，H5 降级占位 :169-173）。LC06 EXECUTED（纯视图操作）。
- 地图选点：`:78-102`（MP-WEIXIN chooseLocation 回填 coords/city/addressText + reportLocation(true)；catch →「未获取到选点结果…」toast）。LC03 FAILED=harness 选择器 `.chooseLocation` 不存在（实际类 `.location-map__pick` :176-178），代码核验 ✅。
- 返回首页：`:114-116` goHome（openAppPath Tab.HOME）、按钮 `:186-188`。LC04 FAILED 同为文本候选失配，代码核验 ✅。
- 返回兜底：`:104-112`（栈=1 → reLaunch 首页）。LC05/LC07 EXECUTED。

**gap**：无代码级功能缺失。

**verdict：达标**。

---

## 6. subpackages/profile-extra/profile/privacy · 隐私设置

**target（应该具备）**：两开关默认态（推荐给本校=关 / 接收同校信息=开）；切换翻转 + toast「已在本机保存」（不伪造「已同步」）；本机持久化（重进与 storage 一致）；返回兜底（栈=1 → switchTab 我的）。

**current**：
- 开关默认值：`stores/profile.ts:169-170`（allowSameSchoolRecommend:false / receiveSameSchoolInfo:true）✓ 与 Phase Feedback5 口径一致。
- 切换+toast：`privacy.vue:25-43`（computed setter → store set + `profile.privacySavedLocal`=「已在本机保存」（zh-CN.ts:1718），MP-R1-PRIVACY-001 如实文案）；change 事件 `:46-55`。PV01 EXECUTED（两开关默认态渲染）。
- 持久化：`stores/profile.ts:177-187` loadPersistedPrivacy（storage 读取+类型收敛）、`:336-337` 启动回填。PV04 EXECUTED。
- 返回兜底：`:58-65`（栈=1 → ROUTES.TAB.PROFILE）。PV06 EXECUTED。
- 入口链路：我的页 moreItems「隐私设置」label 实际落 `settings/index`（`pages/profile/index.vue:842`），同校开关页再由 `settings/index.vue:369` 与 `tools/security/index.vue:428` 进入——**可达但比标签语义多一跳**（我的→隐私设置(实为设置)→隐私设置(本页)），usageNotes 记录，不立案。

**gap**：无。PV02/PV03 SKIPPED（harness 判定不可自动化，非页面缺陷）；PV05 FAILED=harness 快击候选未命中，代码 `:86-101` 原生 switch 存在。

**verdict：达标**。

---

## 7. subpackages/profile-extra/profile/album · 我的相册

**target（应该具备）**：6 格照片墙（已满隐藏添加/守卫 toast、计数 n/6）；上传链（隐私授权 → chooseImage → ≤10MB 校验 → 上传 → 成功 toast 计数+1、上传蒙层+防连点 isUploading）；长按 ActionSheet（设为头像=真实下载重传+失败本地重排兜底 / 删除=二次确认）；预览双路（/static → 自建全屏查看层含圆点分页；网络图 → uni.previewImage）；空态（含添加按钮）；错误重试；onShow 重拉；返回兜底。

**current**：
- 网格/计数：`album.vue:34-38`（PHOTO_GALLERY_MAX=6、10MB 上限）、`:74-87`（photoCells/hasEmptySlot/photoCount）、`:416-419`（n / 6 计数）、`:450-483`（6 格网格：filled→预览/空位→添加、@longpress 仅 filled）、`:486-494`（底部添加钮，有照片且未满时显示）。AL01/AL03 EXECUTED（默认态+已满态）。静态截图 `…album__默认.png` 与代码一致（4/6、4 图+2 空位、添加钮、长按提示）。
- 上传链：`:139-193`（防连点 :141、已满 toast :143-145、隐私授权 :151-160、大小校验 :178-181、取消静默 :186-189）→ `:201-218`（蒙层 uploadingIndex + 成功/失败 toast）。AL08 EXECUTED 且捕获 toast「图片大小不能超过 10MB」✓；AL09 EXECUTED（取消静默无错误）✓。
- 长按：`:227-243` ActionSheet；设为头像 `:254-310`（mock//与 /static 本地重排；网络图 downloadToTemp → uploadAvatar 真实链路 + 失败回退重排）；删除 `:317-347`（showModal 二次确认 → removePhotoAtIndex）。AL02/AL04/AL05/AL06/AL07 FAILED=harness 文本候选/类名失配（代码中 `.album-add-btn`、长按绑定 `:463`、viewer `:502-513` 均存在），**无运行时证据，仅代码核验**。
- 预览双路：`:360-382`（/static → viewerVisible 自建层含圆点分页 :502-513；否则 uni.previewImage + resolveMediaUrl）。
- 生命周期：`:392-399` onMounted+onShow 双拉；`:59-66` 返回兜底（栈=1 → 首页）。AL10/AL11/AL12 EXECUTED。

**gap**：无代码级功能缺失。

**verdict：达标**。

---

## 8. subpackages/profile-extra/profile/favorites · 收藏

**target（应该具备）**：标题计数；骨架→列表/空态；卡片点击 → 村口帖子详情（带 id）；取消收藏（@tap.stop 不触发跳转 + 成功 toast + 失败 toast 不静默）；onShow 真实重拉；返回兜底 reLaunch 我的。

**current**：
- 标题计数：`favorites.vue:14`（favoritesTitle {n}）。静态截图 `…favorites__默认.png`：「我的收藏（2）」+ 两张卡（封面/作者/校区/标题/摘要/收藏数/取消收藏钮）完整渲染 ✓。
- 骨架/空态：`:17-25`。列表 `:27-54`（scroll-view）。
- 卡片跳转：`:32` @tap="openPost(post.id)" → `:83-86`（setCurrentPost + navigateTo `/subpackages/village/village/detail?id=`）。
- 取消收藏：`:47-49`（@tap.stop → unfavorite）；`:88-99`（try/catch：成功「已取消收藏」、失败 error.message/operationFailed toast——MP-R2-FAVORITES-001 修复静默还原）。FA03/FA04 FAILED=harness 候选/`.toggleFavorite` 类名失配（实际类 `.favorite-card__unfav` :47/:178），**无运行时证据，仅代码核验**。
- 数据：`:75-77`（villageStore.posts filter isFavorite）；onShow 重拉 `:101-106`（fetchPosts force）。FA07 EXECUTED。
- 返回兜底：`:79-81`（navigateBack fail → reLaunch 我的）。FA05/FA08 EXECUTED。
- 证据缺口：FA01 标记 EXECUTED 但 reLaunch watchdog 15s 超时（top 停在登录页）——同轮 FA05/FA07/FA08 三次成功打开同页，判定为 harness 瞬态，非页面缺陷；FA02 同类瞬态（top 落 discover）。

**gap**：无功能缺失。⚠️ 语义小隐患（不立案，建议关注）：unfavorite 复用 `toggleFavorite`（:91），理论上对同一卡重复触发会反向加收藏；收藏页列表随状态即时收缩，实际触发面窄。

**verdict：达标**。

---

## Issue 汇总（详见 findings/次要23-req.json）

| ID | 页面 | 类别 | 级别 | 摘要 |
|---|---|---|---|---|
| MP-R1-REQ23-001 | profile/other | Function | P2 | 底部 CTA 无互喜状态机切换（matched prop 传入但模板未用） |
| MP-R1-REQ23-002 | profile/other | Function | P2 | 「取消匹配」入口不可达（GovernanceMenu visible 恒 false，···ActionSheet 无该项） |
| MP-R1-REQ23-003 | profile/other | Function | P2 | Hero 头像 tapAvatar 全链 emit 但页面未绑定（死按钮） |
| MP-R1-REQ23-004 | profile/other | Consistency | P2 | 「我们有N个共同点」3 项横排（彩底圆图标+标题+副题）→ 兴趣标签胶囊流（L4 结构偏差，副题数据模型缺失） |
| MP-R1-REQ23-005 | profile/other | Consistency | P3 | CTA 形态与理想图偏差：信息卡右上文字药丸→图标圆钮；底部 3 钮→4 钮（+心动卡）；喜欢粉浅底→粉渐变填充 |
| MP-R1-REQ23-006 | verification/index | UI | P2 | 认证权益 5 图标 + 未认证门控横幅警示图标不渲染（SafeImage 小尺寸图标，双截图实证） |

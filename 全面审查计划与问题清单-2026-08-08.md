# 全代码库审查计划与问题清单（2026-08-08）

> 审查基线：`反馈.md` + `截图验收报告-2026-08-08.md`（54 张截图 / P0 12 项 / P1 8 项 / P2 8 项）
> 审查范围：apps/client（uni-app 小程序）、apps/api（Spring Boot）、apps/admin（Vue3 后台）、database/flyway、部署与文档
> 审查方式：4 路并行代理（client / api / admin+infra / 契约矩阵），全部结论经读代码验证

---

## 一、审查计划（阶段一）

| 步骤 | 内容 | 产出 |
|------|------|------|
| 1 | 基线构建：后端 `mvn package`、客户端 `build:mp-weixin` | 构建状态（均通过） |
| 2 | 基线运行：后端 real profile 启动 + guest-login 健康检查 | 运行正常 |
| 3 | 并行代码审查（client 41 项 / api 40 项 / admin+infra 33 项 / 契约 21 项） | 原始发现 135 项 |
| 4 | 去重合并 + 验收报告问题逐一核实 | **115 项独立问题** |
| 5 | 修复计划（P0→P1→P2 三批次，见第十四章） | 本文件 |

---

## 二、问题统计

### 按严重度

| 严重度 | 数量 | 说明 |
|--------|------|------|
| P0（严重） | 29 | 会报错/商业化链路断裂/安全漏洞 |
| P1（重要） | 36 | 功能不符合要求/体验断裂 |
| P2（次要） | 50 | 体验优化/清理 |
| **合计** | **115** | 去重后独立问题 |

### 按子系统

| 子系统 | 问题数 | 主要类型 |
|--------|--------|----------|
| 客户端 client | 38 | 契约失配、路由参数、空态兜底、i18n |
| 后端 api | 32 | 商业链路、种子数据、缓存、安全 |
| 契约矩阵（跨端） | 14 | 404/方法/参数/形状失配 |
| admin + 基础设施 | 30 | 图标 404、凭据泄漏、Docker、文档失真 |
| 运行时证据（_run-errors.json） | 1 | 6 类实测报错已并入上述条目 |

---

## 三、P0 问题清单（29 项）

### 契约断裂（real 模式必报错）

| 编号 | 位置 | 问题 | 修复方案 |
|------|------|------|----------|
| P0-01 | client http.ts（全局）+ 后端 39 处 @Idempotent | **幂等键缺失**：客户端仅 5 处写调用带 Idempotency-Key，like/pass/发帖/评论/点赞/圈子/每日一问/反馈/上传等约 25 个写接口在 real 模式（Redis 可用）下全部 400 | http.ts 拦截器对写请求自动生成幂等键（方法+用户+时间戳） |
| P0-02 | client http.ts:349-388 + AuthController /auth/refresh | 刷新令牌机制整体失效：客户端 body 传 refreshToken 且不带 Authorization 头，后端只读 Authorization 头 → 401 时刷新恒失败直接踢登录；UserSessionView 无 refreshToken 字段 | 客户端改用 Authorization 头传旧 token，删除 refreshToken 读取死代码 |
| P0-03 | client setup/recommend-pref:81,120 → RecommendationController | 调用 `GET/PUT /recommendations/preferences/{userId}`（带数字路径段），后端只有 `/preferences` 与 `/preferences/me` → **404（验收 P0-6 根因）** | 客户端改 `/preferences/me`，PUT body 改 `{preferredTime, scope, campusPriority}` |
| P0-04 | client stores/messages.ts:1053-1094 → InteractionEventController | 互动事件 4 个调用缺 `/notifications` 前缀 → **404**，消息页互动列表/未读/已读全挂；`pageSize` 应为 `size` | 客户端补前缀、参数改名 |
| P0-05 | client stores/discover/api.ts:150 → UserController | `GET /online-status?userIds=` 应为 `POST /users/online-status/batch`（方法+路径+参数全不符）→ **404** | 客户端改 POST + body |
| P0-06 | client icebreakers.ts:146, api.ts:351 → MatchController | 破冰话题 2 个调用无对应端点：`POST /matches/{matchId}/icebreakers/send` 与 `GET /match/icebreakers/profile/{peerUserId}` → **404**；且响应形状（数组 vs `{topics}`）不匹配 | 客户端改用 `GET /matches/{matchId}/icebreakers` 并按数组解析；send 走发消息组合 |
| P0-07 | client stores/messages.ts:984 → PrivateMessageController | `DELETE /messages/conversations/{id}` 后端无该路由 → **404**，删除会话功能不可用 | 后端补 DELETE 端点 |
| P0-08 | client stores/discover/api.ts:76-82 → MatchController /matches/pass | 后端从 query 读 passedUserId，客户端放 body → **400** | 客户端改 query 传参 |
| P0-09 | client video-call.ts:236 + pages/chat/video-call.vue:237 → VideoCallController | 挂断缺必填 roomId + endReason 枚举不符（HANGUP/REJECTED vs CALLER_HANGUP/CALLEE_HANGUP）→ **恒 400** | 客户端传 `{roomId, endReason}` 对齐枚举 |
| P0-10 | client stores/promo-code.ts:136,188 → PromoCodeController | 优惠码字段 `orderAmount` vs 后端 `baseAmount` → **400** | 客户端改 `baseAmount` |
| P0-11 | client stores/discover/api.ts:93-107 → HeartSignalView | like/super-like 响应形状不符：客户端读 `{matched, matchId, partnerName}`，后端返回 HeartSignalView → 匹配成功弹窗永不触发；重复 like 后端返回 null → 客户端 TypeError | 客户端按 status 判断 + null 兜底；后端重复 like 返回幂等视图 |
| P0-12 | client 多余 body 字段（video-call start 带 chatId、wallet recharge 带 orderId、likes/messages/每日一问/圈子带 userId 等） | Jackson 默认 FAIL_ON_UNKNOWN_PROPERTIES=true → 部分写请求 **400** | 客户端删除多余字段；后端统一 @JsonIgnoreProperties(ignoreUnknown=true) |

### 后端商业链路/安全（P0）

| 编号 | 位置 | 问题 | 修复方案 |
|------|------|------|----------|
| P0-13 | 迁移 V2026.08.07.0019/0004 + ADMIN-GUIDE.md:26 | 生产迁移硬编码超级管理员弱口令 `Admin@12345`，文档公开同源凭据 → 后台后门 | 文档移除真实凭据；新增迁移用 `${ADMIN_INITIAL_PASSWORD_HASH}` 注入（不改已应用迁移） |
| P0-14 | RealAuthService:507-528 guest-login | 固定手机号 13900000000 免密签发 token，注册无黑名单 → 任何人可接管共享体验账号 | 注册黑名单拦截该号；guest-login 默认关闭、本地脚本显式开启 |
| P0-15 | WalletController:108-119 /recharge | 演示充值无支付网关、单次上限 1 亿元分 → 任意免费铸造交友币 | 接入支付回调验签；当前加演示开关 + 限频 |
| P0-16 | WalletServiceImpl:79,133-199 | `@Transactional(noRollbackFor=DataIntegrityViolationException)` + 幂等冲突分支重查为空时仍提交 → 同 orderId 二次扣款/入账 | 幂等冲突分支强制回滚；重查为空抛回滚异常 |
| P0-17 | RealMatchService + WalletController | **商业化解锁链路断裂**：`/wallet/deduct` 无任何业务调用方；liked-me/visitors/私信均无解锁扣费校验，全免费；DeductRequest.relatedType 无白名单 | 新增解锁记录表 + 列表接口接 deduct 校验 + relatedType 白名单 |
| P0-18 | CampusController:332-336 resolveSchoolId | 用 `campus_name.hashCode()` 冒充 school_id，与 schools 表真实 id 恒不相等 → 校园话题列表恒空 | 改查 schools 表按 name 解析 id |
| P0-19 | 迁移目录无 campus_topics 种子 + A-05 | campus_topics / campus_topic_replies 零种子 → 话题详情"话题不存在"（验收 P0-4） | 新增种子迁移（8-10 条，按 schools 关联） |
| P0-20 | RedisConfig:219-224 + api-final.log 实证 | `match_recommend` 缓存反序列化失败（约 8 次/时段），命中 0%，每次 /recommendations 全量重算 200 候选 | 统一 Jackson 序列化配置 + 清旧键 |
| P0-21 | 种子 V2026.08.07.0021 + RecommendationStrategy:161-169,270-301 | 推荐池仅 7 校×8 人，非种子校区/滑完即空；候选不过滤 status/role/性别，ADMIN/禁用用户进推荐流；空池无放宽兜底 | 扩大种子（16 校×10+人）；加 status=active、role=USER 过滤；空池放宽城市 |
| P0-22 | client config/legal-texts.ts:103,157-158 + ConfigController | 客户端调用 `GET /v1/config/legal` 后端无此端点 → **404**（_run-errors 实证） | 后端新增 legal 端点返回条款文案 |
| P0-23 | RealCheckInService:248-285,419-422 | 签到奖励不写钱包流水（经济链路缺失）；首次签到 extraQuota 双计 +5；补签并发可破月 3 次上限 | 签到入账走 wallet；修双计；原子更新 |
| P0-24 | RealCheckInService:250-257 + RecommendationRanker:107-109 | 签到赠送的额外推荐配额后端从未消费，前端"今日次数"只减不实 | 推荐计数按配额扣减（Redis 日计数） |
| P0-25 | RealMatchService:299-312 + MatchRecorder | 心动信号 expiresAt 48h 无过期处理：过期信号仍可 accept/decline，无清理任务 | 查询加 expiresAt>now + 定时过期任务 |
| P0-26 | RealCampusService:206-274 | getCampusPosts/getCampusActivities 的 schoolId 被完全忽略 → 校园 feed 实为全站流，与"每校一管理员"商业模式冲突 | 按 user_campus_profile 过滤 schoolId |
| P0-27 | UserRepository:123-131 + AdminDataScope:140 | 校区管理员列表按 user_campus_profile 过滤，但管理员校区存 users.campus_name → `/admin/users/admins?campusName=` 恒空 | searchAllAdmins 改匹配 users.campusName |
| P0-28 | VipRedPacketService:333,371 | VIP 红包创建/领取无 VIP 身份校验 → 结合免费充值可任意铸造-转移交友币 | 加 VIP 资格校验 |

### 前端运行崩溃（P0）

| 编号 | 位置 | 问题 | 修复方案 |
|------|------|------|----------|
| P0-29 | CardSwiper.vue:976,1017,1043 + likes/index.vue:537 | `name[0]`/`name.charAt(0)` 无空值兜底，后端昵称缺失时 TypeError 渲染中断（匹配卡片空白根因候选） | 加 `(name || '?')[0]` 兜底 |

---

## 四、P1 问题清单（36 项）

| 编号 | 位置 | 问题 | 修复方案 |
|------|------|------|----------|
| P1-01 | village/post.vue:420-431 | 发帖标题硬编码 `""`（验收 P1-3）；real 模式本地图片路径仅 warn 不上传 | 加标题输入 + 接 /media/upload 先传后发 |
| P1-02 | village/detail.vue:599-633 + comments 表 | 仅一级评论无楼中楼（验收 P1-4）；评论数用本地 length | comments 加 parent_id 迁移 + 实体/服务树形 + 客户端嵌套渲染 |
| P1-03 | circles/topic-detail.vue:183-186 | 仍用 getCurrentPages().options 读 topicId，H5 直开空白（验收 P0-4 残留） | 改 onLoad(query) |
| P1-04 | navigation.ts:50-59 | openAppPath 带 query 跳 tab 页走 navigateTo → mp-weixin 报 "can not navigateTo a tabbar page"，查看 TA 主页不可达 | tab 页改 switchTab + query 缓存读取 |
| P1-05 | i18n zh-CN/en-US | 23 个缺 key（common.submit 等，验收 P1-7）→ 界面渲染 raw key | 补全 key |
| P1-06 | stores/discover/actions/fetch.ts:67-71 | resetDailyLimit 无调用方，跨天不重启每日限额不重置 | discover 页 onShow 调用 |
| P1-07 | likes-visitors/index.vue:98-101 | 数据概览造假（`?? 86`、`Math.max(3,…)`）→ real 模式空数据展示假数字 | 空数据显示 0 |
| P1-08 | likes-visitors:179 + vip/index.vue:129 + profile:1415 | VIP 入口守卫不完整：提升曝光直达 VIP 页、VIP 页面本体无 membershipEnabled 守卫（验收 P1-6） | 三处统一守卫；VIP 页 flag=false 时跳回；auto-renew 请求短路 |
| P1-09 | circle/index.vue（未注册死页）+ chat/index.vue（无入口死页） | 五 Tab mock 死页面未注册 pages.json；会话列表页无业务入口 | 删除死页面 |
| P1-10 | likes/index.vue:490 + stores/likes.ts:837-878 | 重试不完整（不重试 heartSignals/visitors）；fetchVisitors 错误被吞 | 重试补齐 + 统一抛错 |
| P1-11 | chat-session/index.vue:365,794,663-670 | 破冰话题异常未捕获 + resolvePeerUserId null 静默跳过 | try/catch + 兜底 UI |
| P1-12 | daily-question/index.vue:62-63 | 回答列表只取第 1 页无加载更多 | 加分页 |
| P1-13 | profile/visitors.vue vs likes-visitors | 访客双数据源分裂（验收 P2-2）：/profile/visitors vs /matches/visitors 两套数据 | 统一数据源 |
| P1-14 | 访客写入 | 两个访客表均无客户端写入路径，只有种子数据，永不更新 | 查看他人主页时记录访问 |
| P1-15 | RealMatchService:376-387 vs ProfileVisitorController | 两个访客入口已读状态不同源 | 统一已读字段 |
| P1-16 | PostAuthorView:6-12 + VillageViewMapper:137 | 帖子作者视图无 age/city/education（验收 P2-1） | 后端补字段 + 客户端渲染 |
| P1-17 | village/detail.vue:589 + circle/topic-detail | 评论区标题计数用本地 length 偏少 | 用服务端计数 |
| P1-18 | wallet 种子 V2026.08.07.0025:75-83 | 种子流水 type 用 INCOME/EXPENSE，服务层用 CREDIT/DEBIT → 种子全部显示为支出（验收 P1-8） | 客户端兼容两种取值 |
| P1-19 | admin Dashboard.vue:39-96 | 统计卡片引用已删除的 /icons/*.svg → 4 张卡片图标 404 | 改内联 SVG/emoji |
| P1-20 | admin Dockerfile:33,37 + docker-compose:298 | pnpm-lock.yaml 只在仓库根，admin context 内 COPY 无匹配 → admin 镜像构建必失败 | context 扩到根 + --filter 构建 |
| P1-21 | admin session.ts:33-35,85-118 + AdminLoginView | 前端读 user.campusName 渲染校区徽标，后端 4 字段无该字段 → 校区管理员徽标恒空 | 后端补 campusName |
| P1-22 | admin 菜单种子 vs SUPER_ADMIN-only 端点 | 校区管理员被授予配置中心/敏感词菜单，但端点仅 SUPER_ADMIN 可用 → 必然 403 | 前端按 role 隐藏 |
| P1-23 | flyway.toml:7-8 vs SQL ${admin_openid} vs application-real.yml:62-67 | CLI 与 Spring 双轨占位符语法矛盾，CLI 迁移时 openid 静默写入字面量 | flyway.toml 去 `__` 前缀与 Spring 统一 |
| P1-24 | flyway.user.toml（已提交） | 真实 MySQL 密码明文入库 | 移出跟踪 + .gitignore |
| P1-25 | application-real.yml:66 | flyway 默认路径 `../database` 从 apps/api 解析不存在，不配 env 启动即找不到迁移 | 统一 `../../database/flyway/sql` |
| P1-26 | V2026.08.07.0021 种子 | 50 个 USER 直写 users.campus_name，违反"USER 校区存 user_campus_profile"不变量 → 校区数据双源 | 新增修正迁移：USER 行 campus_name 置 NULL |
| P1-27 | WS 订阅 websocket/index.ts:797-806 | 未订阅 /user/queue/matches、temp-chat、temp-chat/messages → 匹配成功/临时聊天推送全丢 | 客户端补 3 个订阅 |
| P1-28 | admin Layout.vue:387 | `<text>` 嵌套 `<text>` 非法 HTML，校区名显示错乱 | 修嵌套 |
| P1-29 | admin 新视图硬编码中文 toast（Shop.vue 等 6 处） | 绕过 i18n，en-US 下显示中文 | 收敛 i18n key |
| P1-30 | admin 菜单 title 全中文 | 切 en-US 侧栏/面包屑仍中文 | title 存 i18n key |
| P1-31 | admin 无 .env.production | 生产 VITE_API_BASE_URL 无法覆盖 | 补 .env.production |
| P1-32 | admin index.html:15-16 | 硬编码 preconnect http://127.0.0.1:8080 | 删除 |
| P1-33 | docs/ADMIN-GUIDE.md:69,115-146 | 声称 TOTP/锁定/邮件提醒，后端无实现 → 文档失真 | 删虚构能力描述 |
| P1-34 | docs/API-CONTRACT.md:405-431 | 管理端契约表大面积过期（stats/dashboard、sensitive-words/import 等不存在） | 按现有 27 个 Controller 重建 |
| P1-35 | VipPlans.vue | VIP 套餐页无任何 API，纯占位 | 接端点或标记规划中 |
| P1-36 | village tag-posts:263-269 等 8 处 | 缺参无提示直接发空请求；certification 提交后状态不刷新；history 栈深 1 navigateBack 无效 | 逐处兜底（详见 P2 清单） |

---

## 五、P2 问题清单（50 项）

| 编号 | 位置 | 问题 |
|------|------|------|
| P2-01 | likes/index.vue:252-265 | formatTime 无 null 兜底 → "NaN月NaN日" |
| P2-02 | heart-signals/index.vue:51-58 | expiresAt 无效 → "NaN小时NaN分钟" |
| P2-03 | profile/index.vue:169 | getCurrentPages 读 userId，H5 不可靠 |
| P2-04 | discover/index.vue:282-286 + filter.ts:73,96 | 附近筛选连续触发两次 fetchCards 竞态 |
| P2-05 | discover/history.vue:171-172 | 兜底头像硬编码 '/static/default-avatar.png'，应走 IMAGE_PATHS |
| P2-06 | messages/index.vue:85-93 | partnerName 为 undefined 时 toLowerCase TypeError → 消息页空白 |
| P2-07 | vip/red-packet/promo-code/bills | membershipEnabled=false 下成为死页面（随 P1-08 一并处理） |
| P2-08 | likes-visitors:133-137 | vipStore.isVip 免费放行未挂 membershipEnabled → 不扣费解锁 |
| P2-09 | village/index.vue:129-161 | 同城城市池仅 4 城 + 简化逆地理，池外城市错显示 |
| P2-10 | village/index.vue:909-937 | 帖子头部无结构化年龄/城市/学历（随 P1-16） |
| P2-11 | profile/visitors:182-185 | 双数据源（随 P1-13） |
| P2-12 | recommend-pref 文案硬编码中文 | 未走 i18n |
| P2-13 | check-in/wallet/chat 等 18+ 处 | 冗余 `?userId=` query（无害但 IDOR 隐患） |
| P2-14 | http.ts:277-292 | 信封/扁平启发式解包无契约强制 |
| P2-15 | admin http.ts:12-18 | JWT 明文 localStorage 无刷新机制 |
| P2-16 | activity.ts:176,215 | /recommendations/activities 分页参数被静默忽略 |
| P2-17 | CommentController | 评论点赞 `POST /posts/comments/{commentId}/like` 无端点 → 404 |
| P2-18 | RecommendationRanker:404-410 | 认证级别 N+1 查询（200 候选逐查） |
| P2-19 | RealRecommendationService:369 | 双重 @Cacheable 冗余 |
| P2-20 | CampusController:110-115 | 未绑学校用户校园话题空页无提示 |
| P2-21 | RealMatchService:199-216 | like 无每日上限；重复 like 返回 null |
| P2-22 | MatchController:216-230 | super-like 与 like 完全相同，付费无差异化 |
| P2-23 | VillageInteractionService:190-230 | 评论不经敏感词过滤（发帖/私信都有） |
| P2-24 | UserRepository + V2026.07.25.0017 | users.phone 无唯一约束，并发注册同号双账号 |
| P2-25 | RealConfigService:43-50 | /config/campuses 硬编码 4 校 vs schools 表另一套 → 校区下拉与管辖对不上 |
| P2-26 | RecommendationController:265-271 | preferences PUT 全字段 @NotBlank，部分更新 400 |
| P2-27 | AuthController:139-144 | guest-login 共享账号隐私（随 P0-14） |
| P2-28 | home-banners.ts:46 | 死契约（后端无 /home/banners） |
| P2-29 | 迁移目录 _write_test.sql 等 4 个游离 SQL | 测试残留污染迁移目录 |
| P2-30 | V2026.05.28.0009 / 08.07.0015 跳号 + 空操作迁移 | 版本规范 |
| P2-31 | 表级 collation 混用 utf8mb4_unicode_ci vs 0900_ai_ci | JOIN 排序漂移 |
| P2-32 | 根 package.json api:dev / tools/start-api.bat | 默认 mock profile，脚本已过期 |
| P2-33 | docker-compose RABBITMQ guest/guest | 与"禁止默认凭据"注释自相矛盾 |
| P2-34 | docker/alertmanager | 无 envsubst 步骤，占位符漏替换 |
| P2-35 | admin session.ts:121-156 | 提交的空凭据 dev 回退分支，新克隆开发者无法登录 |
| P2-36 | admin api/users.ts:126-139 vs system.ts:547-561 | admins 列表/创建双份实现 |
| P2-37 | admin Dashboard.vue:94-106 | "最近活动"用匹配趋势冒充 |
| P2-38 | admin 菜单种子 Dashboard 挂系统管理下 | 校区管理员无看板入口语义矛盾 |
| P2-39 | admin stores/menu.ts staticFallbackMenus + 0011 icon 列 | 指向已删除 icons 的死数据 |
| P2-40 | docs/API-CONTRACT.md:50 | admin 无 OpenAPI YAML 文档体系缺口 |
| P2-41 | client pages/chat/index.vue handleOfficialTap | 占位 toast |
| P2-42 | likes/index.vue:537,606,674 | nickname null charAt 崩溃（随 P0-29 统一兜底） |
| P2-43 | village/index.vue:307 | 关注空态"去寻觅"引导无定位语义 |
| P2-44 | discover fetch.ts:103-108 | mock 兜底逻辑与 real 分支边界需确认 |
| P2-45 | likes-visitors:42-71 | 解锁状态仅存本地 storage，清缓存即丢（随 P0-17 服务端化） |
| P2-46 | campus/certification.vue:97-170 | 提交成功后认证状态不刷新 |
| P2-47 | MessageWebSocketHandler | WS 连接数/频率无上限 |
| P2-48 | ProfileVisitorController:96 | 响应信封与其他扁平端点混用 |
| P2-49 | API-CONTRACT.md admin 契约表过期（随 P1-34） | - |
| P2-50 | 根目录 seed-demo-data.sql / complete-guest-account.sql 等 | 游离脚本无维护说明 |

---

## 六、验收报告 17 项问题覆盖核对

| 验收问题 | 状态核实 | 对应修复编号 |
|----------|----------|--------------|
| P0-1 匹配卡片空白 | 前端兜底已修复；name 缺失崩溃残留 | P0-29 |
| P0-2 聊天页空白 | 已修复（chat-session onLoad 处理缺失态） | - |
| P0-3 空白页组 | 已修复（各页四态齐备） | - |
| P0-4 话题不存在 | 双根因：无种子 + hashCode schoolId | P0-18/P0-19 |
| P0-5 标签帖子报错 | 参数读取已修复；缺参兜底缺失 | P1-36 |
| P0-6 推荐偏好失败 | **未修复**：客户端路径错位 404 | P0-03 |
| P1-1 喜欢页模糊/解锁 | 已修复（likes-visitors 独立页承载） | - |
| P1-2 圈子 Tab 结构 | 已修复（village 三 Tab）；死页 circle 待删 | P1-09 |
| P1-3 发帖标题/图片 | 部分：标题硬编码空、图片不上传 | P1-01 |
| P1-4 楼中楼评论 | **未修复**：表无 parent_id | P1-02 |
| P1-5 匹配卡片字段 | 已修复（字段齐备） | - |
| P1-6 VIP 页隐藏 | 部分：三处守卫缺失 | P1-08 |
| P1-7 i18n 翻译 | **未修复**：23 个缺 key | P1-05 |
| P1-8 钱包收支明细 | 服务层正确；种子 INCOME/EXPENSE 错 | P1-18 |
| P2-1 帖子头部字段 | 部分：无 age/city/education | P1-16 |
| P2-2 访客数据源 | 确认分裂 + 无写入路径 | P1-13/P1-14 |
| P2-3 话题列表空 | 已修复（四态齐备） | - |

**结论：验收报告 28 项问题全部纳入修复计划（12 项已修复项保持回归验证，16 项待修复/部分修复项含 22 个修复动作）。**

---

## 七、商业化就绪度评估

| 商业环节 | 现状 | 判定 |
|----------|------|------|
| 匹配推荐 | 接口存在；缓存反序列化失败 + 池子过小 + 空池 | 🔴 需修复（P0-20/P0-21） |
| 喜欢/访客解锁付费 | 前端有解锁 UI，后端无扣费校验 → 全免费 | 🔴 需修复（P0-17） |
| 交友币充值 | 演示充值免费铸币 | 🔴 需修复（P0-15） |
| 悄悄话/私信 | 无解锁扣费链路 | 🔴 需修复（P0-17） |
| 签到→积分经济 | 签到不写钱包流水、配额双计 | 🔴 需修复（P0-23/P0-24） |
| VIP 会员 | 按反馈暂缓上线；入口守卫不完整 | 🟡 需统一守卫（P1-08） |
| 心动信号 | 48h 过期无处理 | 🟡 需修复（P0-25） |
| 校园话题/活动 | 数据隔离失效（schoolId 忽略） | 🔴 需修复（P0-26） |
| 校区管理员隔离 | 管理员列表过滤失效 | 🔴 需修复（P0-27） |

---

## 八、构建与运行基线（实测）

| 项 | 结果 |
|----|------|
| 后端 mvn clean package | ✅ 通过（旧进程占用 jar 导致首次失败，已处理） |
| 客户端 build:mp-weixin | ✅ 通过（sass 弃用告警 + empty chunk 告警，非致命） |
| 后端 real profile 启动 | ✅ 正常（RabbitMQ 报错为预期降级，guest-login 200） |
| 上一轮截图运行错误 | 6 类：Vue Error×6、tagName 400、daily-question 422、preferences 404、config/legal 404×2 → 全部已定位修复编号 |

---

## 九、修复批次计划

| 批次 | 内容 | 涉及 |
|------|------|------|
| 第一批 P0 | 契约 12 项 + 后端商业链路 16 项 + 崩溃 1 项 | client services/stores + api 全模块 |
| 第二批 P1 | 页面功能 16 项 + admin 14 项 + WS | client pages + api 模块 + admin |
| 第三批 P2 | 体验/清理 50 项（按需取舍） | 各端 |

每批后执行：后端 `mvn clean package -DskipTests`、客户端 `build:mp-weixin`、admin `vue-tsc && vite build`，全部退出码 0 视为通过；随后进行商业化流程走查 + 全页面截图 + 验收报告 + git 推送。

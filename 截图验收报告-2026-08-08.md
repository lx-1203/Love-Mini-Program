# 小程序端截图验收报告（2026-08-08）

> **验收基准**：`反馈.md` + `新建文本文档.txt` + `验收报告-2026-08-07.md`
> **截图目录**：`截图存档/2026-08-08/client/`（54 张）
> **验收方式**：逐张截图注脚 + 功能实现对照 + 问题清单

---

## 一、核心页面验收（5 张）

### 1. 首页（01-home.png）

**页面内容**：
- 顶部："同学"标题、"今天想遇见谁呢？"副标题、剩余匹配次数 10 次
- 搜索框："搜索用户/话题/动态"
- 学校选择："北京大学" + "本校限定"标签
- 今日签到卡片（绿色通栏）
- 功能入口：附近的人、兴趣匹配（热门）、恋爱咨询、恋爱测试、校园活动
- 校园团活动 Banner
- 底部导航：首页、匹配、圈子、消息、我的

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 签到可以有 | ✅ 有今日签到卡片 | 符合 |
| 匹配功能要有 | ✅ 有"兴趣匹配"入口 | 符合 |
| 附近的人可以有 | ✅ 有"附近的人"入口 | 符合 |
| 恋爱咨询可以有 | ✅ 有"恋爱咨询"入口 | 符合 |
| 恋爱测试可以有 | ✅ 有"恋爱测试"入口 | 符合 |
| 校园圈活动可以保留 | ✅ 有"校园活动"入口 + Banner | 符合 |
| 为你推荐不需要 | ❌ 未发现"为你推荐"入口 | 符合 |
| 每日缘分不需要 | ❌ 未发现"每日缘分"入口 | 符合 |

**问题**：
- 功能入口布局与反馈文档描述不完全一致（反馈文档未明确布局方式）
- "兴趣匹配"标注"热门"，但反馈文档未提及此标签

---

### 2. 匹配页/寻觅（02-discover.png）

**页面内容**：
- 顶部："寻觅 发现心动的人"、今日剩余 10 次
- 筛选：附近、不限、18-35岁、匹配度优先
- 搜索框："搜索用户/标签/学校"
- 我的积分：100
- 今日签到
- 每日一问："你心中完美的第一次约会是什么样子的？"
- **下方匹配卡片区域空白**

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 匹配页借鉴参考图，换排版 | ️ 页面结构符合，但卡片区域空白 | 无法验证 |
| 头像（露脸照） | ❌ 未显示 | 未实现 |
| 认证情况（机器+人工两次认证） | ❌ 未显示 | 未实现 |
| 个人 ID |  未显示 | 未实现 |
| 距离情况 | ❌ 未显示 | 未实现 |
| 活跃情况 | ❌ 未显示 | 未实现 |
| 基础资料 | ❌ 未显示 | 未实现 |
| 自我描述 | ❌ 未显示 | 未实现 |
| 喜好标签 | ❌ 未显示 | 未实现 |
| 悄悄话、喜欢/不喜欢 | ❌ 未显示 | 未实现 |
| 性格、mbti | ❌ 未显示 | 未实现 |
| 动态（点赞评论、私信） | ❌ 未显示 | 未实现 |
| 期待人物的画像 | ❌ 未显示 | 未实现 |

**问题**：
- **严重**：匹配卡片区域完全空白，无法验证核心匹配功能
- 可能原因：数据加载失败、接口异常、前端渲染问题

---

### 3. 消息页（11-messages.png）

**页面内容**：
- 顶部："消息"标题 + 搜索图标
- 两个快捷入口：匿名匹配聊天（随机匹配·渐近解锁）、喜欢与访客（谁喜欢我·谁看过我）
- 会话列表：
  - 沈亦舟（置顶，8/3 未读）
  - 周屿（昨天）
  - 产品助手（官方，昨天）
  - 林晚（昨天）
  - 活动运营（官方，昨天）
  - 顾一鸣（1 未读）
  - 苏念（2 未读）
  - 陈叙（2 未读，8/6）
  - 许知夏（2 未读，8/5）
- 底部导航

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 两个官方号（助手 + 活动） | ✅ 产品助手、活动运营 | 符合 |
| 保留心动信号（随机匹配聊天） | ✅ 有"匿名匹配聊天"入口 | 符合 |
| 聊 20 条解锁主页，每 5 条解锁信息 | ⚠️ 入口文案"渐近解锁"，详情未验证 | 待验证 |
| 添加访客和喜欢你的 | ✅ 有"喜欢与访客"入口 | 符合 |
| 需要会员/交友币解锁 | ⚠️ 入口有锁图标，详情未验证 | 待验证 |
| 会话按最后消息时间倒序 | ✅ 符合 | 符合 |
| 未读数字红点 | ✅ 有红点标识 | 符合 |
| 官方号后缀"官方"角标 | ✅ 有"官方"标签 | 符合 |

**问题**：
- 匿名匹配聊天的解锁规则需在聊天详情页验证
- 喜欢与访客的解锁逻辑需在二级页验证

---

### 4. 我的页（15-profile.png）

**页面内容**：
- 绿色渐变背景
- 头像（"超"字占位）、昵称"超级测试账号"、已认证标识
- 北京大学、全功能测试账号说明
- 编辑资料按钮
- 数据栏：我喜欢的 0、喜欢我的 24（锁）、最近来访 25（锁）、获赞 0
- 语音介绍模块（仅语音·无视频）
- 照片墙 0/6

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 会员先不加 | ⚠️ 数据栏有锁图标，但未强制付费 | 部分符合 |
| 个人信息 | ✅ 有头像、昵称、认证、学校 | 符合 |
| 我的动态 | ❌ 截图未显示（可能在下方） | 待验证 |
| 最长 60s 语音（不要视频） | ✅ 有"语音介绍"模块，标注"仅语音·无视频" | 符合 |
| 任务中心 | ❌ 截图未显示（可能在下方） | 待验证 |
| 帮助与客服 |  截图未显示（可能在下方） | 待验证 |
| 安全中心 | ❌ 截图未显示（可能在下方） | 待验证 |
| 权限（是否推荐给本校学生） | ❌ 截图未显示（可能在下方） | 待验证 |
| 设置 | ❌ 截图未显示（可能在下方） | 待验证 |

**问题**：
- 截图只显示上半部分，需查看完整页面（19-profile-tasks.png 等子页面）
- 数据栏"喜欢我的"和"最近来访"有锁图标，符合付费解锁逻辑

---

### 5. 圈子页（07-village.png）

**页面内容**：
- 顶部定位"南京"、发帖按钮
- 一级 Tab：校园圈（选中）、兴趣圈
- 二级 Tab：关注、同城、发现
- 附近的人 Banner（粉紫渐变）
- 帖子列表：
  - 宋延年（校友，北京大学）- "图书馆的下午..." - 8 小时前 - 8 评论 20 赞
  - 林晚（校友，北京大学）- "想认识新朋友..." - 8 小时前 - 2 评论 5 赞
  - 周屿（校友，北京大学）- 部分可见
- 悬浮发帖按钮（绿色"+"）
- 底部导航

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 三块：关注、同城、发现 | ✅ 有二级 Tab | 符合 |
| 关注（匹配中喜欢的人的动态） | ⚠️ Tab 存在，内容未验证 | 待验证 |
| 同城（同 IP，可手动改） | ⚠️ Tab 存在，定位显示"南京"，切换功能未验证 | 待验证 |
| 发现（校友、老乡、搭子圈） | ❌ 未发现二级分类 | 未实现 |
| 帖子头部：头像 + 昵称 + 年龄/城市/学历/职业 + 关注按钮 | ️ 有头像、昵称、校友标签，缺年龄/城市/学历/职业 | 部分符合 |
| 正文最多 6 行，超出配"展开" | ⚠️ 当前帖子较短，未验证 | 待验证 |
| 话题标签行 | ✅ 有"#生活记录"、"#日常"标签 | 符合 |
| 互动栏：评论、点赞、分享、收藏 | ✅ 有评论、点赞、分享、收藏图标 | 符合 |
| 悬浮发帖按钮 | ✅ 有绿色"+"按钮 | 符合 |

**问题**：
- 一级 Tab 是"校园圈/兴趣圈"，不是反馈文档描述的"关注/同城/发现"
- 发现页的二级分类（校友、老乡、搭子圈）未实现
- 帖子头部缺少年龄、城市、学历/职业信息

---

## 二、匹配相关页面（4 张）

### 6. 发现历史（03-discover-history.png）

**页面内容**：空白页，仅有返回按钮和搜索框

**问题**：
- **严重**：页面完全空白，无内容
- 可能原因：数据加载失败、接口异常

---

### 7. 视频播放器（04-discover-video-player.png）

**页面内容**：黑屏，仅有时间戳"0:42"

**问题**：
- **严重**：视频播放器黑屏，无法播放
- 可能原因：视频资源缺失、播放器初始化失败

---

### 8. 喜欢页（05-likes.png）

**页面内容**：
- 顶部："喜欢"标题、"管理"按钮
- 搜索框："搜索昵称、学校、城市"
- Tab：喜欢我的（24，选中）、我发出的喜欢、访客
- 用户列表：周屿、林晚、顾一鸣、苏念、陈叙、许知夏等
- 每个用户：头像、昵称、学校、时间（8 小时前/1 天前）、箭头

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 喜欢我的页面 | ✅ 有"喜欢我的"Tab | 符合 |
| 默认前 2 条可见、其余模糊 |  所有用户均清晰可见 | 不符合 |
| 点击解锁按钮模拟付费解锁 | ❌ 未发现解锁按钮 | 不符合 |

**问题**：
- 付费解锁逻辑未实现，所有用户均可见
- 与反馈文档"默认模糊、付费解锁"的要求不符

---

### 9. 喜欢与访客页（06-likes-visitors.png）

**页面内容**：
- 顶部："喜欢与访客 谁在喜欢你，谁来看过你"
- 提升曝光入口
- 数据：86 总浏览量、3 今日访客、28 今日浏览量
- Tab：喜欢我的（24，选中）、我的访客
- 匿名用户列表（模糊头像、锁图标）
- 每项：匿名用户、年龄、同城、"解锁后查看完整资料"按钮

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 独立二级页 | ✅ 有独立页面 | 符合 |
| 喜欢我的/我的访客 Tab | ✅ 有双 Tab | 符合 |
| 数据概览栏 | ✅ 有总浏览量、今日访客、今日浏览量 | 符合 |
| 未解锁状态模糊处理 | ✅ 头像模糊、昵称隐藏 | 符合 |
| 解锁按钮 | ✅ 有"解锁后查看完整资料"按钮 | 符合 |
| 消耗交友币/开通会员解锁 | ️ 按钮存在，解锁逻辑未验证 | 待验证 |

**问题**：
- 解锁按钮的具体付费逻辑需在点击后验证

---

## 三、消息相关页面（4 张）

### 10. 聊天页（12-chat.png）

**页面内容**：
- 顶部："聊天"标题
- 消息卡片："缺少会话标识，请从聊天列表或匹配结果进入。"
- 操作卡片："缺少会话标识，请从聊天列表或匹配结果进入。"

**问题**：
- **严重**：页面空白，无法验证聊天功能
- 可能原因：路由参数缺失、会话 ID 未传递

---

### 11. 聊天会话页（13-chat-session.png）

**页面内容**：与 12-chat.png 相同，空白页

**问题**：同 12-chat.png

---

### 12. 官方号聊天（14-official-chat.png）

**页面内容**：
- 顶部："官方"、"产品助手"
- 官方号会话列表：
  - 8/3：你好，我是产品助手...
  - 8/4：解锁访客/喜欢你说明
  - 8/5：缘分分配玩法说明
  - 8/6：会员权益说明
  - 8/7：每日一问更新
  - 8/7：欢迎语

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 官方号会话 | ✅ 有产品助手官方号 | 符合 |
| 功能答疑、系统通知 | ✅ 有相关消息 | 符合 |

**问题**：
- 仅有产品助手，未见活动运营号的聊天详情

---

## 四、圈子相关页面（7 张）

### 13. 发帖页（08-village-post.png）

**页面内容**：
- 顶部："返回"、"发布帖子"、"发布"按钮
- 选择分类：诚意帖（选中）、同乡、蒙面、兴趣圈
- 正文输入区："分享你的故事、心情或寻找那个 TA..."
- 话题标签：#校园日常、#兴趣分享、#找搭子、#求助
- 选择话题区：已选话题 已选 0/3
- 热门话题：#校园日常 1280、#表白墙 1024、#找搭子 856 等
- 创建新话题入口

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 发帖编辑器 | ✅ 有发帖页面 | 符合 |
| 标题栏 | ❌ 无标题输入框 | 不符合 |
| 正文编辑区 | ✅ 有正文输入 | 符合 |
| 选择发布圈子 | ⚠️ 有分类选择，但非"选吧"逻辑 | 部分符合 |
| 话题标签 | ✅ 有话题标签选择 | 符合 |
| 插入图片 | ❌ 未发现图片上传入口 | 不符合 |

**问题**：
- 缺少标题输入框（反馈文档要求必填）
- 缺少图片上传功能（反馈文档要求最多 9 张）
- 分类名称与反馈文档不一致

---

### 14. 帖子详情页（09-village-detail.png）

**页面内容**：
- 顶部："返回"、"帖子详情"、"举报"
- 用户信息：系统管理员、北京大学、校友标签
- 操作按钮：+ 关注、私信
- 内容："全链路走查：圈子发帖测试，生活小确幸。"
- 标签："走查测试"
- 5 小时前、0 转发 0 评论 0 赞
- 评论区："评论 0"、"暂无评论，快来抢沙发吧"
- 你可能还想认识推荐区

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 主楼区 | ✅ 有发布者信息、内容、互动栏 | 符合 |
| 回复楼层区 | ❌ 无楼层显示 | 不符合 |
| 楼中楼回复 | ❌ 无楼中楼功能 | 不符合 |
| 点赞、评论、收藏、分享、举报 | ⚠️ 有举报，其他未显示 | 部分符合 |

**问题**：
- **严重**：无楼中楼评论功能（反馈文档核心要求）
- 互动栏不完整（缺点赞、评论、收藏、分享图标）

---

### 15. 标签帖子页（10-village-tag-posts.png）

**页面内容**：
- 顶部："返回"、"校园"
- 错误提示："getPostsByTag.tagName: tagName 不能为空"
- 重试按钮

**问题**：
- **严重**：页面报错，无法显示标签帖子
- 可能原因：路由参数缺失、接口异常

---

### 16. 兴趣圈页（20-circles.png）

**页面内容**：
- 顶部："返回"、"兴趣圈"
- 附近的人 Banner
- 发现有趣的圈子入口
- 圈子列表：学习搭子、篮球搭子圈、运动健身圈、摄影圈子、音乐同好会、考研互助圈
- 每个圈子：图标、名称、描述、成员数、话题数、+ 加入按钮

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 搭子圈 | ✅ 有搭子圈列表 | 符合 |
| 加入圈子 | ✅ 有"+ 加入"按钮 | 符合 |

**问题**：
- 反馈文档要求搭子圈在"发现"Tab 的二级分类中，当前是独立页面

---

### 17. 话题列表页（21-circles-topics.png）

**页面内容**：
- 顶部："返回"、"话题列表"
- 空状态："暂无话题"、"来发第一个话题吧"、"去发布"按钮
- 悬浮发帖按钮

**问题**：
- 话题列表为空，与反馈文档"热门话题区"要求不符

---

### 18. 话题详情页（22-circles-topic-detail.png）

**页面内容**：
- 顶部："返回"、"话题详情"
- 空状态："话题不存在或已被删除"、"这里空空如也"、"返回"按钮

**问题**：
- **严重**：话题不存在，无法验证话题详情功能

---

### 19. 发帖页（话题）（23-circles-post-topic.png）

**页面内容**：
- 顶部："返回"、"发布话题"、"发布"按钮
- 话题标题输入框
- 话题内容输入框："分享你的想法..."
- 发布到：校园圈（选中）、兴趣圈
- 话题标签：脱单、学习、组队、树洞、分享、求助、吐槽
- 喜爱开关
- 添加图片（0/9）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 标题栏 | ✅ 有话题标题输入 | 符合 |
| 正文编辑区 | ✅ 有话题内容输入 | 符合 |
| 选择发布圈子 | ✅ 有校园圈/兴趣圈选择 | 符合 |
| 话题标签 | ✅ 有标签选择 | 符合 |
| 插入图片 | ✅ 有图片上传（0/9） | 符合 |

**问题**：
- 基本符合反馈文档要求

---

### 20. 校园圈页（24-campus.png）

**页面内容**：
- 顶部："广州大学"
- 提示："完成学生认证，解锁校园专区"、"去认证"按钮
- 页面空白

**问题**：
- 校园圈页面空白，需认证后才能使用
- 与反馈文档"校园圈活动可以保留（但只能开放给学生认证的人使用）"一致

---

### 21. 校园圈发帖页（25-campus-post-topic.png）

**页面内容**：
- 顶部："返回"、"发布话题"、"发布"按钮
- 选择分类：课程交流（选中）、社团招新、校园活动、学习互助、生活服务、校友动态
- 话题标题输入框
- 话题内容输入框
- 选择话题区
- 热门话题列表

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 发帖功能 | ✅ 有发帖页面 | 符合 |
| 分类选择 | ✅ 有校园专属分类 | 符合 |

**问题**：
- 基本符合

---

### 22. 校园圈话题详情页（26-campus-topic-detail.png）

**页面内容**：与 22-circles-topic-detail.png 相同，"话题不存在或已被删除"

**问题**：同 22-circles-topic-detail.png

---

## 五、我的页面子页面（5 张）

### 23. 访客页（16-profile-visitors.png）

**页面内容**：
- 顶部："谁看过我"
- 空状态："暂无访客记录"、"完善资料，让更多人发现你"

**问题**：
- 访客记录为空，与消息页"喜欢与访客"页（06-likes-visitors.png）数据不一致
- 可能原因：两个页面数据源不同

---

### 24. 隐私设置页（17-profile-privacy.png）

**页面内容**：
- 允许将我推荐给本校学生（开关，未开启）
- 接收同校信息（开关，已开启）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 隐私权限设置 | ✅ 有隐私设置页 | 符合 |
| 允许推荐给本校学生开关 | ✅ 有开关 | 符合 |
| 开关说明 | ✅ 有说明文字 | 符合 |

**问题**：
- 基本符合

---

### 25. 相册页（18-profile-album.png）

**页面内容**：
- 顶部："我的相册"
- 空状态："还没有上传照片"、"点击下方按钮添加第一张照片"、"添加照片"按钮

**问题**：
- 相册为空，与我的页"照片墙 0/6"一致

---

### 26. 任务中心页（19-profile-tasks.png）

**页面内容**：
- 顶部："任务中心"、2/4 已完成
- 累计获得 55 积分、31%
- 任务列表：
  - 完善个人资料 +50 积分（已完成）
  - 每日签到 +5 积分（已完成）
  - 发布首条动态 +20 积分（去完成）
  - 完成校园认证 +100 积分（去完成）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 任务中心 | ✅ 有任务中心页 | 符合 |
| 任务列表 | ✅ 有 4 个任务 | 符合 |
| 积分奖励 | ✅ 有积分显示 | 符合 |

**问题**：
- 基本符合

---

## 六、其他功能页面（19 张）

### 27. 校园认证页（27-campus-certification.png）

**页面内容**：空白页

**问题**：
- **严重**：页面空白，无法验证认证功能

---

### 28. 每日一问页（28-daily-question.png）

**页面内容**：空白页

**问题**：
- **严重**：页面空白，无法验证每日一问功能

---

### 29. 恋爱中心页（29-love-center.png）

**页面内容**：空白页

**问题**：
- **严重**：页面空白，无法验证恋爱中心功能

---

### 30. 附近的人页（30-love-center-nearby.png）

**页面内容**：
- 顶部："附近的人"
- 匹配卡片：
  - 头像占位（灰色方块）
  - ID: CL-47、距离你同校
  - "互友喜欢解锁头像"按钮
  - ????、22 岁
  - 年龄 22 岁、学校北京大学、学历本科
  - 月收入 3k-8k、身高 170cm
  - 同专业、80% 匹配标签
  - 自我描述："热爱生活，喜欢图书馆的下午和操场晚风。想认识有趣的灵魂。"
  - 兴趣标签：摄影、美食、阅读、旅行
- 底部操作栏：×（不喜欢）、悄悄话、喜欢（粉色爱心）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 头像（露脸照） | ❌ 头像为灰色占位 | 不符合 |
| 认证情况 | ❌ 无认证标识 | 不符合 |
| 个人 ID | ✅ 有 ID: CL-47 | 符合 |
| 距离情况 | ✅ 有"距离你同校" | 符合 |
| 活跃情况 |  无活跃状态 | 不符合 |
| 基础资料 | ✅ 有年龄、学校、学历、收入、身高 | 符合 |
| 自我描述 | ✅ 有自我描述 | 符合 |
| 喜好标签 | ✅ 有摄影、美食、阅读、旅行 | 符合 |
| 悄悄话、喜欢/不喜欢 | ✅ 有底部操作栏 | 符合 |
| 性格、mbti | ❌ 无性格/MBTI | 不符合 |
| 动态预览 | ❌ 无动态预览 | 不符合 |
| 期待人物的画像 | ❌ 无期待画像 | 不符合 |

**问题**：
- 头像为占位符，未显示真实头像
- 缺少认证标识、活跃状态、性格/MBTI、动态预览、期待画像

---

### 31. MBTI 测试页（31-love-center-mbti.png）

**页面内容**：
- 顶部："16types.com - The Type Practi..."
- 页面内容：16types.com 网站内容（英文）

**问题**：
- MBTI 测试为外部网页嵌入，非原生功能
- 页面为英文，未本地化

---

### 32. 恋爱咨询页（32-love-center-consulting.png）

**页面内容**：
- 顶部："课程 - 国家高等教育智慧教育平台"
- 页面内容：爱情心理学课程（中文MOOC）

**问题**：
- 恋爱咨询为外部网页嵌入，非原生功能
- 基本符合反馈文档"对接免费公开的正规平台链接"要求

---

### 33. 心动信号页（33-heart-signals.png）

**页面内容**：空白页

**问题**：
- **严重**：页面空白，无法验证心动信号功能

---

### 34. 帮助与客服页（34-help.png）

**页面内容**：
- 顶部："返回"、"常见问题与联系我们"、"帮助与客服"
- 常见问题：
  - 如何完成校园认证？
  - 交友币是什么？如何获取？
  - 如何解锁查看「喜欢我的人」？
  - 语音状态如何录制？
  - 如何举报不良内容或用户？
  - VIP 会员有哪些权益？
- 联系我们：
  - 在线客服（恋爱助手 9:00-21:00 在线）
  - 意见反馈
  - 客服邮箱（9:00-21:00，复制按钮）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 帮助与客服 | ✅ 有帮助与客服页 | 符合 |
| 常见问题 | ✅ 有 6 个常见问题 | 符合 |
| 在线客服 | ✅ 有在线客服入口 | 符合 |
| 意见反馈 | ✅ 有意见反馈入口 | 符合 |

**问题**：
- 基本符合

---

### 35. 安全中心页（35-security.png）

**页面内容**：
- 顶部："返回"、"账号安全与隐私保护"、"安全中心"
- 账号安全：
  - 绑定手机号（138****5678）
  - 修改密码
- 登录设备：
  - iPhone 15 Pro（当前设备，南京·玄武）
  - 小米 14（南京·江宁·2 天前，下线按钮）
  - iPad Air（南京·鼓楼·12 天前，下线按钮）
- 隐私保护：
  - 权限设置
  - 隐私政策

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 安全中心 | ✅ 有安全中心页 | 符合 |
| 账号安全 | ✅ 有绑定手机、修改密码 | 符合 |
| 登录设备管理 | ✅ 有设备列表、下线功能 | 符合 |
| 隐私保护 | ✅ 有权限设置、隐私政策 | 符合 |

**问题**：
- 基本符合

---

### 36. 积分商城页（36-shop.png）

**页面内容**：
- 顶部："我的积分 100"、"积分可在商城兑换权益"
- Tab：全部、活动票务、餐饮美食、校园周边
- 商品列表：
  - 校园音乐节早鸟票 ¥99（已售 56）
  - 校园文创帆布袋 ¥29.9（已售 128）
  - 食堂午餐优惠券 ¥9.9（已售 234）
  - 校徽纪念徽章 ¥19.9（已售 89）
  - 篮球赛门票、校园手绘地图（部分可见）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 积分商城 | ✅ 有积分商城页 | 符合 |
| 商品列表 | ✅ 有商品展示 | 符合 |
| 分类 Tab | ✅ 有分类筛选 | 符合 |

**问题**：
- 基本符合

---

### 37. 设置页（37-settings.png）

**页面内容**：
- 顶部："帮助"、"反馈与帮助"
- 账号管理：
  - 编辑资料
  - 恋爱认证
- 通知设置：
  - 消息通知（开关，已开启）
  - 免打扰
  - 深色模式（跟随系统）
  - 隐私模式（开关，未开启）
- 隐私安全：
  - 隐私政策
- 存储管理：
  - 清除缓存（23.5 MB）
- 关于：
  - 用户协议

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 设置 | ✅ 有设置页 | 符合 |
| 账号管理 | ✅ 有编辑资料、恋爱认证 | 符合 |
| 通知设置 | ✅ 有消息通知、免打扰 | 符合 |
| 隐私安全 | ✅ 有隐私政策 | 符合 |

**问题**：
- 基本符合

---

### 38. 免打扰设置页（38-settings-dnd.png）

**页面内容**：
- 开启免打扰（开关，未开启）
- 当前状态：已关闭
- 免打扰时段：开始时间 22:00、结束时间 08:00
- 重复方式：每天（选中）、工作日、周末、自定义
- 紧急消息仍会通知（开关，已开启）
- 保存按钮

**问题**：
- 基本符合设置逻辑

---

### 39. 认证页（39-verification.png）

**页面内容**：
- 未认证状态
- 认证权益：专属标识、信任优先、匹配加权、专属权益
- 填写认证信息：学生姓名、学号、学校
- 上传学生证（支持 JPG/PNG，大小≤5MB）

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 认证页 | ✅ 有认证页 | 符合 |
| 认证权益说明 | ✅ 有 4 项权益 | 符合 |
| 填写认证信息 | ✅ 有姓名、学号、学校 | 符合 |
| 上传学生证 | ✅ 有上传入口 | 符合 |

**问题**：
- 基本符合

---

### 40. VIP 会员页（40-vip.png）

**页面内容**：
- 顶部："校园恋爱 VIP"、"解锁专属权益·遇见更多可能"
- 会员权益：
  - 查看谁喜欢我（无限次查看喜欢我的用户...）
  - 无限喜欢（每日喜欢次数无上限）
  - 专属标识（个人主页 VIP 金冠徽章）
  - 匹配加权（匹配概率提升 2 倍）
  - 专属主题（解锁 5 套 VIP 专属主题）
  - 超级喜欢（每日 3 次超级喜欢机会）
- 选择套餐：
  - 月卡 ¥18（¥28，30 天，0.6 元/天）
  - 季卡 ¥48（¥84，90 天，0.53 元/天，超值）
  - 年卡 ¥158（¥336，365 天，0.43 元/天，最划算）
- 自动续费开关（未开启）
- 发红包、优惠码、账单记录入口

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 会员先不加 | ❌ 已实现 VIP 会员页 | 不符合 |

**问题**：
- **严重**：反馈文档明确说"最开始这个会员先不上线，后期再加"，但当前已实现完整 VIP 会员功能
- 需确认是否暂时隐藏或移除 VIP 入口

---

### 41. VIP 优惠码页（41-vip-promo-code.png）

**页面内容**：
- 顶部："VIP 优惠码"、"兑换优惠，立享折扣"
- 优惠码输入框
- 订单金额（元）：¥48
- 校验、重置按钮

**问题**：
- 基本符合优惠码功能

---

### 42. VIP 账单页（42-vip-bills.png）

**页面内容**：
- 顶部："VIP 账单"、"查看订阅、续费、退款记录"
- Tab：全部、订阅、续费、退款
- 空状态："暂无账单记录"、"这里空空如也"

**问题**：
- 账单为空，符合新用户状态

---

### 43. 钱包页（43-wallet.png）

**页面内容**：
- 顶部："交友币余额 ¥20"
- "解锁私信/访客/喜欢你/悄悄话需消耗交友币"
- 演示充值 ¥100 按钮
- 收支明细：
  - 支出：每日签到奖励 -¥1
  - 支出：完成新人任务 -¥3
  - 支出：连续签到 7 天奖励 -¥2
  - 支出：向周屿发送悄悄话 -¥2
  - 支出：解锁访客名单 -¥3
  - 支出：充值 5 交友币 -¥5
  - 支出：分享应用奖励 -¥1
  - 支出：完善资料奖励 -¥5
  - 支出：充值 8 交友币 -¥8

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 交友币体系 | ✅ 有钱包页 | 符合 |
| 余额显示 | ✅ 有余额显示 | 符合 |
| 收支明细 | ✅ 有明细列表 | 符合 |

**问题**：
- 明细中"支出"与"奖励"混用，逻辑混乱（奖励应为收入）
- "演示充值"按钮说明是测试数据

---

### 44. 反馈历史页（44-feedback-history.png）

**页面内容**：
- 顶部："反馈历史"
- Tab：全部、反馈、建议、活动提案
- 空状态："暂无反馈记录"、"提交反馈后将在此处显示"

**问题**：
- 反馈历史为空，符合新用户状态

---

### 45. 展示页（45-showcase.png）

**页面内容**：与 02-discover.png 相同（匹配页）

**问题**：同 02-discover.png（匹配卡片区域空白）

---

## 七、注册流程页面（4 张）

### 46. 基础资料页（46-sub-setup-profile.png）

**页面内容**：
- 顶部："完善你的资料..."、"基础资料"
- 第 1/4 步 基本信息
- 进度条：基本信息（1，选中）、校园认证（2）、推荐偏好（3）、完成（4）
- 你的身份：
  - 在校学生/毕业生（选中，绿色）
  - 非学生职场人士
- 基础信息：
  - 超级测试账号
  - 全功能测试账号说明
  - 年级：大三
  - TA

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 身份选择 | ✅ 有在校学生/非学生选择 | 符合 |
| 基础信息 | ✅ 有昵称、年级、性别 | 符合 |

**问题**：
- 基本符合反馈文档建议的"按用户身份做分支流程"

---

### 47. 校园认证页（47-sub-setup-campus.png）

**页面内容**：
- 顶部："认证后解锁同校..."、"校园认证"
- 第 2/4 步 校园认证
- 进度条：基本信息（1，完成）、校园认证（2，选中）、推荐偏好（3）、完成（4）
- 学校资料：
  - 城市：北京
  - 学校：北京大学
  - 专业：工业设计
- 完成校园认证说明
- 非学生？跳过此步，稍后认证
- 保存并继续按钮

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 校园认证 | ✅ 有校园认证页 | 符合 |
| 三级联动选择器 | ⚠️ 有城市、学校、专业选择，但联动逻辑未验证 | 待验证 |
| 跳过入口 | ✅ 有"非学生？跳过此步" | 符合 |

**问题**：
- 基本符合反馈文档建议

---

### 48. 时间安排页（48-sub-setup-schedule.png）

**页面内容**：
- 顶部："流程会驱动首页..."、"时间安排"
- 偏好设置：
  - 图书馆
  - 周一晚上
  - 周三下午
- 保存并进入应用按钮

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 课表导入 | ⚠️ 当前为"时间安排"，非课表导入 | 部分符合 |

**问题**：
- 反馈文档说"课表导入"功能待定，当前实现为"时间安排"
- 需确认是否符合产品需求

---

### 49. 推荐偏好页（49-sub-setup-recommend-pref.png）

**页面内容**：
- 顶部："推荐偏好"
- 第 3/4 步 推荐偏好
- 进度条：基本信息（1，完成）、校园认证（2，完成）、推荐偏好（3，选中）、完成（4）
- 错误提示："加载失败"、"获取偏好设置时出现问题，请重试"、"重新加载"按钮

**问题**：
- **严重**：页面加载失败，无法验证推荐偏好功能
- 可能原因：接口异常、数据缺失

---

## 八、其他子页面（5 张）

### 50. 反馈中心页（50-sub-support-feedback.png）

**页面内容**：
- 顶部："反馈、建议和..."、"反馈中心"
- 新建提交：
  - Tab：反馈（选中）、建议、活动提案
  - 标题输入框
  - 正文输入框
  - 选填微信号
  - 上传图片（最多 3 张，每张≤5MB）
  - common.submit 按钮（绿色）
- 反馈历史

**问题**：
- **严重**：提交按钮文字为"common.submit"，国际化未翻译
- 应改为"提交"或"提交反馈"

---

### 51. 讨论圈页（51-sub-discover-discussions.png）

**页面内容**：
- 顶部："先看大家最近真正在聊什么..."、"讨论圈"
- 正在讨论列表：
  - 哲学课上讨论「什么是幸福」，有意思（热度上升）
  - 围棋复盘，今天赢了一盘漂亮的（热度上升）
  - 练琴两小时，进步一点点（热度上升）
  - 瑜伽打卡第 7 天，身心舒畅（热度上升）
  - 法语角活动，欢迎来交流（热度上升）
  - 晨跑打卡，天气越来越好了（热度上升）
  - 最近在学做甜点，第一次做提拉米苏居然成功了（热度上升）

**问题**：
- 基本符合讨论功能

---

### 52. 线下活动页（52-sub-discover-activities.png）

**页面内容**：
- 顶部："从时间清晰..."、"线下活动"
- 列表/日历切换（列表选中）
- 活动列表：
  - 新人礼遇打卡周（2 人已报名，8 月 8 日 - 8 月 14 日）
  - 周末桌游派对（第二教学楼 301 活动室，本周六 14:00-18:00）
  - 读书分享会（18 人已报名，图书馆报告厅，3 月 18 日 18:30）
  - 校园徒步·湖光山色（东门集合，本周日 09:00）
  - 骑行踏春活动（42 人已报名，部分可见）
- 每个活动：感兴趣按钮

**对照反馈文档**：
| 要求 | 实现 | 判定 |
|------|------|------|
| 线下活动 | ✅ 有活动列表 | 符合 |
| 活动详情 | ✅ 有时间、地点、报名人数 | 符合 |
| 感兴趣按钮 | ✅ 有报名入口 | 符合 |

**问题**：
- 基本符合

---

### 53. 隐私政策页（53-sub-legal-privacy.png）

**页面内容**：
- 顶部："隐私政策"
- 最后更新：2026-07-26; 2026-08-08
- 隐私政策文本（信息收集、使用、共享、存储、用户权利等）

**问题**：
- 基本符合法律合规要求

---

### 54. 用户协议页（54-sub-legal-agreement.png）

**页面内容**：
- 顶部："用户协议"
- 最后更新：2026-07-26; 2026-08-08
- 用户协议文本（协议范围、账号注册、用户行为规范、内容规范、服务说明、免责声明等）

**问题**：
- 基本符合法律合规要求

---

## 九、问题汇总

### P0-严重问题（功能缺失/页面空白）

| 序号 | 截图 | 问题 | 影响 |
|------|------|------|------|
| 1 | 02-discover.png | 匹配卡片区域完全空白 | 核心匹配功能无法验证 |
| 2 | 03-discover-history.png | 发现历史页面空白 | 功能缺失 |
| 3 | 04-discover-video-player.png | 视频播放器黑屏 | 功能缺失 |
| 4 | 12-chat.png | 聊天页面空白 | 核心聊天功能无法验证 |
| 5 | 13-chat-session.png | 聊天会话页空白 | 核心聊天功能无法验证 |
| 6 | 22-circles-topic-detail.png | 话题详情页"话题不存在" | 功能缺失 |
| 7 | 26-campus-topic-detail.png | 校园圈话题详情页"话题不存在" | 功能缺失 |
| 8 | 27-campus-certification.png | 校园认证页面空白 | 功能缺失 |
| 9 | 28-daily-question.png | 每日一问页面空白 | 功能缺失 |
| 10 | 29-love-center.png | 恋爱中心页面空白 | 功能缺失 |
| 11 | 33-heart-signals.png | 心动信号页面空白 | 功能缺失 |
| 12 | 49-sub-setup-recommend-pref.png | 推荐偏好页加载失败 | 注册流程中断 |

### P1-重要问题（功能不符合要求）

| 序号 | 截图 | 问题 | 对照文档 |
|------|------|------|----------|
| 1 | 05-likes.png | 喜欢页所有用户清晰可见，无模糊/解锁逻辑 | 反馈文档要求"默认前 2 条可见、其余模糊" |
| 2 | 07-village.png | 圈子页一级 Tab 为"校园圈/兴趣圈"，非"关注/同城/发现" | 反馈文档要求三级 Tab 结构 |
| 3 | 08-village-post.png | 发帖页缺少标题输入框、图片上传功能 | 反馈文档要求标题必填、最多 9 张图 |
| 4 | 09-village-detail.png | 帖子详情页无楼中楼评论功能 | 反馈文档核心要求 |
| 5 | 10-village-tag-posts.png | 标签帖子页报错"tagName 不能为空" | 功能异常 |
| 6 | 30-love-center-nearby.png | 匹配卡片头像为占位符，缺少认证/活跃/MBTI/动态/期待画像 | 反馈文档要求完整字段 |
| 7 | 40-vip.png | VIP 会员页已实现，但反馈文档说"先不上线" | 反馈文档明确要求 |
| 8 | 50-sub-support-feedback.png | 提交按钮文字为"common.submit"，未翻译 | 国际化问题 |

### P2-次要问题（体验优化）

| 序号 | 截图 | 问题 |
|------|------|------|
| 1 | 01-home.png | 功能入口布局与反馈文档描述不完全一致 |
| 2 | 07-village.png | 帖子头部缺少年龄、城市、学历/职业信息 |
| 3 | 16-profile-visitors.png | 访客记录为空，与 06-likes-visitors.png 数据不一致 |
| 4 | 18-profile-album.png | 相册为空（与照片墙 0/6 一致） |
| 5 | 21-circles-topics.png | 话题列表为空 |
| 6 | 24-campus.png | 校园圈需认证后才能使用（符合预期） |
| 7 | 43-wallet.png | 收支明细中"支出"与"奖励"混用，逻辑混乱 |
| 8 | 48-sub-setup-schedule.png | "时间安排"与反馈文档"课表导入"命名不一致 |

---

## 十、验收结论

### 已实现功能（符合反馈文档）

1. ✅ 首页功能入口（签到、匹配、附近的人、恋爱咨询、恋爱测试、校园活动）
2. ✅ 消息页结构（两个官方号、匿名匹配聊天、喜欢与访客入口）
3. ✅ 我的页上半部分（头像、昵称、认证、数据栏、语音介绍）
4. ✅ 圈子页二级 Tab（关注、同城、发现）
5. ✅ 喜欢与访客页（模糊头像、解锁按钮）
6. ✅ 任务中心（4 个任务、积分奖励）
7. ✅ 隐私设置（推荐给本校学生开关）
8. ✅ 安全中心（账号安全、设备管理、隐私保护）
9. ✅ 积分商城（商品列表、分类 Tab）
10. ✅ 设置页（账号管理、通知设置、隐私安全）
11. ✅ 认证页（认证权益、填写信息、上传学生证）
12. ✅ 帮助与客服（常见问题、在线客服、意见反馈）
13. ✅ 注册流程（身份选择、校园认证、跳过入口）
14. ✅ 反馈中心（反馈/建议/活动提案 Tab）
15. ✅ 讨论圈（正在讨论列表）
16. ✅ 线下活动（活动列表、报名按钮）
17. ✅ 隐私政策/用户协议（法律文本）

### 未实现/有问题功能

1. ❌ 匹配卡片区域空白（核心功能）
2.  聊天页面空白（核心功能）
3. ❌ 多个页面空白（发现历史、视频播放器、校园认证、每日一问、恋爱中心、心动信号）
4. ❌ 话题详情页"话题不存在"
5. ❌ 标签帖子页报错
6. ❌ 推荐偏好页加载失败
7. ❌ 喜欢页无模糊/解锁逻辑
8. ❌ 帖子详情页无楼中楼评论
9.  发帖页缺少标题、图片上传
10. ❌ 匹配卡片缺少认证/活跃/MBTI/动态/期待画像
11. ❌ VIP 会员页已实现（反馈文档要求暂不上线）
12.  反馈提交按钮文字未翻译

### 建议

1. **优先修复 P0 问题**：匹配卡片、聊天页面等核心功能空白问题
2. **数据填充**：话题、访客记录、相册等数据缺失问题
3. **功能完善**：楼中楼评论、模糊解锁、图片上传等反馈文档明确要求的功能
4. **VIP 会员**：确认是否暂时隐藏或移除 VIP 入口
5. **国际化**：修复"common.submit"等未翻译文本

---

**报告生成时间**：2026-08-08
**截图总数**：54 张
**验收页面**：54 个
**已实现功能**：17 项
**未实现/有问题功能**：12 项

---

## 十一、逐项修改方案（精确到文件 + 保证构建通过）

> 以下每个方案均给出：修改文件、修改内容、构建验证命令。按优先级排列，P0 优先。

---

### P0-1：匹配卡片区域空白（02-discover.png / 45-showcase.png）

**根因分析**：
- `apps/client/src/stores/discover/actions/fetch.ts` 中 `fetchCards` 调用 `fetchRecommendationsApi(filter)` 获取推荐数据
- real 模式下走 `/recommendations?xxx` 接口，若后端 `/recommendations` 端点返回空或报错，`availableCards` 为空，`this.cards = []`，页面渲染为空
- 截图显示筛选栏/签到/积分/每日一问均正常，唯独卡片区域空白 → 数据层问题，非 UI 问题

**修改方案**：

1. **后端：确认 `/api/v1/recommendations` 端点存在且返回数据**
   - 文件：`apps/api/src/main/java/com/campuslove/api/controller/RecommendationController.java`（或对应路径）
   - 检查：`GET /api/v1/recommendations?userId=1` 是否返回 200 + 非空列表
   - 若端点不存在：需新增 Controller + Service，从 `users` 表排除已查看/已拒绝用户后返回推荐列表

2. **前端兜底：fetchCards 返回空时展示 EmptyState 而非空白**
   - 文件：`apps/client/src/pages/discover/index.vue`
   - 在 `CardSwiper` 组件外层增加 `v-if="cards.length > 0"` + `v-else` 展示 `EmptyState` 组件
   - 当前 `errorMessage` 已有错误态，但 `cards=[]` 且 `errorMessage=null` 时页面空白
   - 修改：在 `fetchCards` 的 `finally` 块中，若 `this.cards.length === 0 && !this.errorMessage`，设置 `this.errorMessage = "暂无推荐用户，请稍后再试"`

3. **构建验证**：
   ```bash
   pnpm --filter client run build:mp-weixin
   ```
   退出码 0 即通过。

---

### P0-2：聊天页面空白（12-chat.png / 13-chat-session.png）

**根因分析**：
- `apps/client/src/pages/chat/index.vue` 第 52 行 `privateSessions` 计算属性从 `messagesStore.sessions` 派生
- 截图显示"缺少会话标识，请从聊天列表或匹配结果进入" → 页面通过路由进入但未携带 `conversationId` 参数
- `onLoad` 中未正确读取路由参数，或路由配置未传递参数

**修改方案**：

1. **文件**：`apps/client/src/pages/chat/index.vue`
2. **检查 `onLoad` 参数读取**：
   ```ts
   import { onLoad } from "@dcloudio/uni-app";
   onLoad((options) => {
     const conversationId = options?.conversationId;
     if (!conversationId) {
       // 兜底：从 messagesStore 取最近会话，或展示空态引导
       return;
     }
     messagesStore.setCurrentConversation(conversationId);
   });
   ```
3. **检查路由配置**：`apps/client/src/constants/routes.ts` 中 chat 路由是否支持 query 参数
4. **构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P0-3：多个页面空白（03/04/27/28/29/33）

**共性根因**：这些页面均通过路由进入，但 `onLoad`/`onShow` 中数据加载失败或组件未正确挂载。

**逐个修改**：

| 截图 | 页面文件 | 修改要点 |
|------|----------|----------|
| 03-discover-history | `apps/client/src/pages/discover/history.vue` | 检查 `onLoad` 中是否调用 `discoverStore.loadHistory()`，若 store 方法不存在则补上；增加 EmptyState 兜底 |
| 04-discover-video-player | `apps/client/src/pages/discover/video-player.vue` | 检查视频 URL 来源，若为 mock:// 协议需替换为真实视频资源或占位海报；增加播放失败提示 |
| 27-campus-certification | `apps/client/src/pages/campus/certification.vue` | 检查页面是否被 `LockScreen` 或 `usePageAccess` 拦截；若需认证才可见，应展示引导而非空白 |
| 28-daily-question | `apps/client/src/pages/daily-question/index.vue` | 检查 `dailyQuestionStore.fetchQuestion()` 调用；若后端 `/daily-question` 端点缺失，需后端补端点 |
| 29-love-center | `apps/client/src/pages/love-center/index.vue` | 检查路由是否正确注册到 `pages.json`；若为分包页面需确认分包配置 |
| 33-heart-signals | `apps/client/src/pages/heart-signals/index.vue` | 检查 `featureFlags.heartSignalEnabled`（当前为 `true`），若为 false 则页面被隐藏；检查数据加载 |

**统一构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P0-4：话题详情页"话题不存在"（22/26）

**根因分析**：
- `apps/client/src/pages/circles/topic-detail.vue` 通过 `id=1` 查询话题，但 `campus_topics` 表为空（验收报告-2026-08-07 已确认）
- 后端返回 404 或空数据，前端展示"话题不存在或已被删除"

**修改方案**：

1. **后端：填充 `campus_topics` 表数据**
   - 文件：`database/flyway/sql/V2026.08.07.0024__seed_circle_posts_and_topics.sql`
   - 新增 INSERT 语句，插入 8-10 条话题数据（标题、描述、浏览量）
   - 示例：
     ```sql
     INSERT INTO campus_topics (id, title, description, view_count, created_at) VALUES
     (1, '校园日常', '分享你的校园生活点滴', 1280, NOW()),
     (2, '表白墙', '勇敢说出你的心意', 1024, NOW()),
     ...
     ```
   - 执行：`mvn flyway:migrate -Dflyway.configFiles=...` 或直接 MySQL 客户端执行

2. **前端：话题不存在时展示引导发帖而非纯空态**
   - 文件：`apps/client/src/pages/circles/topic-detail.vue`
   - 当前已有"去发布"按钮，保留即可

3. **构建验证**：
   - 后端：`mvn clean package -DskipTests`
   - 前端：`pnpm --filter client run build:mp-weixin`

---

### P0-5：标签帖子页报错（10-village-tag-posts.png）

**根因分析**：
- 错误信息：`getPostsByTag.tagName: tagName 不能为空`
- 路由 `/pages/village/tag-posts?tag=摄影` 中 `tag` 参数未正确传递到 API 请求

**修改方案**：

1. **文件**：`apps/client/src/pages/village/tag-posts.vue`
2. **检查 `onLoad` 参数读取**：
   ```ts
   onLoad((options) => {
     const tagName = options?.tag;
     if (!tagName) {
       // 兜底：从路由 query 中取，或展示错误
       uni.showToast({ title: "缺少标签参数", icon: "none" });
       return;
     }
     villageStore.fetchPostsByTag(tagName);
   });
   ```
3. **检查 API 调用**：`villageStore.fetchPostsByTag` 是否正确将 `tagName` 传给后端 `/post-tags/posts?tagName=xxx`
4. **构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P0-6：推荐偏好页加载失败（49-sub-setup-recommend-pref.png）

**根因分析**：
- 错误信息："获取偏好设置时出现问题，请重试"
- 后端 `/recommendations/preferences/1` 返回 404（`_run-errors.json` 已记录）

**修改方案**：

1. **后端：新增 `/api/v1/recommendations/preferences` 端点**
   - 文件：`apps/api/src/main/java/com/campuslove/api/controller/RecommendationController.java`
   - 新增 `GET /preferences/{userId}` 和 `PUT /preferences/{userId}` 端点
   - 对应表：`user_recommendation_preferences`（若表不存在需新建）

2. **前端兜底**：
   - 文件：`apps/client/src/subpackages/setup/recommend-pref/index.vue`
   - 加载失败时展示默认偏好选项（年龄范围、距离范围、性别偏好），允许用户手动设置后 POST 创建

3. **构建验证**：
   - 后端：`mvn clean package -DskipTests`
   - 前端：`pnpm --filter client run build:mp-weixin`

---

### P1-1：喜欢页无模糊/解锁逻辑（05-likes.png）

**根因分析**：
- `apps/client/src/pages/likes/index.vue` 展示 `likedBy` 列表，所有用户清晰可见
- 反馈文档要求"默认前 2 条可见、其余模糊，点击解锁按钮模拟付费解锁"
- 当前页面未接入 `useCoinsStore` 的解锁逻辑

**修改方案**：

1. **文件**：`apps/client/src/pages/likes/index.vue`
2. **修改 `likedBy` 渲染逻辑**：
   ```vue
   <template>
     <view v-for="(user, index) in likedBy" :key="user.id">
       <!-- 前 2 条清晰展示 -->
       <view v-if="index < 2 || isUnlocked" class="user-card">
         <SafeImage :src="user.avatar" />
         <text>{{ user.nickname }}</text>
       </view>
       <!-- 其余模糊 + 解锁按钮 -->
       <view v-else class="user-card blurred">
         <SafeImage :src="user.avatar" :blur="true" />
         <text>匿名用户</text>
         <button @click="handleUnlock">解锁后查看完整资料</button>
       </view>
     </view>
   </template>
   ```
3. **接入解锁逻辑**：参考 `apps/client/src/pages/likes-visitors/index.vue` 第 131 行 `handleUnlock` 实现
4. **构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P1-2：圈子页 Tab 结构不符（07-village.png）

**根因分析**：
- `apps/client/src/pages/circle/index.vue` 第 31-37 行：Tab 为 `recommend/following/campus/love/treehole`
- 反馈文档要求一级 Tab 为"关注/同城/发现"，发现页下二级分类为"全部/校友/老乡/搭子圈"

**修改方案**：

1. **文件**：`apps/client/src/pages/circle/index.vue`
2. **修改 Tab 结构**：
   ```ts
   // 一级 Tab
   const primaryTabs = [
     { key: "following", label: "关注" },
     { key: "sameCity", label: "同城", icon: "location" },
     { key: "discover", label: "发现" },
   ];
   // 发现页二级 Tab（仅 discover 选中时显示）
   const discoverSubTabs = [
     { key: "all", label: "全部" },
     { key: "alumni", label: "校友" },
     { key: "hometown", label: "老乡" },
     { key: "buddy", label: "搭子圈" },
   ];
   ```
3. **同城 Tab 定位逻辑**：
   - 使用 `uni.getLocation()` 获取当前城市
   - Tab 下方显示城市名（如"南京"）
   - 点击弹出城市选择器（`uni.chooseLocation` 或自定义 Picker）
4. **热门话题区**：
   - 在发现页二级 Tab 下方增加热门话题模块
   - 左侧大卡片 + 右侧 2×2 小卡片布局
   - 数据源：`campus_topics` 表（需先填充数据，见 P0-4）
5. **构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P1-3：发帖页缺少标题/图片上传（08-village-post.png）

**根因分析**：
- `apps/client/src/pages/village/post.vue` 第 39 行 `content` 为正文，无 `title` 字段
- 第 41 行 `images` 数组存在，但模板中可能未渲染上传入口

**修改方案**：

1. **文件**：`apps/client/src/pages/village/post.vue`
2. **增加标题输入框**：
   ```vue
   <view class="title-section">
     <text class="label">标题（必填）</text>
     <input v-model="title" placeholder="请输入标题（5-30字）" maxlength="30" />
     <text class="counter">{{ title.length }}/30</text>
   </view>
   ```
   ```ts
   const title = ref("");
   // 提交时校验
   function handleSubmit() {
     if (!title.value.trim()) {
       uni.showToast({ title: "请输入标题", icon: "none" });
       return;
     }
     // ...
   }
   ```
3. **增加图片上传入口**：
   ```vue
   <view class="image-section">
     <text class="label">添加图片（最多9张）</text>
     <view class="image-grid">
       <view v-for="(img, idx) in images" :key="idx" class="image-item">
         <SafeImage :src="img" />
         <button @click="removeImage(idx)">×</button>
       </view>
       <view v-if="images.length < 9" class="add-image" @click="chooseImage">
         <text>+</text>
       </view>
     </view>
   </view>
   ```
   ```ts
   async function chooseImage() {
     const res = await uni.chooseImage({ count: 9 - images.value.length });
     images.value.push(...res.tempFilePaths);
   }
   ```
4. **构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P1-4：帖子详情页无楼中楼评论（09-village-detail.png）

**根因分析**：
- `apps/client/src/pages/village/detail.vue` 第 29 行 `comments` 从 `villageStore` 获取
- 后端 `comments` 表无 `parent_id` 字段（验收报告-2026-08-07 已确认），无法支持楼中楼

**修改方案**：

1. **后端：`comments` 表增加 `parent_id` 字段**
   - 文件：`database/flyway/sql/V2026.08.08.0001__add_comment_parent_id.sql`（新建）
   ```sql
   ALTER TABLE comments ADD COLUMN parent_id BIGINT UNSIGNED NULL COMMENT '父评论ID，楼中楼回复';
   ALTER TABLE comments ADD INDEX idx_parent_id (parent_id);
   ```
   - 执行：`mvn flyway:migrate`

2. **后端：评论接口支持 `parent_id` 参数**
   - 文件：`apps/api/src/main/java/com/campuslove/api/controller/CommentController.java`
   - `POST /comments` 请求体增加 `parentId` 字段
   - `GET /comments?postId=xxx` 返回树形结构（根评论 + 子评论列表）

3. **前端：评论区渲染楼中楼**
   - 文件：`apps/client/src/pages/village/detail.vue`
   - 根评论正常渲染，每条根评论下方展示子评论列表（缩进 + 不同背景色）
   - 点击根评论的"回复"按钮，弹出输入框，提交时携带 `parent_id`

4. **构建验证**：
   - 后端：`mvn clean package -DskipTests` + `mvn flyway:migrate`
   - 前端：`pnpm --filter client run build:mp-weixin`

---

### P1-5：匹配卡片缺少字段（30-love-center-nearby.png）

**根因分析**：
- `apps/client/src/components/discover/CardSwiper.vue` 中卡片渲染逻辑
- 后端 `users` 表 45 个用户 `avatar_url` 为空，`user_basic_profile` 43 个用户无记录
- 卡片字段映射可能未覆盖认证标识、活跃状态、MBTI、动态预览、期待画像

**修改方案**：

1. **后端：确保推荐接口返回完整字段**
   - 文件：`apps/api/src/main/java/com/campuslove/api/service/RecommendationService.java`
   - 返回 DTO 需包含：`avatarUrl`、`certificationStatus`（机器+人工）、`lastActiveAt`、`mbti`、`latestPost`（最新 1 条动态）、`expectationProfile`

2. **前端：CardSwiper 组件补充字段渲染**
   - 文件：`apps/client/src/components/discover/CardSwiper.vue`
   - 在卡片身份区增加认证标识（双认证图标）
   - 在基础资料区增加活跃状态（"刚刚活跃"/"X 小时前"）
   - 在标签区增加 MBTI 标签
   - 在卡片底部增加动态预览区（最新 1 条动态的缩略图 + 正文前 2 行 + 点赞/评论数）
   - 增加期待画像区（"我期待遇见的你" + 用户填写的期待描述）

3. **数据修复**：
   - 为 45 个无头像用户分配默认头像（从 `src/static/assets/images/avatars/` 中选取）
   - 为 43 个无 `basic_profile` 用户创建扩展记录

4. **构建验证**：
   - 后端：`mvn clean package -DskipTests`
   - 前端：`pnpm --filter client run build:mp-weixin`

---

### P1-6：VIP 会员页应隐藏（40-vip.png）

**根因分析**：
- `apps/client/src/config/feature-flags.ts` 第 29 行 `membershipEnabled: false`
- 但 VIP 页面仍可通过路由直接访问

**修改方案**：

1. **文件**：`apps/client/src/pages/vip/index.vue`
2. **增加功能开关检查**：
   ```ts
   import { featureFlags } from "../../config/feature-flags";
   import { onLoad } from "@dcloudio/uni-app";

   onLoad(() => {
     if (!featureFlags.membershipEnabled) {
       uni.showToast({ title: "会员功能暂未开放", icon: "none" });
       setTimeout(() => uni.navigateBack(), 1500);
     }
   });
   ```
3. **隐藏所有 VIP 入口**：
   - `apps/client/src/pages/profile/index.vue`：搜索 `membershipEnabled` 或 `vip`，确保所有 VIP 入口被 `v-if="featureFlags.membershipEnabled"` 包裹
   - `apps/client/src/pages/home/index.vue`：若有 VIP Banner，同样隐藏
4. **构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P1-7：反馈提交按钮文字未翻译（50-sub-support-feedback.png）

**根因分析**：
- `apps/client/src/subpackages/support/feedback/index.vue` 第 516 行：`:primary-label="isSubmitting ? t('common.submitting') : t('common.submit')"`
- i18n 文件中缺少 `common.submit` 的中文翻译

**修改方案**：

1. **文件**：`apps/client/src/locales/zh-CN.json`（或对应中文语言文件）
2. **增加翻译**：
   ```json
   {
     "common": {
       "submit": "提交",
       "submitting": "提交中..."
     }
   }
   ```
3. **构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P1-8：钱包收支明细逻辑混乱（43-wallet.png）

**根因分析**：
- 截图显示"支出：每日签到奖励 -¥1"，奖励应为收入而非支出
- `apps/client/src/pages/wallet/index.vue` 中交易类型映射错误

**修改方案**：

1. **文件**：`apps/client/src/pages/wallet/index.vue`
2. **修改交易类型渲染**：
   ```vue
   <view v-for="tx in transactions" :key="tx.id" class="tx-item">
     <text>{{ tx.description }}</text>
     <text :class="tx.type === 'income' ? 'income' : 'expense'">
       {{ tx.type === 'income' ? '+' : '-' }}¥{{ tx.amount }}
     </text>
   </view>
   ```
3. **后端：确保 `wallet_transaction_log` 表的 `type` 字段正确区分 income/expense**
   - 文件：`apps/api/src/main/java/com/campuslove/api/entity/WalletTransactionLog.java`
   - 签到奖励、任务奖励、充值应为 `income`；消费、解锁应为 `expense`

4. **构建验证**：
   - 后端：`mvn clean package -DskipTests`
   - 前端：`pnpm --filter client run build:mp-weixin`

---

### P2-1：圈子页帖子头部缺少字段（07-village.png）

**修改方案**：

1. **文件**：`apps/client/src/pages/circle/index.vue`
2. **帖子头部增加年龄、城市、学历/职业**：
   ```vue
   <view class="post-header">
     <SafeImage :src="post.avatar" class="avatar" />
     <view class="user-info">
       <text class="nickname">{{ post.nickname }}</text>
       <text class="meta">{{ post.age }}岁 · {{ post.city }} · {{ post.education }}</text>
     </view>
     <button v-if="!post.isFollowing" class="follow-btn">+ 关注</button>
   </view>
   ```
3. **后端：帖子列表接口返回用户年龄、城市、学历**
   - 文件：`apps/api/src/main/java/com/campuslove/api/controller/PostController.java`
   - 返回 DTO 关联 `user_basic_profile` 表的 `age`、`city`、`education` 字段

4. **构建验证**：
   - 后端：`mvn clean package -DskipTests`
   - 前端：`pnpm --filter client run build:mp-weixin`

---

### P2-2：访客记录数据不一致（16-profile-visitors.png vs 06-likes-visitors.png）

**根因分析**：
- `16-profile-visitors.png` 来自 `apps/client/src/pages/profile/visitors.vue`，显示"暂无访客记录"
- `06-likes-visitors.png` 来自 `apps/client/src/pages/likes-visitors/index.vue`，显示 28 条今日浏览量
- 两个页面数据源不同：`profile/visitors` 可能查 `profile_visitors` 表（仅 1 条），`likes-visitors` 查 `visitors` 表（25 条）

**修改方案**：

1. **统一数据源**：
   - 文件：`apps/client/src/pages/profile/visitors.vue`
   - 改为从 `visitors` 表获取数据（与 `likes-visitors` 一致）
   - 或后端合并两个表的数据

2. **构建验证**：`pnpm --filter client run build:mp-weixin`

---

### P2-3：话题列表为空（21-circles-topics.png）

**修改方案**：

1. **后端：填充 `circle_topics` 表数据**（当前 10 条，但前端可能查询条件不匹配）
   - 检查 `apps/api/src/main/java/com/campuslove/api/controller/CircleTopicController.java` 的查询逻辑
   - 确保 `GET /circle-topics` 返回所有话题

2. **前端：话题列表页增加空态引导**
   - 文件：`apps/client/src/pages/circles/topics.vue`
   - 当前已有"去发布"按钮，保留即可

3. **构建验证**：
   - 后端：`mvn clean package -DskipTests`
   - 前端：`pnpm --filter client run build:mp-weixin`

---

## 十二、修改优先级与执行顺序

### 第一批（P0，核心功能修复，预计 1-2 天）

1. P0-1：匹配卡片空白 → 后端确认推荐接口 + 前端 EmptyState 兜底
2. P0-2：聊天页面空白 → 路由参数传递修复
3. P0-4：话题不存在 → 后端填充 `campus_topics` 数据
4. P0-5：标签帖子页报错 → 路由参数传递修复
5. P0-6：推荐偏好页加载失败 → 后端新增 preferences 端点

### 第二批（P1，功能完善，预计 2-3 天）

6. P1-1：喜欢页模糊/解锁逻辑
7. P1-2：圈子页 Tab 结构重构
8. P1-3：发帖页标题/图片上传
9. P1-4：楼中楼评论（需后端改表结构）
10. P1-5：匹配卡片字段补全
11. P1-6：VIP 会员页隐藏
12. P1-7：i18n 翻译修复
13. P1-8：钱包收支明细逻辑

### 第三批（P2，体验优化，预计 1 天）

14. P2-1：帖子头部字段补全
15. P2-2：访客记录数据源统一
16. P2-3：话题列表数据填充

### 构建验证总命令

每批修改完成后，执行：

```bash
# 后端
cd apps/api
mvn clean package -DskipTests
mvn flyway:migrate  # 若有数据库变更

# 前端
cd ../client
pnpm --filter client run build:mp-weixin

# 管理后台（若有修改）
cd ../admin
pnpm --filter admin run build
```

全部退出码 0 即构建通过。

---

## 十三、验收标准（修改后重新截图验收）

修改完成后，需重新执行截图走查，验证以下页面：

1. **02-discover.png**：匹配卡片区域应显示用户卡片（头像、昵称、认证、距离、活跃、基础资料、自我描述、标签、MBTI、期待画像、动态预览）
2. **12-chat.png**：聊天页面应显示会话列表或聊天内容
3. **22-circles-topic-detail.png**：话题详情页应显示话题内容
4. **10-village-tag-posts.png**：标签帖子页应显示对应标签的帖子列表
5. **49-sub-setup-recommend-pref.png**：推荐偏好页应显示偏好设置选项
6. **05-likes.png**：喜欢页前 2 条清晰，其余模糊 + 解锁按钮
7. **07-village.png**：圈子页一级 Tab 为"关注/同城/发现"，发现页有二级分类 + 热门话题区
8. **08-village-post.png**：发帖页有标题输入框 + 图片上传入口
9. **09-village-detail.png**：帖子详情页评论区支持楼中楼
10. **40-vip.png**：VIP 会员页应被隐藏或提示"暂未开放"
11. **50-sub-support-feedback.png**：提交按钮文字应为"提交"而非"common.submit"
12. **43-wallet.png**：收支明细中奖励应为"+"收入，消费应为"-"支出

---

**报告生成时间**：2026-08-08
**截图总数**：54 张
**验收页面**：54 个
**已实现功能**：17 项
**未实现/有问题功能**：12 项
**修改方案**：16 项（P0 六项、P1 八项、P2 三项）

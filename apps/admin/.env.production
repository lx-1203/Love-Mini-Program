# 生产环境配置（vite build 时加载）
# 后端 API 基础地址：生产部署走同源反向代理（/api → 后端），避免浏览器 CORS 跨域。
# 部署方式：构建产物由 nginx 静态托管（见 apps/admin/docker/nginx.conf），
# nginx 将 /api 前缀请求反代到后端 API 服务（docker 内 campus-api:8080），
# 浏览器始终访问同源地址，无需在页面中硬编码后端 IP/端口。
VITE_API_BASE_URL=/api

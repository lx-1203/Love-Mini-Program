# 展示版（Showcase）微信小程序构建环境变量
#
# 说明：
# - 本文件由 `uni build --platform mp-weixin --mode mp-weixin-showcase` 加载
#   （uni-app 构建 mode 对应 .env.mp-weixin-showcase）。
# - 展示版以「超级管理员」视角展示全部功能：开启 VITE_SHOWCASE_MODE 后
#   showcase.ts 会将全部 featureFlags 置 true、旁路路由守卫、VIP 全亮。
# - 数据源为后端 mock profile（真实 HTTP + 内存数据 + 免认证）：
#   VITE_API_MODE=real 表示走 HTTP 接口；VITE_API_BASE_URL 指向后端 mock 服务。
# - 微信开发者工具需勾选「不校验合法域名」（本地 http://127.0.0.1:8080）。
VITE_SHOWCASE_MODE=true
VITE_API_MODE=real
VITE_API_BASE_URL=http://127.0.0.1:8080/api
VITE_APP_VERSION=v0.1.0-showcase

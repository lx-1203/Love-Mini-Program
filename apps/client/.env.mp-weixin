# 微信小程序构建环境变量
#
# 说明：
# - @dcloudio/vite-plugin-uni 在 `uni build --platform mp-weixin` 时使用的 mode 为 `mp-weixin`，
#   不会自动加载 .env.production，因此需要单独维护本文件。
# - 小程序发布构建默认使用 real 模式（infra #33）。
# - 构建前必须将 VITE_API_BASE_URL 替换为真实后端域名（微信小程序合法域名
#   必须为 HTTPS 且已在小程序后台配置 request 合法域名），例如：
#     VITE_API_MODE=real
#     VITE_API_BASE_URL=https://api.your-domain.com/api
# - 如需本地临时预览（无后端），可临时改回 mock 模式（前端 mock 数据），
#   但严禁携带 mock 配置发布生产包：
#     VITE_API_MODE=mock
VITE_API_MODE=real
# 本地联调经 HTTPS 反向代理（scripts/dev-https-proxy.mjs）转发到 8080：
# 微信基础库 3.x 起 <image> 拒绝加载 http:// 图片，必须走 https（开发者工具勾选「不校验 TLS 证书」）。
# ⚠️ 发布前必须恢复为真实 HTTPS 域名
VITE_API_BASE_URL=https://127.0.0.1:8443/api
VITE_APP_VERSION=v0.1.0

# 生产构建环境变量（H5 生产构建）
#
# 说明（infra #32）：
# - 生产构建默认使用 real 模式调用真实后端 API。
# - 构建前必须将 VITE_API_BASE_URL 替换为真实后端域名（微信小程序合法域名
#   必须为 HTTPS），例如：
#     VITE_API_MODE=real
#     VITE_API_BASE_URL=https://api.your-domain.com/api
# - 如需本地临时预览（无后端），可临时改回 mock 模式（前端 mock 数据），
#   但严禁携带 mock 配置发布生产包：
#     VITE_API_MODE=mock
VITE_API_MODE=real
VITE_API_BASE_URL=https://api.campuslove.example.com/api
VITE_APP_VERSION=v0.1.0

# 生产构建环境变量（小程序发布包默认使用 mock 模式）
#
# 说明：
# - 项目当前没有线上真实后端域名，因此生产构建默认使用 mock 模式，
#   所有业务接口由前端 mock 数据提供。
# - VITE_API_BASE_URL 在 mock 模式下仅作为占位地址使用，不会被真实请求命中；
#   切换为 real 模式时，必须将其替换为真实后端域名，例如：
#   VITE_API_MODE=real
#   VITE_API_BASE_URL=https://your-domain.com/api
VITE_API_MODE=mock
VITE_API_BASE_URL=https://mock.invalid/api
VITE_APP_VERSION=v0.1.0

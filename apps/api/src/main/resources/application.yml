spring:
  application:
    name: campus-love-api
  profiles:
    default: mock
server:
  port: ${APP_PORT:8080}

# 微信小程序配置
wechat:
  miniapp:
    app-id: ${WECHAT_APP_ID:wx_test_appid}
    app-secret: ${WECHAT_APP_SECRET:test_secret}

# 文件上传配置
campuslove:
  upload:
    dir: ${UPLOAD_DIR:uploads}
